# ── Lifeform 工作平台 · 数据源配置模板 ──
# 复制为 .env 后按需修改（.env 已被 .gitignore 忽略，不入库）
#
# VITE_DATA_SOURCE 取值：
#   api  —— 真实 API（Phase 3 交付/验收口径，需求设计文档 §8 R-C09「阶段完成时必须切换真实 API」）
#   mock —— 静态演示数据（视图 UI 先行开发用，需求设计文档 §8「Mock 先行」）
# 切换零 UI 改动：两种数据源由 WorkPlatformDataProvider 统一抽象。
# api 模式下若后端不可达，请求会降级为演示数据并在页面显式标注「演示数据」，不会静默伪造成功。
VITE_DATA_SOURCE=api

# BFF 前缀（开发经 Vite 代理转发到 wp-bff，默认 http://127.0.0.1:8090）
VITE_API_BASE=/api/wp

# 多租户标识（默认租户）
VITE_TENANT_ID=default
