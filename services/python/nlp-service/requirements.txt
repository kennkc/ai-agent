# NLP service dependencies (Phase 0 minimal skeleton; models introduced in Phase 2)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
# Phase 4 brain stage: LangGraph orchestration (R4-06) + Redis client for semantic cache (R4-04)
langgraph>=0.2
redis>=5.0
# Phase 6 / WB-10: credential encryption for model-access configuration (AES-256-GCM)
cryptography>=42.0.0
# Brain-layer Postgres base (audit IN3 / memory graph IN-02 / model config WB-10).
# Was an implicit dependency (lazy import in app/brain/pg.py); declared explicitly so one install covers it.
psycopg2-binary>=2.9.9
