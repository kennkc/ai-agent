server:
  port: 8081

app:
  nlp:
    base-url: ${NLP_BASE_URL:http://127.0.0.1:8000}
    # true：意图识别不可用时降级为 FALLBACK（问答仍返回模板回答）；false：直接返回 502 上游不可用
    degrade-on-failure: ${NLP_DEGRADE_ON_FAILURE:true}
  body:
    base-url: ${BODY_BASE_URL:http://127.0.0.1:8083}
    degrade-on-failure: ${BODY_DEGRADE_ON_FAILURE:true}
  # Phase 4 大脑层（R4-06）：规划→检索→生成→来源标注
  brain:
    degrade-on-failure: ${BRAIN_DEGRADE_ON_FAILURE:true}

grpc:
  # 默认 19092：原规划的 9092 与 Kafka 宿主端口（127.0.0.1:9092）冲突，
  # 会导致启动失败 `Failed to start gRPC health server on port 9092`，故整体外移到 19092。
  # 见《Agent-Lifeform 服务注册表》§5 与《技术债台账》DEBT-018。
  port: ${GRPC_PORT:19092}

spring:
  application:
    name: session-manager
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: ${OTLP_ENDPOINT:http://localhost:4318/v1/traces}

logging:
  level:
    com.agent: INFO
