<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.agent</groupId>
        <artifactId>agent-lifeform-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>
    <artifactId>proto-contracts</artifactId>
    <name>proto-contracts</name>
    <description>Generated Java contracts for proto/</description>

    <!--
      设计说明（构建可移植性调整，2026-09-12）：
      protobuf/gRPC 生成结果已随仓库固化在 src/main/java 下（90 个文件，含 6 个 *Grpc.java），
      默认构建不再调用 protoc，原因：
        1) 免去每次构建下载/解包 protoc 与 protoc-gen-grpc-java（离线环境构建更稳）；
        2) 规避部分 Windows 环境下 protobuf-maven-plugin 清理 target/protoc-dependencies
           时 File.delete 长时间阻塞的问题（详见 docs/Phase2-DEMO.md「构建环境说明」）。
      proto 契约变更后，用 `mvn -Pproto-gen -pl proto-contracts generate-sources` 重新生成，
      生成目录为 target/generated-sources/protobuf，再同步回 src/main/java。
    -->
    <dependencies>
        <dependency><groupId>com.google.protobuf</groupId><artifactId>protobuf-java</artifactId><version>${protobuf.version}</version></dependency>
        <dependency><groupId>io.grpc</groupId><artifactId>grpc-protobuf</artifactId></dependency>
        <dependency><groupId>io.grpc</groupId><artifactId>grpc-stub</artifactId></dependency>
        <dependency><groupId>javax.annotation</groupId><artifactId>javax.annotation-api</artifactId><version>1.3.2</version></dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>proto-gen</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.xolstice.maven.plugins</groupId>
                        <artifactId>protobuf-maven-plugin</artifactId>
                        <version>0.6.1</version>
                        <configuration>
                            <protoSourceRoot>${project.basedir}/../../../proto</protoSourceRoot>
                            <protocArtifact>com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}</protocArtifact>
                            <pluginId>grpc-java</pluginId>
                            <pluginArtifact>io.grpc:protoc-gen-grpc-java:${grpc.version}:exe:${os.detected.classifier}</pluginArtifact>
                        </configuration>
                        <executions>
                            <execution>
                                <goals><goal>compile</goal><goal>compile-custom</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
