# dev 分支代码分析与开发进度验证

| 项 | 内容 |
|---|---|
| 项目 | Agent-Lifeform · AI Agent 生命体架构（`E:\AI\ai-agent`） |
| 分析日期 | 2026-09-12 |
| 新分支 | `dev`（基于 `origin/workbuddy/main`） |
| 基线提交 | `9385f23 docs(phase2): 新增项目进度日志报告目录…` |
| 分析方式 | git 分支/提交核对 + 全量文件清单 + 关键源码走查 + 交付文档比对；**2026-09-13 追加动态验证**（本机工具链已装齐，重跑构建、VS1 联调与业务链路冒烟） |
| 结论 | Phase 0/1/2 均已落地，代码为真实实现（非占位）；本机已可完整编译与容器级联调；冒烟发现 **1 个真实集成缺陷**（D-1，**已于 2026-09-13 修复并通过冒烟复测**，见 §八） |

---

## 一、分支操作记录

```
git fetch --all
git checkout -b dev origin/workbuddy/main
# Switched to a new branch 'dev'
# branch 'dev' set up to track 'origin/workbuddy/main'.
```

| 分支 | HEAD | 定位 |
|---|---|---|
| `main` | `a305c5e` | 仅初始 README（近乎空壳），非开发主线 |
| **`dev`（新建）** | **`9385f23`** | **基于 `workbuddy/main`，后端主线，用于后续开发** |
| `origin/workbuddy/main` | `9385f23` | 实际开发主线（Phase 0→2） |
| `origin/codex/phase0-1-hardening` | `1df6651` | 前端 Vue3 工作台线（含 7 个提交，未并入主线） |

> `dev` 本地领先 `main` 共 **10 个提交**；相对 `codex` 分支为 **领先 3 / 落后 7**（两条线已分叉，详见 §五）。

---

## 二、代码规模与结构

仓库共 **219 个文件**（不含 `.git`）。

| 层次 | 模块 | 文件 | 行数 |
|---|---|---:|---:|
| Java | gateway-service（路由/JWT/租户透传） | 6 | 224 |
| Java | session-manager（会话/总线/VS1 问答） | 13 | 696 |
| Java | **sense-service（五感采集，Phase 2 主体）** | **43** | **4070**（主 3322 + 测 748） |
| Java | body-service（本地检索 MVP） | 5 | 136 |
| Java | proto-contracts（**生成源码，已入库**） | 90 | 41 900 |
| Python | nlp-service（规则+L0 级联、OCR 代理） | 12 | 1183 |
| 前端 | web/console + web/work-platform（静态原型） | 3 | 1659 |
| 契约 | proto/*.proto（6 个） | 6 | 367 |

sense-service 主代码按包分布（36 个业务类）：`channel` 7、`staging` 4、`controller` 2、`dynamic` 2、`nats` 2，其余 `collect/normalize/quality/schedule/health/deadletter/event/metrics/model/security/util/config/common/grpc` 各 1。

顶层结构：`proto/` `contracts/` `services/java|python` `web/` `infra/` `scripts/` `docs/`，配 `docker-compose.yml`（redis / postgres / qdrant / nats / nacos / minio / jaeger / kafka 共 8 个中间件）与 `.gitlab-ci.yml`（build → test → package 三段流水线）。

---

## 三、开发进度验证（按阶段）

### Phase 0 胚胎期 — ✅ 完成
Monorepo + Maven 多模块 + Python FastAPI 骨架 + docker-compose 基础设施 + Nacos 注册 + gRPC 健康服务（端口 9091–9094）+ proto 生成 + CI 骨架 + 启动/健康检查脚本。

### Phase 1 神经期 — ✅ 完成
网关路由（session/sense/nlp/body）+ JWT 鉴权与租户透传 + BusProxy（NATS）+ Kafka 生产/消费基线 + OTLP 链路追踪 + Jaeger + 统一错误码 + 跨租户保护 + SSRF 防护 + **VS1 主链路**（session → intent → 本地检索 → 模板回答）。

### Phase 2 感官期 — ✅ 完成（1 项受外部依赖降级）
需求 **R2-01 ~ R2-10**：Must 6/6 = 100%，Should 3/4 完成 + R2-03（OCR）因本机未装引擎待补测。

代码走查确认「真实现、非骨架」：

- **渠道可插拔（R2-01）**：`SenseChannel` 接口 + `ChannelRegistry` 经 Spring 容器自动发现 Bean；`TouchChannel`（URL/文件/纯文本，jsoup 正文提取 + 编码探测）、`VisualChannel`、`AudioChannel`、`NoseChannel`、`TasteChannel` 五渠道齐备。
- **采集主链路（R2-02/04/09/10）**：`CollectPipeline` 串起 渠道路由（隔离降级）→ resilience4j 指数退避重试（max-attempts=3）→ 五字段标准化 → 质检 → 隔离暂存 → 事件 `lifeform.sense.collected` → 指标；重试耗尽落 `DeadLetterStore`。失败出口完整。
- **质检（R2-10）**：`QualityGate` 分硬规则 `rejectReason()`（EMPTY_CONTENT / ENCODING_GARBLED / REPETITIVE_GARBAGE / BELOW_QUALITY_THRESHOLD）与加权评分（0.45 内容 + 0.20 长度 + 0.20 可打印 + 0.15 密度），且 `pass()` 委托 `rejectReason()` 实现**单一事实来源**——与阶段报告 §6-B1 记录的问题修复一致，代码中已落实。
- **渠道韧性（R2-09）**：`ChannelHealthMonitor` 周期探针 + DEGRADED/DOWN 阈值 + 自动隔离 + 恢复计数。
- **意图双级级联（R2-07/08）**：`rules.py`（关键词+正则+句尾位置权重，12 场景，阈值 0.35）→ `l0_model.py` 兜底 → 不可判归「情感闲聊」，结果带 `engine` 字段（RULE/L0/FALLBACK），与文档描述一致。

### 静态校验补充
- 全仓库 grep `TODO / FIXME / not implemented`：**无遗留占位标记**（仅命中错误码注释中的格式示例）。
- 生成产物（90 个 Java stub）已入库，`protobuf-maven-plugin` 移出默认生命周期（与 A-1 卡死修复方案一致）。

---

## 四、测试与验收证据（文档记载；2026-09-12 起已在本机复跑）

| 层次 | 用例 | 结果 |
|---|---:|---|
| Java 单测 | 52（sense 45 / session 4 / gateway 2 / body 1） | 0 失败 0 错误 |
| Python 单测 | 19（意图 15 / OCR 4） | 全通过 |
| **合计** | **71** | **100% 通过** |

硬指标：L0 留出集准确率 **90.0%**（目标 ≥85%）、P99 延迟 **0.052 ms**（目标 <50ms）、场景覆盖 **12**（要求 ≥10）、`contract-check.py` **0 FAIL**。

> ⚠️ **重要说明**：上述数据出自 `docs/项目进度日志报告/Phase2-测试验收报告.md`（验收日期 2026-09-12，源机环境 JDK 21 + Maven 3.9.11 + Python 3.12.13）。
> 本文初稿（2026-09-12）为静态验证，当时本机尚无 JDK/Maven/Node/Docker。**该前提已于 2026-09-12 晚解除**：本机已安装 JDK 21.0.12.1、Maven 3.9.11、Node 22.23.2、Git 2.55、Docker Desktop 4.4.4（含 WSL2 后端、8 容器基础设施），并已重跑构建（Java `mvn clean package` → BUILD SUCCESS，54 测试全绿；前端 `npm ci && typecheck && build` 通过）。
> 2026-09-13 进一步完成容器级联调与业务链路冒烟，结果见 **§八**。

---

## 五、风险与后续开发注意项

| # | 事项 | 影响 | 建议 |
|---|---|---|---|
| 1 | ~~**本机缺工具链**（JDK/Maven/Node/Docker）~~ | **已解除**：JDK 21.0.12.1 / Maven 3.9.11 / Node 22.23.2 / Docker Desktop 4.4.4 均已装好，构建与容器联调通过 | 保留 `E:\software` 下的工具链以便复用 |
| 2 | `dev` 与 `codex/phase0-1-hardening` 已分叉 | 前端 **Vue3 工作台**（含 Vite/TS，约 +6454 行：OverviewView/TasksView/ChatView/ModuleView 等）只在 codex 线；本分支 `web/` 仍是静态原型 | 规划合并：先定前端归属，再决定 rebase/merge 策略 |
| 3 | ~~Docker 端到端冒烟未做~~ | **已做**：8 容器 + 4 Java + Python + 前端全部起通，`healthcheck.sh` 16/16 OK（见 §八）；原 `POST /api/session/{id}/ask` 因缺陷 **D-1** 返回 503，**已修复并通过冒烟复测（200 / LLEN=2）** | 无（后续新增跨服务调用请统一走 `OutboundHttp` / `HttpClients`） |
| 4 | OCR 引擎未装（PaddleOCR/Tesseract） | R2-03 端到端未验收，视觉渠道恒 `DEGRADED` | 需要视觉能力时安装引擎，无需改代码 |
| 5 | protobuf 生成源码入库 | `.proto` 变更需手工同步产物，易漏 | 后续加 CI：`-Pproto-gen` 重生成后 `git diff --exit-code` |
| 6 | 本地领先远程 1 个提交 | `850f49d` 当时仅本地 | 如多人协作再推送 |
| 7 | L0 为轻量实现（非 BERT 级） | 「日程提醒」类场景留出集仅 60% | Phase 7 按设计升级模型，当前满足总体 ≥85% |

---

## 六、Phase 3 下一步（躯体期）

Phase 2 已预留事件出口（`lifeform.sense.collected`）。Phase 3 目标是打通「感官 → 躯体」闭环：

1. 消费暂存事件，实现 ACCEPTED 批次入库通道（暂存 → 正式库）；
2. 文档切分 + 向量化 + Qdrant 索引，替换 body-service 的本地检索 MVP；
3. 语义检索 P99 < 500ms 与召回率指标，对接 Console 躯体视图；
4. 补齐 OCR 端到端验收与 CI proto 同步校验。

---

## 七、验证结论

- **分支操作**：`dev` 已基于 `workbuddy/main` 创建并建立跟踪，工作区干净，可直接开发。
- **代码真实性**：核心链路（采集编排/质检/健康隔离/意图级联）均为可用实现，无 TODO 占位。
- **进度**：Phase 0/1/2 **已完成**（Phase 2 文档记为通过），Phase 3–8 待开发；总进度约 **3/9 阶段**。
- **本机可复现性**：工具链已就绪，构建 / 单元测试 / 容器级联调均可在本机重跑（§八）。
- **当前唯一功能性阻碍**：缺陷 **D-1**（Java 侧 HTTP 客户端 h2c 升级导致调用 Python 服务失败），阻塞「问答」链路；修法明确、改动极小。
  → **2026-09-13 已修复**：统一锁定 HTTP/1.1 + 补齐超时 / 降级 / 错误码语义 / 回归用例，`ask` 与消息落库冒烟复测通过（§8.6）。

---

## 八、端到端动态验证（2026-09-13 追加）

### 8.1 环境

| 组件 | 版本 / 位置 |
|---|---|
| JDK | 21.0.12.1（`E:\software\java\jdk-21`） |
| Maven | 3.9.11 |
| Node / npm | 22.23.2 |
| Git | 2.55.0.windows.5 |
| Docker Desktop | **4.4.4 (build 73704)**，client/server 20.10.12，compose v2.2.3 |
| Python venv | `services/python/venv`（FastAPI 0.141.1 / uvicorn 0.52.4 / pydantic 2.13.5） |

> Docker 版本说明：本机为 Windows 10 **Build 19041**，Docker Desktop 4.50+ 要求 19045、4.49 起要求 19044，均无法安装；**4.4.4 是仍支持 19041 的最后版本线**。请勿把 Docker Desktop 升级到 19045 门槛以上的版本。

### 8.2 容器级联调（VS1）

8 个基础设施容器（redis / postgres-pgvector / qdrant / nats / nacos / minio / jaeger / kafka）+ 4 个 Java 服务 + nlp-service + 前端 work-platform 全部启动，`scripts/healthcheck.sh` **16 项全 OK**：

```
[infrastructure]   redis 6379 / postgres 5432 / qdrant 6333 / nats 4222 /
                   nacos 8848 / minio 9000 / kafka 9092 / jaeger 16686    8/8 OK
[java services]    gateway 8080 / session 8081 / sense 8082 / body 8083    4/4 HTTP 200
[grpc health]      9091 / 19092 / 9093 / 9094
[python+frontend]  nlp-service 8000 / work-platform 3001
```

Nacos 服务发现正常（4 个服务互相可见）；`scripts/contract-check.py` → **FAIL 0 / WARN 3**；`--work-platform` 全部通过。

### 8.3 业务链路冒烟（dev token → 建会话 → 发消息 → 落库 → Kafka）

| # | 步骤 | 结果 |
|---|---|---|
| 1 | `POST /api/auth/token?tenantId=default`（网关 dev 端点） | ✅ 200，签发 HS384 JWT（193 字符，loopback-only 校验生效） |
| 2 | 无 token 调 `POST /api/session` | ✅ 401 `AGENT_UNAUTHORIZED`（鉴权与租户透传生效） |
| 3 | `POST /api/session`（带 token） | ✅ 200，返回 `session_id`；Redis `session:{id}` 哈希写入 6 个字段、TTL 2h |
| 4 | `GET /api/session/{id}` | ✅ 200，`status=ACTIVE`、`message_count=0`，租户归属校验生效 |
| 5 | NATS 总线（`lifeform.rpc.sense.ping`） | ✅ 起 sense-service 时 `bus_channel=nats`，`bus_reply={"service":"sense-service","status":"pong","channel":"TOUCH"}`；不起则优雅降级为 `direct` |
| 6 | Kafka 事件 `lifeform.session.created` | ✅ 生产成功（offset=0），同服务 KafkaEventConsumer 成功消费到同一条 payload |
| 7 | `POST /api/body/ingest`（知识入库） | ✅ 200，`chunk_count=3`（直连与经网关均通过） |
| 8 | `POST /api/body/retrieve`（检索） | ✅ 200，命中 2 个 chunk（打分检索可用） |
| 9 | `POST /api/session/{id}/ask`（问答主链路） | ✅ 200 / 875ms，`intent=闲聊`、返回模板回答 + 1 条 citations（**修复 D-1 前为 503**） |
| 10 | 消息落库 `session:{id}:messages` | ✅ `LLEN=2`、`message_count=2`（user + assistant 各 1 条，latency_ms=171） |

**结论：会话生命周期、租户隔离、JWT 鉴权、NATS 同步总线、Kafka 生产/消费、知识入库与检索、**意图识别与问答主链路**、消息落库共 8 条链路全部实测通过（D-1 修复后）。**

### 8.4 缺陷 D-1：JDK HttpClient 的 h2c 升级打挂 Python 服务调用（**已于 2026-09-13 修复**）

**现象**：同一 URL，`curl` 直连 nlp-service 返回 200，但 session-manager 调用恒 503；nlp-service 侧日志为：

```
POST /api/nlp/intent HTTP/1.1" 422 Unprocessable Entity
WARNING:  Unsupported upgrade request.
WARNING:  Invalid HTTP request received.
```

**定位**（最小复现，无需改任何源码）：用 JDK 21 单文件程序对同一 URL、同一 JSON 分别以 HTTP/2 与 HTTP/1.1 发送：

```
HTTP_2   -> HTTP 422  {"detail":[{"type":"missing","loc":["body"],"msg":"Field required","input":null}]}
HTTP_1_1 -> HTTP 200  {"intent":"闲聊","confidence":0.5,"engine":"FALLBACK",...}
```

**根因**：`session-manager` 的 `NlpClient` / `BodyClient` 使用 `RestClient.builder().baseUrl(...).build()`，底层默认是 `JdkClientHttpRequestFactory` → `java.net.http.HttpClient`，其默认 `version = HTTP_2`。对**明文**连接，JDK 客户端会先发 `Connection: Upgrade, HTTP2-Settings` / `Upgrade: h2c` 做协议升级握手；uvicorn(h11) 不支持 h2c，直接把请求体吞掉 → FastAPI 报 `422 Field required(body)` → Java 侧抛 `RestClientException` → 包装成 503。

**影响面**：`NlpClient`（意图识别）与 `BodyClient`（知识检索）同一写法，即 **VS1 问答链路的两步外部调用在真实运行时都不可用**。此前的健康检查只探端口/actuator，不触发跨服务调用，因此该缺陷被全绿的 healthcheck 掩盖。

**建议修法**（2 个文件、各 2 行，强制 HTTP/1.1）：

```java
HttpClient http = HttpClient.newBuilder().version(HttpClient.Version.HTTP_1_1).build();
this.client = RestClient.builder().baseUrl(baseUrl)
        .requestFactory(new JdkClientHttpRequestFactory(http))
        .build();
```

备选：改用 Apache HttpClient 5 连接工厂；或在 nlp-service 前置一个支持 h2c 的网关。修复后应重跑 §8.3 的步骤 9–10，并补一条「跨语言服务调用」的契约用例防回归。

### 8.5 本机环境注意项（复用）

1. **`docker-compose.yml` 缺 `.env`**：`MINIO_ROOT_USER` 等 4 个变量未定义会让 `up -d` 直接失败；已从 `.env.example` 生成 `E:\AI\ai-agent\.env`。
2. **端口冲突**：Kafka 容器占用宿主 `9092`，而 session-manager 的 gRPC 默认也是 `9092` → 启动失败。启动时需设 `GRPC_PORT=19092`（配置本身支持 `${GRPC_PORT:9092}`）；`healthcheck.sh` 里 `check_port session-grpc 9092` 探的其实是 Kafka，建议改探 19092。
3. **宿主环境变量污染**：本机工具进程设了 `SERVER__PORT=51007`，Spring 宽松绑定会把它当成 `server.port`，4 个服务会一起去抢 51007。启动脚本必须先清除 `SERVER__PORT` / `SERVER__*`。
4. **服务常驻**：由助手工具调用拉起的子进程会在调用结束时被回收，长期运行请自行执行 `scripts\start-dev.bat all`。
5. **网络**：本机直连 `github.com` 不通，clone/fetch 走镜像；`git push` 需真实 GitHub 凭据，尚未验证。

### 8.6 D-1 修复记录（2026-09-13）

**根因再校准**：准确表述不是「JDK 默认 HTTP/2 必然出错」，而是 **uvicorn(h11) 不兼容 h2c 升级握手**。两组对照实验：

| 实验 | 对端 | HTTP_2 | HTTP_1_1 |
|---|---|---|---|
| 1 | 宽松 HTTP/1.1 服务端（Python 标准库） | 200，body 完好 | 200，body 完好 |
| 2 | Tomcat（body-service） | 200 | 200 |

→ 实验 1 证明 JDK 发送 `Upgrade: h2c` 属合规行为，宽松服务端照常接收 body；**只有 uvicorn 会把请求判为非法并丢弃 body**。
→ 实验 2 修正此前的过度推断：`BodyClient` 打的是 Tomcat，**从未受影响**；受影响的是 `NlpClient` 与 `VisualChannel` 的 OCR POST（两者都打 uvicorn）。

**改动清单**

| 文件 | 改动 |
|---|---|
| session-manager `orchestration/OutboundHttp.java`（新增） | 统一出口：HTTP/1.1 + connect 3s / read 10s 的 HttpClient 与 RestClient 工厂 |
| session-manager `NlpClient` · `BodyClient` | 改用 `OutboundHttp.restClient(...)`；失败默认降级（意图→FALLBACK 闲聊、检索→空结果）并输出保留 cause 的 WARN；`app.*.degrade-on-failure=false` 时抛 502 |
| session-manager `ErrorCode` · `GlobalExceptionHandler` · `BizException` | 新增 `AGENT_UPSTREAM_UNAVAILABLE`（映射 502）与携带 cause 的构造 |
| session-manager `application.yml` | 新增 `app.nlp/body.degrade-on-failure`（默认 true） |
| sense-service `util/HttpClients.java`（新增） | 渠道侧统一 HTTP/1.1 客户端构建器 |
| sense-service `TouchChannel` · `VisualChannel` | 改用 `HttpClients.builder(...)`，顺带消除 OCR POST 的同类隐患 |
| 新增测试 3 个（7 用例） | `OutboundHttpTest`（裸 socket 断言请求行为 HTTP/1.1 且无 `h2c` / `HTTP2-Settings`）、`UpstreamDegradeTest`（降级与 502 语义）、`HttpClientsTest`（版本 + 超时断言） |

**验证结果**

| 项 | 修复前 | 修复后 |
|---|---|---|
| Java 测试 | 54 通过 | **61 通过**（+7 回归用例） |
| `POST /api/session/{id}/ask` | 503 `AGENT_BUS_UNAVAILABLE` | **200 / 875 ms**，返回模板回答 + 1 条 citations |
| Redis `session:{id}:messages` | `LLEN=0` | **`LLEN=2`**、`message_count=2` |
| nlp-service 日志 | `422 Unprocessable Entity` + `Unsupported upgrade request` | `POST /api/nlp/intent HTTP/1.1 200 OK` |
| 杀掉 nlp-service 后再 ask | 503 | **200（降级 FALLBACK）** + `WARN 意图识别降级为 FALLBACK` |
| 契约校验 | FAIL 0 / WARN 3 | FAIL 0 / WARN 3（未漂移） |

> 遗留（未做，非本次范围）：`VisualChannel` 的 OCR 端到端仍需安装 PaddleOCR/Tesseract 才能验收；OCR 路径已随本次改动一并锁定 HTTP/1.1。

---

*编制：WorkBuddy · 2026-09-12 · 基线 `9385f23`*
*补充：WorkBuddy · 2026-09-13 · 追加 §八（端到端动态验证 + 缺陷 D-1 的分析与修复）*
