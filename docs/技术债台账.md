# 技术债台账（DEBT Registry）

> **口径时间**：2026-09-18（Phase 3 躯体期落地后复核 · 含 Phase 3 审核补全） · **代码基线**：`a5e0cd5`（Phase 3 代码收口）+ 审核补全轮
> **来源**：设计文档《D5-冻结前收尾/D5-1-简化版演进规范》§5.1 清单 —— 本文件是**仓库内的权威副本**

## 为什么把台账搬进仓库

代码里散落着 `// DEBT-00X` 注释（proto 源 3 处、Python 2 处），但台账原先**只存在于设计文档侧**
（`AGENT_CONTEXT/D5-冻结前收尾/D5-1-简化版演进规范.md` §5.1）。由此产生两个问题：

1. **查不到**：读代码的人看到 `DEBT-007` 无从确认含义，必须跨到知识库检索；
2. **不完整且滞后**：原清单只有 DEBT-001/002/003 三条，而代码已引用到 `DEBT-009`；
   同时 DEBT-003 对应的实现**已经升级完了**，清单还写着"待替换"。

本文件把「设计文档原清单」与「代码中实际引用的全部编号」合并，并**逐条按当前代码核实状态**。

---

## 一、台账总表

状态图例：✅ 已闭合 · 🟡 未闭合（触发点未到） · 🔴 未闭合（触发点已到）

| 债ID | 模块（实际落点） | 简化实现（代码实况） | 正式目标 | 触发点 | 状态 |
|---|---|---|---|---|---|
| **DEBT-001** | body-service | ~~内存 Map + 字符命中率打分~~ → **已换真三层存储**（PG 元数据 + Qdrant 向量 + Redis 热缓存） | Qdrant 向量检索 | VS3（Phase 3） | ✅ **已闭合** |
| **DEBT-002** | session-manager（回答生成） | 字符串模板拼接 | LLM 生成 | VS2 | 🟡 |
| **DEBT-003** | session-manager（会话存储） | ~~内存 Map~~ → **已换真 Redis** | Redis 持久化 | P4 | ✅ **已闭合** |
| **DEBT-005** | gateway-service | 最小 HMAC JWT | OAuth2 / OIDC | P7 | 🟡 |
| **DEBT-006** | sense-service（测试缺口） | 仅单渠道单测 | 五渠道集成测试 | P2 | 🔴 |
| **DEBT-007** | nlp-service | 字符 n-gram TF-IDF + 最近质心 | BERT 级模型（ONNX） | P4 | 🟡 |
| **DEBT-009** | session-manager（测试缺口） | 仅总线代理单测 | 消息链路（NATS/Kafka）测试 | P2 | 🔴 |
| **DEBT-010** | nlp-service（嵌入） | 字符 n-gram 哈希（768 维），**非语义相似** | BAAI/bge-m3（1024 维） | 装 `sentence-transformers` + 权重 | 🟡 |
| **DEBT-011** | nlp-service（重排） | 词法覆盖率打分（非交叉编码器） | bge-reranker-v2-m3 | 装 `sentence-transformers` + 权重 | 🟡 |
| **DEBT-012** | sense-service（事件投递） | 采集事件**携带正文**入库；超 Kafka 单消息上限时无回读路径 | 按 `staging_ref` 回读暂存对象（MinIO） | 正文 > 1MB | 🟡 |
| **DEBT-013** | body-service（文档解析） | **不支持 PDF/DOCX 解析**（显式拒绝，不静默当文本） | 接入文档解析通道（PDFBox / OCR） | 需要上传 PDF 时 | 🟡 |

> `DEBT-004` / `DEBT-008` **从未被使用**（编号跳号，见 §4.4）。
> **2026-09-18 补记**：DEBT-010 / 011 / 012 在代码里早已引用（`nlp-service` 两处 + `SenseEventPublisher` 一处），
> 但此前**未登记本表** —— 与 §4.4「后续新债从 DEBT-010 起编」的约定直接冲突。本轮审核已补齐，
> 并新开 DEBT-013（PDF 解析缺口）。

---

## 二、逐条详情

### DEBT-001 · body-service 检索为内存实现 —— 已闭合 ✅

- **原简化实现**：`BodyStore` 用 `ConcurrentHashMap<String, List<StoredChunk>>` 按租户存切片；
  检索打分是「整串包含 = 1.0，否则按查询字符在正文中的命中率」——**纯内存、无持久化、无语义相似度**。
- **正式目标**：Qdrant 向量检索。`docker-compose.yml` 已定义 qdrant 服务（端口 6333/6334），
  但**当时没有任何服务调用它**。
- **当前实况（Phase 3，2026-09-18）**：body-service **整体重写**为持久化知识管道：
  - 冷层真相源 `PgMetadataStore`（PostgreSQL）· 温层向量 `QdrantClient`（集合 `lifeform_knowledge`）·
    热层缓存 `HotCacheStore`（Redis，租户前缀指纹）；
  - 统一门面 `StorageFacade` + 热度分层 `TierRouter`（阈值可配）；
  - 入库 `IngestService`（分块 → 嵌入 → 向量入库）、检索 `RetrievalService`（缓存优先 → 向量召回 → 重排）、
    `RagPipeline`（检索增强生成）；
  - **旧的 `BodyStore` / `BodyController` / `BodyStoreTest` 已删除**（被取代即销项）。
- **关闭依据**：端到端实测——文档入库后可被语义检索命中（语义相近可召回，非字符匹配），
  检索 P99 **366ms**（< 500ms 验收）、命中率 0.925、缓存命中率 0.45；租户隔离生效；
  重入库幂等、删除无孤儿向量。验收脚本 35 项 PASS / 0 FAIL。
- **状态**：✅ **已闭合**（2026-09-18，Phase 3 躯体期）
- **证据**：`services/java/body-service/src/main/java/com/agent/body/{store,service,client,controller}/`；
  `pom.xml` 已引入 `spring-boot-starter-data-redis` / `postgresql` 驱动，`QdrantClient` 直连 Qdrant REST API。
- 备注：原台账描述为「本地文件检索」，与当时实现（纯内存）不符（见 §4.2）；现已由真实三层存储取代。

### DEBT-002 · 回答生成为模板拼接

- **简化实现**：`SessionController.buildAnswer()` 把检索到的首条切片截前 240 字，
  套进固定句式「根据知识库《X》检索结果：…」；无检索结果时返回固定致歉文案。
- **正式目标**：LLM 生成
- **触发点**：VS2 完成
- **状态**：🟡 未闭合
- **证据**：`services/java/session-manager/src/main/java/com/agent/session/controller/SessionController.java:132-138`；
  债务标记在 `proto/brain/v1/brain.proto:1`。
- 备注：台账原文把模块记为「大脑」，实际代码落在 **session-manager**（大脑期尚未开工）。

### DEBT-003 · 会话存储 —— 已闭合 ✅

- **原简化实现**：会话与消息存进程内存 Map。
- **当前实况**：**已改为真实 Redis**。`session-manager` 依赖 `spring-boot-starter-data-redis`，
  `application.yml` 配置 `${REDIS_HOST:127.0.0.1}:${REDIS_PORT:6379}`，
  `SessionController` 全程使用 `StringRedisTemplate`（Hash 存会话元数据 + List 存消息，2h TTL）。
- **关闭依据**：Redis 客户端与配置均真实存在，且会话写入/读取/删除全走 Redis
  （`SessionController.java:10,22,45-51,102,112-117,126`）。
- **状态**：✅ **已闭合**（2026-09-17 核实）
- ⚠️ 但 proto 源与两处生成物的注释**仍写着旧实现**（见 §4.3）。

### DEBT-005 · 网关为最小 JWT

- **简化实现**：`JwtService` 使用 `io.jsonwebtoken.security.Keys.hmacShaKeyFor()` 做对称 HMAC 签发验签，
  无签发方/密钥轮转/OIDC 发现端点。
- **正式目标**：OAuth2 / OIDC
- **触发点**：P7（见《D5-3-安全演进路径》）
- **状态**：🟡 未闭合（触发点未到）
- **证据**：`services/java/gateway-service/src/main/java/com/agent/gateway/security/JwtService.java:5,27`。

### DEBT-006 · 五渠道集成测试缺口

- **简化实现**：感官测试只有 `TouchChannelTest`（单渠道）、`CollectPipelineTest`、
  `ChannelHealthMonitorTest` —— **没有覆盖五渠道协同的路由/降级集成测试**。
- **正式目标**：五渠道集成测试（含隔离渠道自动降级到可用渠道的端到端验证）
- **触发点**：P2
- **状态**：🔴 未闭合（触发点已到，Phase 2 已宣告验收）
- **证据**：`services/java/sense-service/src/test/java/com/agent/sense/` 下无五渠道协同用例。
- 备注：《G1-验证追踪视图》中 C16 感官渠道管理器的验收方法即本项，状态标 🟡 待验（见 §4.5）。

### DEBT-007 · L0 意图模型为统计分类器（非 BERT）

- **简化实现**：字符 n-gram（单字 0.4 / bigram 1.0 / trigram 0.8）TF-IDF + 最近质心，
  纯 Python 手写、零第三方依赖，余弦相似度 + 分离度合成置信度。
- **正式目标**：BERT 级模型（ONNX 推理），接口按 `predict / predict_batch / stats` 抽象以便整体替换
- **触发点**：P4 大脑期接入模型推理服务
- **状态**：🟡 未闭合（触发点未到）
- **证据**：`services/python/nlp-service/app/intent/l0_model.py:49`；实测留出集 **90.0%** / P99 **0.035ms**，
  已满足 R2-08 的「准确率 ≥ 85%、延迟 < 50ms」验收指标，故**不构成验收阻塞**。

### DEBT-009 · 消息链路测试缺口

- **简化实现**：会话侧只有 `BusProxyTest` 单测；**无 NATS / Kafka 真实链路测试**
  （发布—订阅、应答、断连重试、死信等）。
- **正式目标**：消息链路测试
- **触发点**：P2（NATS 接入）
- **状态**：🔴 未闭合（触发点已到）
- **证据**：`services/java/session-manager/src/test/java/com/agent/session/` 下无 NATS/Kafka 用例。
- 备注：同 DEBT-006，《G1-验证追踪视图》中 C09 数据总线代理的验收方法即本项，状态标 🟡 待验。

### DEBT-010 · 嵌入走字符 n-gram 哈希后端（非 BGE-M3）

- **简化实现**：`HashNgramEngine` —— 字符 1/2/3-gram + 英文词元做 blake2b 哈希分桶（768 维）+ 亚线性 TF 加权 + L2 归一化。
  **纯本地、零依赖、跨进程确定性**，但其相似度本质是「字面特征重叠」，**不等价于语义相似度**。
- **正式目标**：BAAI/bge-m3（sentence-transformers，1024 维）。
- **触发点**：安装 `sentence-transformers` + 下载 BGE-M3 权重（本机未装）。
- **状态**：🟡 未闭合（触发点未到）—— 但**不构成 Phase 3 验收阻塞**：
  R3-03 的验收口径是「嵌入延迟 P95 < 200ms；向量维度正确」，降级后端满足；且引擎链**自动优先 BGE-M3**，
  装好即切换，无需改代码。
- **证据**：`services/python/nlp-service/app/embedding.py:1,95-123`；`app/main.py:3`；
  切换后须重建 Qdrant 集合（维度 768→1024），见 `QdrantClient.ensureCollection`。
- ⚠️ **切换影响面**：换后端 = 换向量空间 → **必须全量重索引 + 评测回归**（设计 §6 回归注意）。

### DEBT-011 · 重排走词法覆盖率打分（非交叉编码器）

- **简化实现**：`LexicalRerankerEngine` —— 查询特征覆盖率 0.7 + 词元命中率 0.3，再乘长度惩罚（理想区间 120–640 字）。
  与 DEBT-010 的哈希嵌入**同源共享 `_features`**，故降级链路上排序单调一致。
- **正式目标**：bge-reranker-v2-m3（CrossEncoder）。
- **触发点**：安装 `sentence-transformers` + 权重。
- **状态**：🟡 未闭合（触发点未到）。R3-06 属 Should 项，验收为「重排后相关性提升可量化」——
  当前返回 `score`（召回分）与 `rerank_score`（重排分）双列可对照，且重排不可用时**降级不阻断主链路**。
- **证据**：`services/python/nlp-service/app/reranker.py:1,76-107`；`app/main.py:4`。

### DEBT-012 · 采集事件携带正文（超限无回读路径）

- **简化实现**：Phase 2 的 `SenseEventPublisher` 事件荷载增加 `title` / `content` 正文字段，
  使「感官 → 躯体」闭环**不依赖 MinIO 在线**即可自洽。
- **正式目标**：正文超过 Kafka 单消息上限（默认 1MB）时，改为按 `staging_ref` **回读暂存对象**（MinIO）。
- **触发点**：采集正文 > 1MB 的文档。
- **状态**：🟡 未闭合（触发点未到）。当前超限时 `SenseCollectedConsumer` 会**如实记录"事件只带暂存引用时无法直接入库"**，不静默丢弃。
- **证据**：`services/java/sense-service/src/main/java/com/agent/sense/event/SenseEventPublisher.java:67`；
  `services/java/body-service/src/main/java/com/agent/body/event/SenseCollectedConsumer.java:134`。

### DEBT-013 · 不支持 PDF / DOCX 文档解析（本轮审核新开）

- **简化实现**：`DocumentParser` 支持 `md / markdown / text / txt / html / htm / auto`，
  对 `pdf` / `doc` / `docx` **显式抛出 `AGENT_BAD_REQUEST` 并给出指引**——PDF 需版面还原（PDFBox）或走 OCR 通道。
- **正式目标**：接入文档解析通道（PDFBox / Apache Tika 或复用 Phase 2 OCR），使设计 §7「上传文档（MD/PDF）」的 PDF 分支可用。
- **触发点**：演示/生产需要上传 PDF 时。
- **状态**：🟡 未闭合（触发点未到，降级行为诚实）。
- **为什么不开成"已支持"**：把 PDF 二进制当文本塞进分块器会产出**垃圾向量且无人察觉**，
  属于典型"看起来成功"的伪实现——本仓库红线是宁可报错也不产脏知识。
- **证据**：`services/java/body-service/src/main/java/com/agent/body/chunk/DocumentParser.java:47-49,81-84`；
  回归用例 `chunk/DocumentParserTest.unsupportedFormatsAreRejectedWithGuidanceNotSilentlyChunked`。

---

## 三、关联未实现项（**不属于技术债**，但常被一起问到）

技术债 = 「先用简化版顶着，到点换正式版」。以下属于**明示未实现 / 环境缺口**，性质不同，单列：

| 项 | 性质 | 当前表现 |
|---|---|---|
| OCR 端到端 | 环境缺口（需装 PaddleOCR / Tesseract） | 视觉渠道 `available=false`，如实降级，不伪造 |
| 听觉渠道（ASR） | 设计预留（R2-03 只保留接口与注册位） | `available()=false` / `healthy()=false`，自动标记 DOWN 并隔离 |
| wp-bff 契约 31 个 planned 端点 | 分期实现 | 契约标 `x-wp-status: planned`；`/overview` 用 `gaps` 列出 15 个未覆盖域，前端标注 Mock |
| ~~body-service 缺 OTLP 配置、未接入统一错误码~~ | ✅ **2026-09-18 Phase 3 已补齐** | 已加 `management.otlp.tracing.endpoint`（接入 Jaeger 链路）；已建 `ErrorCode` / `BizException` / `GlobalExceptionHandler` 统一错误响应 |

---

## 四、一致性问题登记（2026-09-17 核实发现，**未擅自修改源码**）

### 4.1 `DEBT-003` 编号冲突 —— 同一编号两个含义

| 位置 | 该编号被用来表示 |
|---|---|
| `proto/session/v1/session.proto:10` + 生成物 | 会话存储为内存 Map → 换 Redis |
| `services/python/nlp-service/app/main.py:1` | 意图识别升级为「规则引擎 + L0 双级联」→ P4 混合识别 |

两者是**完全不同的债务**。推测 `main.py` 那一行是编号误用（复制模板时未改号）。
**处置**：未擅自改号（编号分配属设计侧决策），登记为待办 §6-1。

### 4.2 台账描述与代码实况不符

- **DEBT-001** 台账写「本地文件检索」，实际是**纯内存 Map**，连文件持久化都没有；
  且 `docker-compose.yml` 里的 qdrant 容器**无人调用** —— 容易被误读为"已经在用 Qdrant 了"。
  ✅ **2026-09-18 已消解**：Phase 3 已用真实三层存储取代（PG 元数据 + Qdrant 向量 + Redis 热缓存），
  描述与实况一致（见 §二 DEBT-001）。
- **DEBT-002** 台账把模块记为「大脑」，实际落在 **session-manager**。

### 4.3 生成物注释滞后（已闭合债务仍写着旧实现）

DEBT-003 已闭合，但下列位置仍保留旧描述：

| 位置 | 说明 |
|---|---|
| `proto/session/v1/session.proto:10` | **契约源码**，需改注释后重新生成 |
| `services/java/proto-contracts/src/main/java/com/agent/session/v1/SessionServiceGrpc.java`（6 处） | protoc 生成产物，随上者重生成 |
| `services/python/nlp-service/generated/session/v1/session_pb2_grpc.py`（3 处） | 同上 |

**处置**：本仓库的 protobuf 生成源码**已入库**，改注释须走 `-Pproto-gen` 重新生成并提交产物。
因该 profile 会真实调用 protoc（本机历史上出现过构建阻塞），**本次未动**，登记为待办 §6-2。

### 4.4 编号跳号

`DEBT-004` 与 `DEBT-008` **在全仓（含设计文档）从未出现**。可能是预留号或漏登记。
**处置**：保留空号不回收，避免与历史文档冲突；后续新债从 `DEBT-010` 起编。

### 4.5 验收口径待对齐

《G1-验证追踪视图》中 **C09 数据总线代理**、**C16 感官渠道管理器** 的验收方法正是
DEBT-009 / DEBT-006，两者状态标 **🟡 待验**；而 Phase 2 报告已宣告验收通过。
**处置**：属"诚实留痕"的正常结果，但建议在 `项目进度总览.md` 中把这两项**显式登记为验收留有余项**，
避免"阶段已验收"被读成"所有验证方法都已跑过"。

### 4.6 设计选型 ↔ 实现口径差异（2026-09-18 审核新登记）

Phase 3 审核发现两处「设计文档写了、实现没这么做」的差异。**均不违反 R3-01/R3-08 的验收条款**
（R3-01 验收 = 统一增删改查 + 分层规则可配置；R3-08 验收 = 重复查询命中 ≥ 30%），
但设计文档的选型描述与实况不一致，必须留痕，否则后人读设计会误判技术栈。

| # | 设计文档口径 | 实现实况 | 定性 | 处置建议 |
|---|---|---|---|---|
| 1 | `Phase3-开发设计文档` §1/§3.1：冷层为 **PG + pgvector**（「向量+SQL 混合」） | 冷层是**纯 PG 关系表** `knowledge_document` / `knowledge_chunk`（`content TEXT`，**无 vector 列**）；向量只在 Qdrant | 设计选型未落地（非债务：R3-01 交付物只要求 `StorageFacade` + `TierRouter`，已交付） | 二选一：① 改设计文档对齐实况（推荐，Qdrant 已承担向量主库职责，PG 再存向量属冗余）；② Phase 4+ 补 pgvector 列做冷备份 |
| 2 | `Phase3-开发设计文档` §1/§2 技术选型/§3.1：Redis **语义缓存（向量相似 0.95）** | 热层是**精确指纹缓存**：问题归一化（去空白/小写/去尾标点）后 SHA-256 → key。**不做向量相似匹配** | 设计选型未落地（非债务：R3-08 验收为"重复查询缓存命中 ≥ 30%"，实测 45%，达标） | ① 改设计文档对齐实况（已采纳，见下）；② 若要语义缓存，需在 `HotCacheStore` 增加「query 向量 → 近邻查缓存」路径 |

> **共同点**：两处都是"设计写得比实现更先进"。本仓库的红线是**不伪造能力**——
> 实现没做就写清楚没做，而不是把设计文档的选型当成已交付结果写进报告。

**2026-09-18 已闭合 ✅（用户确认采纳方案①「改设计文档对齐实况」）**。分两步收口：

1. **实现侧自述**（当日先行改准）：`TierRouter` 冷层注释、`wp-bff` 中间件 `role` 文案、
   `README.md` 数据层与路线图、`infra/README.md` 组件表 —— 均写明
   「PostgreSQL 关系元数据真相源 / 镜像为 pgvector 变体但**未启用向量列** / 向量由 Qdrant 独占」。
   `docker-compose.yml` 保留 `pgvector/pgvector:pg16` 镜像（不影响运行，为 Phase 4+ 留选择余地）。
2. **设计文档原文**（`E:\AI\核心知识\AGENT_CONTEXT\Phase3-躯体期\`，当日按实况修订 + HTML 孪生同步）：
   - `需求设计文档.md` §3 R3-01：`Redis(热)/Qdrant(温)/PG+pgvector(冷)` → **`PostgreSQL(冷)`**，
     并注明「冷层为关系元数据真相源，不存向量；向量主库由 Qdrant 独占（对齐实现，2026-09-18 修订）」。
   - `开发设计文档.md` §1 架构图：冷层一行 → `PostgreSQL (冷: 元数据真相源) ← pg16`；
     `CacheFirstStrategy (Redis 语义缓存)` → **`(Redis 指纹缓存)`**（框图宽度与相邻行保持一致）。
   - `开发设计文档.md` §2 技术选型表：`冷存储 PostgreSQL + pgvector / 16-0.7 / 向量+SQL 混合`
     → **`PostgreSQL（关系元数据真相源）/ 16 / 冷层不存向量，向量由 Qdrant 独占`**；
     `缓存 Redis 语义缓存（向量相似 0.95）` → **`Redis 精确指纹缓存（归一化 query → SHA-256）/ 实测命中 45%`**。
     表下新增「**与实现的对齐说明**」引用块，写明修订原因、实测数据与"不违反 R3-01/R3-08"的结论。
   - `开发设计文档.md` §3.1：`冷数据 → PG+pgvector（全量归档）`
     → **`冷数据 → PostgreSQL（关系元数据全量归档 · 真相源，不存向量）`**。

> 沉淀：**设计文档与实现出现选型差异时，"改代码"与"改设计"都要有明确授权**；
> 只有一方获准时，另一方至少要在**实现侧自述**里消歧，别让注释继续误导读者。

### 4.7 wp-bff 写路径一度缺失鉴权（2026-09-18 审核**发现并已闭合** ✅）

| 项 | 内容 |
|---|---|
| 现象 | 补「工作平台知识入库」时新增了 `POST /api/wp/knowledge`，但它落在 `/knowledge` 这个**只读免令牌**路径下，而未被 `authorizeControl()` 覆盖 → 浏览器直连 `:8090` 即可**无鉴权写入知识库** |
| 为什么算"一致性"问题而非单纯 bug | `docs/功能开发流程.md` 当时写着「只读端点（**含 `/knowledge`**）不需控制令牌」——该文档以**路径**为单位描述安全边界，而实际安全边界应以**方法**为单位。文档口径与安全模型错位，才让新写的 POST 显得"合规" |
| 影响 | 仅暴露在本机开发端口（8090 默认不对外），**未进入任何提交时的生产配置**；但属真实写入后门，必须修 |
| 修复 | ① `handleKnowledgeIngest` 前置 `authorizeControl()`：来源白名单 + `X-WP-Control-Token`，401/403 且**不触达体层**；② 契约按方法分标 `x-wp-security`（读 `origin-allowlist` / 写 `origin-allowlist + control-token`）；③ 补 2 条反向用例，两条均断言 `jsonRequest` **未被调用**；④ 文档口径改为「**按方法判定，不按路径**」 |
| 现状 | ✅ **已闭合**（`node --test` 29 项全绿）。开发链路不受影响：Vite 代理对 `/api/wp/*` 统一注入令牌；直连 8090 被拒 |

> 台账记这一条，不是因为存在未偿债务，而是因为**它的成因（"路径级安全描述"与"方法级安全实现"错位）
> 会在下次新增写端点时重演**。判断口径已写入 `docs/功能开发流程.md` §3.8。

### 4.8 未映射路由返回 500 而非 404（2026-09-18 前端联通验证**发现并已闭合** ✅）

| 项 | 内容 |
|---|---|
| 现象 | 请求任何**未实现**的路由（如 `GET /api/body/definitely-not-a-route`）返回 `500 AGENT_INTERNAL_ERROR`，而非 `404`；方法不匹配、媒体类型不支持的场景同样落入兜底 500 |
| 根因 | 三个 Java 服务（`body-service` / `sense-service` / `session-manager`）的 `GlobalExceptionHandler` 只有「业务异常 / 参数异常 / 兜底 Exception」三档，Spring 路由层抛出的 `NoResourceFoundException` / `NoHandlerFoundException` / `HttpRequestMethodNotSupportedException` / `HttpMediaTypeNotSupportedException` 无对应处理器，全部落进兜底 `Exception` 分支 |
| 为什么算一致性问题 | 前端与契约校验依赖 `404` 判定「已登记但未实现」。返回 500 会让「端点不存在」与「服务内部故障」不可区分，降级路径无法据此决策 |
| 影响 | 误导排障方向（把拼错的 URL 当成服务故障）；正常链路不受影响 |
| 修复 | ① 三服务 `GlobalExceptionHandler` 新增 404/405/415 映射；② `ErrorCode` 补 `AGENT_METHOD_NOT_ALLOWED`；③ 三服务各补 `GlobalExceptionHandlerTest`（body 6 项 / sense 5 项 / session 5 项） |
| 现状 | ✅ **已闭合**（Java `mvn test` 130 项全绿；实测未映射路由 → 404） |

### 4.9 前端 Mock 与真实 API 的字段/文案漂移（2026-09-18 **发现并已闭合** ✅）

「Mock 先行」策略下 Mock 是**视图开发期的接口契约实现**，一旦真实 API 就位，Mock 必须反向对齐，
否则切到 `api` 后同一视图会出现「字段对得上、文案/语义对不上」的隐性错位。本轮切换 `api` 前逐字段核对，发现 4 类漂移：

| # | 漂移项 | Mock 原值 | 真实 API 值 | 处置 |
|---|---|---|---|---|
| 1 | 嵌入后端标识 | `bge-m3` / 1024 维 / `degraded=false` | `hash-ngram-768` / 768 维 / `degraded=true`（DEBT-010 诚实上报） | ✅ 已改 Mock 对齐真实值 |
| 2 | 重排后端标识 | `bge-reranker` | `lexical-coverage`（DEBT-011） | ✅ 已改 Mock |
| 3 | 中间件 up/down 状态 | jaeger/kafka **down**、nats/nacos/minio **up**（写反） | jaeger/kafka **up**、nats/nacos/minio **down** | ✅ 已改 Mock；`summary` 静态字面量 `up:6/down:2` 同步修为 `5/3` |
| 4 | PostgreSQL 角色文案 | wp-bff 写 `关系库 · pgvector 冷存储`（与 §4.6 同源失真） | 实况为关系元数据真相源 | ✅ 已改 `wp-bff/server.js`，与 Mock 一致 |

> **同类隐患**：`KnowledgeStats.retrieval` 的 `documents/chunks/ingest_failures/sample_limit`
> 四个字段**真实 API 已返回、Mock 与 `types/index.ts` 均缺失** —— 属 TypeScript 类型未覆盖真实响应，
> 已一并补齐。**教训**：Mock 与真实响应要做**双向**核对（不是只查「Mock 有的真实有没有」），
> 否则「真实多出来的字段」会被静默丢弃。

### 4.10 前端对「未实现端点」的降级语义（2026-09-18 **发现并已改造** ✅）

| 项 | 内容 |
|---|---|
| 现象 | `api` 模式下，契约标 `planned` 的端点（`/tasks`、`/chat`、`/results`、写路径等）在前端无兜底：要么整页报错，要么**静默返回 Mock 数据并当作真实结果呈现** |
| 为什么算一致性问题 | 本仓库红线是「**不伪造能力**」。静默伪造比报错更危险——用户会以为功能已上线 |
| 改造 | ① 新增 `plannedEndpointError(scope, method, path, cause)`：登记降级 + 抛说明性错误；② `getTasks/getChat/getResults/getSearch` 改用 `safe()` 回落**演示数据**并显式登记降级（页面标注「演示数据」）；③ 写操作（`ask/createTask/retryTask/installSkill/.../updatePreferences`）一律走 `plannedEndpointError`，**不伪造成功**；④ `TasksView.retryTask` / `ModuleView.retryAutomation` / `OverviewView.applySuggestion` 补 `try/catch`，失败不改本地状态 |
| 现状 | ✅ 已落地（`vue-tsc --noEmit` + `vite build` 通过） |

### 4.11 开发代理目标硬编码（2026-09-18 配置对齐**发现并已修复** ✅）

| 项 | 内容 |
|---|---|
| 现象 | `web/work-platform/vite.config.ts` 的 `/api/wp` 与 `/api` 代理目标（`http://127.0.0.1:8090` / `:8080`）与开发端口 3001 均为硬编码字面量 |
| 影响 | 换机、改端口、容器化运行时要改源码；与「配置与代码分离」口径不一致 |
| 修复 | 改为可由环境变量覆盖：`WP_BFF_URL` / `WP_GATEWAY_URL` / `WP_DEV_PORT`，默认值不变（行为零变化） |
| 现状 | ✅ 已修复（构建通过，默认值与原硬编码一致） |

> 另：`.env.example` 由 `VITE_DATA_SOURCE=mock` 改为 `api`，并补注释说明取值语义与降级行为 ——
> 对齐 `Phase3-需求设计文档` §8 R-C09「阶段完成时必须切换真实 API 数据」。
> 本地 `.env`（不入库）本就为 `api`，故**实际运行行为未变**，仅影响新克隆者的默认值。

---

## 五、管理规则（引《D5-1-简化版演进规范》§5.2）

- **创建**：任何简化实现落地时同步创建 —— 代码 `// DEBT-00X` 注释 **+ 登记本文件**
- **追踪**：每里程碑（M1/M2/M3）审查一次，更新状态
- **关闭**：替换完成且契约测试通过 → 销项（在本文件标注 ✅ 并写明关闭依据与日期）
- **红线**：**触发点到达后 2 个迭代内必须替换**，否则升级为风险项（阻断里程碑演示）
- **红线现状**：DEBT-006 与 DEBT-009 触发点已到（P2）且尚未闭合，按红线**应升级为风险项**（见 §6-3）

---

## 六、待办

| # | 事项 | 建议动作 | 归属 |
|---|---|---|---|
| 1 | `DEBT-003` 编号冲突（`nlp-service/app/main.py`） | 确认意图升级是否应占用独立编号，若是则改为新号并同步本文件 | 设计侧确认 |
| 2 | proto 源与生成物注释滞后（DEBT-003 旧描述） | 改 `proto/session/v1/session.proto` 注释 → `-Pproto-gen` 重生成 → 提交产物 | 需网络/构建窗口 |
| 3 | DEBT-006 / DEBT-009 触发点已到 | 在其被替换前，按红线升级为风险项并写入总览 | 项目侧 |
| 4 | 本文件与设计文档台账同步 | 设计文档侧 §5.1 改为指向本文件，避免两处各写一份 | 设计侧 |
| 5 | ~~契约生成物与契约源**无同步门禁**（改了 `.proto` 忘重生成不会被任何检查发现）~~ | ✅ **2026-09-17 已落地**：新增 `scripts/proto-sync-check.sh`（双向比对：未入库 / 内容不同 / 契约已无）+ CI job `proto-check-java`（`mvn -Pproto-gen generate-sources` 后比对） | 已完成 |
| 6 | ~~Python 侧 proto 生成物（12 个）**未入库且 CI 无生成步骤** → 换机后 `generated/` 可能整个缺失而无人察觉~~ | ✅ **2026-09-17 已落地**：CI job `proto-check-python` 每次重新生成 + 12 文件数断言 + 6 模块导入自检（产物仍按既有决策不入库） | 已完成 |
| 7 | ~~`grpcio-tools` 未写进 `services/python/nlp-service/requirements.txt`~~ | ✅ **已澄清为记账错误（2026-09-17 发现）**：它**一直在 `requirements-dev.txt`**（与 `grpcio`、`protobuf` 并列）。它是**构建期**依赖，运行时不需要，本就不该进 `requirements.txt`。前次登记时只看了 `requirements.txt` 未查 dev 文件，属我方的核实疏漏 —— 已同步修正 `docs/proto契约使用说明.md` §4.2 与 §八 | 已完成（记账修正） |
| 8 | `Phase0-开发执行日志.md` 第 3 条称「proto 生成物**双侧入库**（Java 90 + Python 12）」与事实不符（Python 侧从未入库，`.gitignore:32` 忽略） | 历史归档按约定**不回改**；已在 `docs/proto契约使用说明.md` §八 登记纠正 | 已登记 |
| 9 | **Java 侧 CI 门禁未在真实 runner 首跑验证**（本机 Windows 有 `protoc` 清理阻塞史，未触发 `-Pproto-gen`；比对逻辑已用合成生成物验证 90/90 + 三类偏差均可报出） | 首次流水线运行后确认；脚本用 `find` 自适应 Maven 输出目录，失败时会打印明确排查提示 | 待首跑确认 |
| 10 | ~~DEBT-010/011/012 代码已引用但台账未登记~~ | ✅ **2026-09-18 已补齐**（含新增 DEBT-013），§7 索引同步 | 已完成 |
| 11 | 设计文档 §1/§3.1 写「PG+pgvector」、§2 写「Redis 语义缓存（向量相似 0.95）」，实现均未采用 | 见 §4.6：建议改设计文档对齐实况（Qdrant 已承担向量主库；精确指纹缓存实测命中 45% > 30% 验收） | 设计侧确认 |
| 12 | **Phase 3 需求文档 §8/§9 的 IN-01 / IN-05 / WB-03 / WB-05 / MC-01 未被列入交付范围** | IN-05 已于 2026-09-18 补交付（`/retrieve/plan` 预留接口）；IN-01（技能包 MinIO+etcd）、WB-03（专家中心）、WB-05（技能市场）、MC-01（NATS 协作总线服务化）**仍未实现** —— 属**阶段范围缺口**，需在规划侧确认归属阶段 | 规划侧确认 |
| 13 | wp-bff 的安全边界曾以**路径**为单位描述（"只读端点含 `/knowledge`"），而实现按**方法**判定 → 下次给同一路径加写方法时会再次误判为免鉴权（§4.7 即此成因） | 已改为按方法描述。**机制化建议**：把「需要控制令牌的方法集合」提为 `CONTROL_REQUIRED` 常量，并加一条测试断言"所有非 GET 的业务方法都在该集合内"，从结构上杜绝重演 | 下轮加固 |
| 14 | ~~**设计文档 pgvector / 语义缓存口径待落笔**（§4.6）~~ | ✅ **2026-09-18 已落笔（用户确认方案①）**：改 `Phase3-需求设计文档` §3 R3-01 + `Phase3-开发设计文档` §1/§2/§3.1，新增「与实现的对齐说明」引用块，两份 HTML 孪生同步重生成 | 已完成 |
| 15 | 前端 `VITE_TENANT_ID` 默认值 `default` 与 wp-bff 的 `X-Tenant-Id` 透传口径未做联动校验（多租户切换时前端需手工改 `.env`） | 评估是否改为运行时租户选择器（`/api/wp/*` 已支持 `X-Tenant-Id` 头透传，BFF 侧无需改动） | 规划侧确认 |
| 16 | Mock ↔ 真实响应字段一致性**未做成自动校验**（§4.9 的 4 类漂移靠人工逐字段核对才发现） | 建议加一条脚本：抓取真实 `api` 响应 JSON Schema，与 `mock.ts` 样例做**双向**字段比对，纳入 CI | 下轮加固 |

---

## 七、代码标记索引（`DEBT-00X` 出现在哪）

| 位置 | 编号 |
|---|---|
| `proto/body/v1/body.proto:1` | DEBT-001（✅ 已闭合，注释待随契约重生成刷新） |
| `proto/brain/v1/brain.proto:1` | DEBT-002 |
| `proto/session/v1/session.proto:10` | DEBT-003（滞后） |
| `services/java/proto-contracts/.../session/v1/SessionServiceGrpc.java` ×6 | DEBT-003（生成物，滞后） |
| `services/python/nlp-service/generated/session/v1/session_pb2_grpc.py` ×3 | DEBT-003（生成物，滞后） |
| `services/python/nlp-service/app/main.py:1` | DEBT-003（**冲突**） |
| `services/python/nlp-service/app/main.py:2` · `app/intent/l0_model.py:10` | DEBT-007 |
| `services/python/nlp-service/app/embedding.py:1,7` · `app/main.py:3` | DEBT-010 |
| `services/python/nlp-service/app/reranker.py:1,7` · `app/main.py:4` | DEBT-011 |
| `services/java/sense-service/.../event/SenseEventPublisher.java:67` | DEBT-012 |
| `services/java/body-service/.../chunk/DocumentParser.java`（类注释） | DEBT-013 |

> **2026-09-18 审核补记**：DEBT-010/011/012 此前只存在于代码注释、未登记本表（已补齐）；
> DEBT-013 为本轮新开。**登记率现已 100%**：代码中出现的每个 `DEBT-0xx` 都能在本表查到。

---

*本文件首次建立于 2026-09-17 · 维护：探索者一号（WorkBuddy 侧）*
*状态核实方式：逐条对源码取证据（文件 + 行号），非沿用设计文档描述*
*2026-09-17 更新：§6-5 / §6-6 落地的门禁 = `scripts/proto-sync-check.sh`（本地可跑）+ `.gitlab-ci.yml` 的 `proto-check-python` / `proto-check-java`（CI 阻断）*
*2026-09-18 更新：Phase 3 审核补记 —— 补齐 DEBT-010/011/012 登记、新开 DEBT-013、登记 §4.6 两处设计↔实现口径差异、§6 新增 3 条待办*
*相关文档：`docs/proto契约使用说明.md`（契约侧使用现状与生成链路）、`docs/功能开发流程.md` §3.10（契约治理流程）、`docs/优化日志/2026-09-18-Phase3需求审核与补全.md`（本轮审核全记录）*
