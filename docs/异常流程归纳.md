# Agent-Lifeform 异常流程归纳

| 项 | 值 |
|---|---|
| 口径时间 | 2026-09-18 |
| 代码基线 | `6616616`（本轮代码提交，见文末「变更台账」） |
| 覆盖范围 | 运行期异常流程（§2，共 7 节）+ 研发期异常流程（§3，编号 D-01 ~ D-30，共 30 条） |
| 编制依据 | 源码 + 运行态实测（非文档转述）；历史过程记录见 `docs/项目进度日志报告/Phase*/`、`docs/优化日志/` |

---

## 0. 为什么要单独有一份

异常处理的知识此前是**散落**的：

| 位置 | 覆盖的部分 |
|---|---|
| `java-services/07-运行时配置与横切约定.md` §5 / §5.1 / §5.2 | 错误码表 + Java 三服务的路由层分流 |
| `java-services/04/05/06-*.md` 各自章节 | 单服务的异常出口与降级清单 |
| `wp-bff/server.js` 注释 | BFF 的鉴权与 `available=false` 语义 |
| `web/work-platform/src/api/status.ts` | 前端的降级登记约定 |
| 各阶段报告 §问题与解决、`优化日志/` | 研发期踩过的坑（按当时情境记录） |

缺的是**一张端到端的总图**：一次失败在链路上经过哪些层、每层给出什么状态码与错误码、是否降级、谁能看见。本文补这一层，并把研发期异常一并归纳为可复用的规范动作。

> **一个必须说清的口径**：本文描述的**运行期异常流程全部有实测证据**（状态码与响应体为 `curl` 实拍）；**研发期异常流程**来自本项目各阶段的真实记录，非推测。

---

## 1. 运行期链路总览

### 1.1 调用拓扑

```
浏览器 (Vue3 + Element Plus)
  │  /api/wp/*    ← 开发期经 Vite(3001) 代理注入 X-WP-Control-Token
  ▼
wp-bff (8090, Node)          ← 工作平台聚合层；唯一面向前端的 BFF
  ├──→ body-service  (8083, Java)   ──→ nlp-service (8000, Python)   嵌入 / 重排 / 分块
  ├──→ middleware 探针（TCP，8 个）
  └──→ Jaeger（链路追踪聚合）

浏览器 / 其他客户端
  │  /api/*       ← 需 Bearer JWT
  ▼
gateway-service (8080, Java WebFlux)
  ├──→ session-manager (8081)   ──→ nlp-service(8000) · body-service(8083)
  └──→ sense-service   (8082)   ──→ nlp-service(8000) OCR 通道
                                     └─→ Kafka `lifeform.sense.collected` → body-service
```

### 1.2 各层的异常职责（**不要跨层越权处理**）

| 层 | 只负责 | 明确不做 |
|---|---|---|
| gateway | 认证/租户（401/403），转发 | 不实现 `GlobalExceptionHandler`，`JwtAuthFilter.reject()` 手写同格式 JSON |
| BFF | 聚合、代理、**写路径鉴权**、路由层分流 | 不吞体层错误（体层错误码原样上报） |
| Java 三服务 | 业务异常 → 错误码 → HTTP，**路由层分流** | 不伪造上游结果 |
| nlp-service | 算法与请求校验 | 不决定业务语义 |
| 前端 | 降级判定与**可见化** | 不静默回落 Mock |

---

## 2. 运行期异常流程

### 2.1 统一错误信封（全平台唯一格式）

**所有错误响应都是 `{code, message, details}`**（R1-08）。客户端只需一套解析逻辑。

| 服务 | 出处 |
|---|---|
| gateway-service | `JwtAuthFilter.reject()` 手写 JSON（WebFlux 不走 `@ControllerAdvice`） |
| session-manager / sense-service / body-service | `common/GlobalExceptionHandler` + `ErrorCode` 枚举 |
| wp-bff | `server.js` → `fail(req,res,status,code,message,details)` |
| nlp-service | `app/main.py` → 三个 `@app.exception_handler` |

### 2.2 路由层分流：404 / 405 / 415（**三种语义必须可区分**）

**为什么必须区分**：前端据 **404** 判定「端点未实现（契约 `x-wp-status: planned`）」，据 **5xx** 判定「服务故障」。若未映射路由落进兜底 500，两者不可区分，前端既不能降级也不能报障。

| 情形 | HTTP | 错误码 | 响应头/明细 |
|---|---|---|---|
| 路径不存在 | **404** | `AGENT_NOT_FOUND` | Java `details={}`；BFF `details={path}` |
| 路径存在、方法不支持 | **405** | `AGENT_METHOD_NOT_ALLOWED` | 必带 `Allow` 头；`details={method, supported}` |
| Content-Type 不支持 | **415** | `AGENT_BAD_REQUEST` | `details={content_type}`（Java 侧） |
| 请求体不合法 | **400** | `AGENT_BAD_REQUEST` | `details` 为「字段 → 原因」映射 |

**各服务实现落点**

| 服务 | 机制 |
|---|---|
| Java 三服务 | `@ExceptionHandler({NoResourceFoundException, NoHandlerFoundException})` → 404；`HttpRequestMethodNotSupportedException` → 405；`HttpMediaTypeNotSupportedException` → 415 |
| wp-bff | `ROUTE_GUARD`（**由 `IMPLEMENTED_ENDPOINTS` 派生**，不另立第三份端点真相）→ 命中路径但方法不符则 405 + `Allow`；否则 404 |
| nlp-service | `StarletteHTTPException` 统一处理器（透传框架的 `Allow` 头）；**422 一律归并为 400**，使状态码语义与 Java/BFF 一致 |

**实测证据**（2026-09-18，运行态）

| 探测 | 结果 |
|---|---|
| `GET 8083 /api/body/definitely-not-a-route` | 404 `AGENT_NOT_FOUND` |
| `DELETE 8083 /api/body/knowledge` | 405 `AGENT_METHOD_NOT_ALLOWED`，`details.supported=[POST, GET]` |
| `POST 8083 /api/body/rag/answer` + `text/plain` | 415 `AGENT_BAD_REQUEST` |
| `GET 8090 /api/wp/not-exist` | 404 `AGENT_NOT_FOUND`，`details={path}` |
| `GET 8090 /api/wp/knowledge/search` | 405 + `Allow: POST` |
| `GET 8090 /api/wp/middleware/redis/start` | 405 + `Allow: POST`（参数化路径同样覆盖） |
| `GET 8000 /api/nlp/not-exist` | 404 `AGENT_NOT_FOUND` |
| `GET 8000 /api/nlp/embed` | 405 + `allow: POST` |
| `POST 8000 /api/nlp/embed` `{"texts":"abc"}` | 400 `details={texts: Input should be a valid list}` |

### 2.3 信封不一致的历史（**三个形状 → 一个**）

| 服务 | 2026-09-18 之前 | 现在 |
|---|---|---|
| Java 三服务 / gateway | `{code,message,details}` | 不变（基准） |
| wp-bff | `{error: "..."}` | ✅ 已统一 |
| nlp-service | FastAPI 默认 `{detail: ...}` | ✅ 已统一 |

> **行为变更提示**：任何解析 BFF 旧 `error` 字段的外部消费方需改读 `message`。仓库内消费方已全部核对：前端只读 HTTP 状态码（`error.response.status`），4 处旧断言已改写为断言 `code`/`message`。
>
> **规范性结论**：新增服务的错误出口**必须**沿用该信封，`07-运行时配置与横切约定` §5 已有明文约定。

### 2.4 鉴权异常流程（401 / 403）

| 触发 | HTTP | 错误码 | 硬约束 |
|---|---|---|---|
| 无 / 非法 Bearer（gateway） | 401 | `AGENT_UNAUTHORIZED` | — |
| JWT 缺 `tenant_id`（gateway） | 403 | `AGENT_FORBIDDEN` | — |
| BFF 控制端点无令牌 / 令牌错 | 401 | `AGENT_UNAUTHORIZED` | **不触达下游** |
| BFF 写路径来源不在白名单 | 403 | `AGENT_FORBIDDEN` | **不触达下游（CSRF 防护）** |

**判定的核心不变量：鉴权必须按「方法」判定，不能按「路径」判定。** `GET /api/wp/knowledge` 免令牌（只读），但 `POST`（写）必须与 `/middleware/{key}/start|stop` 同级校验。反向用例必须断言「鉴权失败时下游零调用」——否则写成 `assert status==401` 却漏了后门。

实测：无令牌 `401`、伪造 `Origin` `403`，且测试断言 `jsonRequest` 未被调用。

### 2.5 降级链路（逐服务）

**① session-manager —— 上游不可用时的业务降级**

| 上游 | 降级行为 | 开关 |
|---|---|---|
| nlp-service | 意图识别退化为 `FALLBACK`（`intent=闲聊, confidence=0.5, engine=FALLBACK`），**让问答链路继续走模板回答而非整链路 503** | `app.nlp.degrade-on-failure`（默认 true） |
| body-service | 检索退化为空结果 | `app.body.degrade-on-failure`（默认 true） |

关掉开关 → 抛 `BizException(AGENT_UPSTREAM_UNAVAILABLE)` → **502**。

**② body-service —— 上游引擎缺失时的确定性降级**

| 能力 | 缺失时 | 标记 |
|---|---|---|
| 嵌入 | 回落 `hash-ngram-768` 确定性后端（DEBT-010） | `degraded=true` |
| 重排 | 回落 `lexical-coverage` 词法打分（DEBT-011） | `degraded=true` |

入库失败**不留半成品**：文档状态机 `PENDING → INDEXED`，异常时写 `FAILED` 并把原因截断落库后**原样抛出**（不吞错、不假装成功）。
重排不可用**不阻断检索**（保留召回顺序 + `log.warn`）。

**③ sense-service —— 渠道级自愈**

`渠道故障 → 健康监控自动隔离 → 路由降级到候选渠道 → 指数退避重试（默认 maxAttempts=3）→ 重试耗尽入死信（DeadLetterStore）`。

**④ wp-bff —— 体层不可用时的「可用性信封」（**设计取舍，非缺陷**）**

体层不可用/被拒时返回 **HTTP 200 + `{available:false, reason, error_code?}`**，而不是 5xx。理由：对知识视图而言「体层不可用」是一种**要展示的状态**（页面渲染「体层不可用」标签），且需保持响应体结构统一。

> **已知代价**：监控无法仅凭状态码区分该情形，需读 `available` 字段。若要按状态码告警，需改为 502 并在前端同步调整——**当前不改**，如实登记为取舍。

**⑤ 前端 —— 降级必须可见**

`status.ts` 的 `reportDegrade(scope, reason)` / `reportApiOk(scope)`：失败即登记，成功即清除；顶栏徽标 `API · 降级 N` + 全局横幅 + 逐模块提示。

- **读端点**：可回落演示数据，但必须登记降级 + 页面标注「演示数据」
- **写端点**：一律走 `plannedEndpointError()` —— 登记降级 + 抛说明性错误，**绝不伪造成功**
- 视图层 retry/apply 类动作全部 `try/catch`，**失败不改本地状态**

**⑥ tool-executor —— 沙箱真隔离不可用时的显式降级（Phase 5）**

| 能力 | 缺失/不可用时 | 标记 | 是否伪实现 |
|---|---|---|---|
| Docker 沙箱 | 回落 `RestrictedProcessBackend`（受限子进程，非真隔离） | `active_backend=process-restricted` + `degraded=true` + `isolated=false` | ❌ 否 —— 降级**可见**，`GET /api/tool/sandbox` 如实上报 |
| 审计落 PG | 回落进程内审计 | `audit_backend=in-memory` + `degraded=true` | ❌ 否 |
| 工具注册表 | 进程内（重启丢失） | `registry_backend=in-memory`（DEBT-019） | ❌ 否 |
| 工具决策（Python 侧） | 规则决策 | `decider="rule"` | ❌ 否 |

**工具执行失败**（超时 / 非零退出 / 沙箱拒绝）一律返回 `success:false` + 真实错误码（见 `07-运行时配置与横切约定.md` §5.2），
信封 `details` 带 `call_id` / `audit_id`；**连续失败 3 次触发熔断 30s**（`AGENT_TOOL_CIRCUIT_OPEN`），不静默返回空结果。
BFF `/api/wp/tools*` 在 tool-executor 不可达时回 **200 + `available=false` + `gaps`**（与体层同一「可用性信封」约定）。

**⑦ BFF 子请求失败：四类原因必须可区分（Phase 5 · 2026-09-19 加固）**

BFF 聚合多分支时，子请求失败**不再统一塌缩为 `null`**，而是携带原因码：

| `reason` | 语义 | 运维动作 | 若曾合并会怎样 |
|---|---|---|---|
| `timeout` | 对端在预算内未返回（冷启动 / 负载高） | **重试通常即恢复** | 被读成"服务不可用" → 白重启服务 |
| `unreachable` | 连接被拒 / DNS 失败 | 查进程与端口 | 同上 |
| `endpoint_missing`（HTTP 404） | **端点不存在 —— 版本不匹配的强信号** | 升级对端服务 | 被读成"不可用" → 白排查网络 |
| `http_error` / `bad_json` | 对端 5xx / 返回非 JSON | 查对端日志 | 无区分度 |

分片缺失码随之外带后缀：`sandbox_timeout`（慢）/ `sandbox_missing`（版本旧）/ `sandbox_unavailable`（真连不上）。
响应同时给出 `partial_note`（人类可读解释）与 `probe_budget_ms`（各子请求预算，排查"慢 vs 没有"用）。
**前端 `ExecutionView.vue` 必须把码翻成人话**，不得直接把 raw code 打到用户界面。

> **教训**：沙箱冷探测 5.7s 超出 BFF 工具域 5.0s 预算（**余量仅 0.7s**）时，工具域曾把"慢"叙述成"沙箱不可用"——
> 与服务侧真相恰好相反，且**不影响任何功能断言**，单测全绿、契约 0 FAIL，只表现为静默降级。
> **凡返回 `null` / `false` / 空数组的位置，都要问一句「调用方还能区分出哪几种情况」。**

### 2.6 planned 端点：三态判定

| 状态码 | 语义 | 前端动作 |
|---|---|---|
| 2xx | 正常（含 BFF 的 `available=false` 可用性信封） | 直接用，读 `available` 判降级 |
| **404** | 契约 `x-wp-status: planned`，本阶段不提供 | 登记降级 → 展示演示数据或禁用入口 |
| 400 / 401 / 403 / 405 / 415 | 客户端用错（参数/凭据/方法/媒体类型） | **报错并上抛**，不得静默降级 |
| 5xx | 服务故障 | 登记降级 + 报障 |

`x-wp-status: planned` 的端点在第 2.5 ④ 与本节共同约束下，**不会出现「静默拿 Mock 冒充真实结果」**。

### 2.7 异常处置决策树（排障用）

```
现象
├─ 拿到 404？
│   ├─ 该路径在契约里是 planned ──────────→ 正常，前端走降级展示
│   └─ 契约里是 implemented ─────────────→ 真问题：查路由是否注册（@PostMapping / 检查 ROUTE_GUARD 派生）
├─ 拿到 405？ ────────────────────────────→ 客户端方法用错，看 Allow 头自纠；若 Allow 与预期不符 → 契约与实现不一致
├─ 拿到 415？ ────────────────────────────→ Content-Type 必须是 application/json
├─ 拿到 401 / 403？
│   ├─ 直连 8090 写路径 ─────────────────→ 预期行为（令牌由 Vite 代理注入，浏览器直连必然 401）
│   └─ 经代理仍 401 ────────────────────→ 令牌文件与进程不一致（重启后令牌会重新生成）
├─ 拿到 502 / 503？ ─────────────────────→ 上游不可用：看该链路的 degrade-on-failure 开关与上游健康
├─ 拿到 500？ ──────────────────────────→ 兜底分支。**只记日志、不回内部消息**；查服务日志 "Unhandled exception"
└─ 200 但数据不对？ ────────────────────→ 读 available / degraded / data_source 三个字段，别只看状态码
```

---

## 3. 研发期异常流程（过程性，D 组）

**编排原则**：这一类异常不产生业务代码，却反复消耗时间。归纳成「现象 → 根因 → 规范动作」，可直接前置检查。

> **编号约定**：`D-xx` 按**新增时间**递增，**与章节顺序不同步** ——
> 后续轮次新增的条目挂到所属类目下、取当时的下一个空号。
> 因此出现「3.2 里是 D-31/D-32、3.3 里是 D-33~D-35、3.4 里却是 D-13~D-16」这类顺序跳变是**预期行为**。
> **唯一硬约束：编号全局唯一**（新增条目前先 `grep -o '^| D-[0-9]*' | sort | uniq -d` 确认无撞号）。

### 3.1 环境与工具类

| # | 现象 | 根因 | 规范动作 |
|---|---|---|---|
| D-01 | `ls`/`head`/`tail`/`dirname` 报 `command not found` | WorkBuddy 注入的 `shell-runtime-bash-env.sh` 打坏了 PATH | 每条命令前 `export PATH="/usr/bin:/bin:/c/Windows/System32:$PATH"` |
| D-02 | `mvn` 报找不到 POM | 根目录无 POM，聚合 POM 在 `services/java` | 一律 `cd services/java` 再跑 |
| D-03 | 托管 Python 与系统 Python 都没有 pytest | 依赖装在项目 venv | 用 `services/python/venv/Scripts/python.exe -m pytest` |
| D-04 | HTML 孪生生成失败（`markdown` 缺失） | 曾用 anaconda，已卸载 | 用托管 Python + `PYTHONPATH=E:\AI\核心知识\.workbuddy\tmp\pylibs` |
| D-05 | `wmic` / `Start-Process` / `rm -rf target` 被安全策略拦截 | 沙箱黑名单 | 换 `Get-CimInstance`、`WMI Invoke-CimMethod`、Maven `clean` |
| D-06 | `taskkill //PID` 在 Git Bash 报「无效参数」 | MSYS 参数转义 | 改用 PowerShell `Stop-Process -Id <pid> -Force` |
| D-07 | `curl --data-binary @/tmp/x.json` 请求体被破坏 | MSYS 把 `/tmp` 改写成 Windows 路径 | 用 stdin：`cat file \| curl --data-binary @-` |

### 3.2 构建与运行类

| # | 现象 | 根因 | 规范动作 |
|---|---|---|---|
| D-08 | `Unable to rename ...jar.original` 打包失败 | **Windows 文件锁**：运行中的实例锁住 `target/` | 先 `Stop-Process` 停实例，再 `package` |
| D-09 | 服务起在随机端口（如 53568） | 环境注入了 WorkBuddy 路由变量，被 Spring **松散绑定**误读为 `server.port` | 启动显式 `--server.port=<port>` |
| D-10 | PG 冷层不可用，静默回落内存 | 配置默认凭据 `lifeform/lifeform` 与 compose 的 `agent/agent123` 不符 | 默认值对齐 `docker-compose.yml` |
| D-11 | 后台起的服务在命令结束后消失 | `nohup ... &` 随 shell 退出被回收 | 用**后台任务**方式起常驻进程 |
| D-12 | 8090 跑的是旧版代码（端点 404） | 运行态与代码不同步 | 改动后**必须重建 + 重启**，并用 `netstat` 反查 PID 确认进程身份 |
| D-31 | 健康检查脚本 `exit=7`，`[java services]` 之后一行都没输出 | `set -e` + `code=$(curl ...)`：**赋值语句的退出码 = curl 的退出码**，服务不可达即终止脚本 | 命令替换加 `\|\| true`；**检查脚本必须在"被测对象挂掉"时仍能跑完并报告**，否则它恰在最该工作时失效 |
| D-32 | 页面「满屏演示数据 + 一堆降级」，看不出是环境问题 | `WP_BFF_URL`（代理目标）与 `WP_BFF_PORT`（BFF 监听）**互不校验**，端口错则请求 500 被 `safe()` 兜住并逐域回落 Mock | `vite.config.ts` 的 `bffReachabilityGate()` 启动探活 + 终端告警；启动脚本里 **URL 由 PORT 单点派生** |

### 3.3 超时预算类

> 这一类的共同点：**缺陷只存在于两个数字的差值里**，两侧各自看都"没问题"，单测全绿、契约 0 FAIL。

| # | 现象 | 根因 | 规范动作 |
|---|---|---|---|
| D-33 | 上游报「XX 不可用」，但直连该端点返回 200 | **跨服务超时预算差值**：对端操作耗时 5.7s > 上游子请求预算 **5.0s**（余量仅 0.7s），`timeout` 与「缺失」在上游被塌缩成同一个 `null` | 失败原因分码返回（`timeout` / `unreachable` / `endpoint_missing` / …）；慢操作**单独放宽预算**；见 §2.5 ⑦ |
| D-34 | 「预算倒挂」只存在于两个数字的**差值**里，单测全绿、契约 0 FAIL，直到抖动才现形 | **无权威登记表**：上游预算与下游最坏耗时分散在各自代码/语言里，**纸上自洽、相遇即错**。实测 wp-bff 对 `brain/ask` 用默认 5000ms，而下游 LLM 级联最坏 **22s**（余量 **0.23x**）；即便只跑模板引擎，最坏 3s×2=6s **也已超预算** —— 倒挂是**现在成立**而非未来风险 | 建 `contracts/timeout-budget.yaml` 登记表（两个数字必须同框）+ `scripts/timeout-budget-check.py` 门禁（语义/锚点/魔法数字三层）+ `start.sh gate` 前置自检；wp-bff 改按操作类型分档，**生成型必须显式声明 `GENERATE` 档** |
| D-35 | 门禁全绿，但**绕过常量的调用点**仍写着 `timeoutMs: 30000`（= 下游最坏，余量 0） | **门禁自身有盲区**：锚点校验锚的是**常量定义**，看不见调用点就地写死的字面值 | 加一层「魔法数字扫描」扫调用点；**加门禁后必须反向验证门禁有效**（植入违规应 FAIL），否则只是多了一个绿着好看的东西 |

### 3.4 测试与验收类

| # | 现象 | 根因 | 规范动作 |
|---|---|---|---|
| D-13 | 单测全绿但端到端失败（Qdrant 拒收 point id） | **服务端约束单测无法覆盖** | 构建期单测 + 运行时 E2E **双层验收**，缺一不可 |
| D-14 | E2E 判定不确定 | 上一轮残留数据污染 | 每轮先清残留：DB TRUNCATE + Qdrant 集合 DELETE + Redis FLUSHALL |
| D-15 | 中文请求体落库后乱码 | Git Bash **内联 `-d '中文'`** 的编码转换 | 用文件 + stdin 传体；断言时校验**中文往返**（查询回显 + 锚点词命中） |
| D-16 | 30 分钟排查后发现请求根本没发出去 | 只看状态码不读响应体 | 异常用例必须同时断言 **状态码 + 错误码 + `details`** |

### 3.5 门禁与一致性类

| # | 现象 | 根因 | 规范动作 |
|---|---|---|---|
| D-17 | 覆盖率门禁红灯 `103/106` | 新增测试文件未登记进模块文档 | 新增源文件必同步 `docs/java-services/*`（有门禁，会拦） |
| D-18 | 门禁全绿，但文档里的用例数/行数/端点数**全错** | **门禁只守「存在性」，不守「准确性」** | 数字必须拿实测输出对（`grep -c @Test`、`wc -l`、`contract-check`）；已提台账待办 17 做自动化断言 |
| D-19 | 同一提交里"红灯亮了、数字全错却静默通过" | 见 D-18 | 收尾自检清单逐项核，不依赖门禁 |
| D-20 | 一台机器改完，另一台收到的是过期基线 | 未先拉远端 | 开发前先 `git fetch` + 用 `git ls-remote` 比哈希（`git rev-list --count` 在缺 tracking ref 时**静默输出空行**，会误判为落后 0） |
| D-21 | `git push origin` 无限挂起 | `origin` 是 `ghfast.top` 加速代理 | **一律用 `github`（SSH）**；`main` 分支刻意停初始提交，不要推 |
| D-22 | 同一消息对同一文件发多个编辑，部分**静默丢写**（每个都报"成功"） | 工具层并发写覆盖 | 同文件多处改动用**脚本 + 命中次数断言**，或逐个改；改完用 `grep -q` 复验落盘 |

### 3.6 文档与口径类

| # | 现象 | 根因 | 规范动作 |
|---|---|---|---|
| D-23 | 报告"验收通过"，实测端点却 404 | 报告里的路径是**照抄会 404** 的写法（`/api/body/rag` 实为 `/rag/answer`） | 文档里每条端点路径用 `grep -n "@PostMapping\|@GetMapping"` 反查；示例逐条与真实返回值比对 |
| D-24 | 读设计的人误判技术栈（以为用了 pgvector） | 设计写「PG+pgvector」「Redis 语义缓存」，实现是纯 PG 关系表 + 精确指纹缓存 | **分两层处置**：实现侧自述（注释/`role` 文案/README）可直接改准；**需求·设计文档原文属口径变更，先问用户再落笔** |
| D-25 | Mock 与真实 API 字段漂移，切 `api` 后静默丢数据 | 只做**单向**核对（"Mock 有的真实有没有"），漏掉"真实多出来的字段" | **双向**逐字段核对：值/枚举/文案三类都要比；同一份数据有多处字面量要一起改 |
| D-26 | 中间件 up/down 状态前端显示与实况相反 | Mock 快照与实况写反 | Mock 快照要按**实测**校准，不靠记忆 |
| D-27 | 阶段报告内部数字互相矛盾 | 回填时只改了总数没改分项（或反之） | 「合计 + 分项 + 交叉引用」**三处一起核**；分项之和必须等于总数 |
| D-28 | 改了 `.md` 忘了重生 `.html` | 孪生需显式生成 | 收尾跑 STALE 检查：`[ "$md" -nt "$html" ] && echo STALE` |

### 3.7 安全类（**最高优先，出现必查全仓**）

| # | 现象 | 根因 | 规范动作 |
|---|---|---|---|
| D-29 | 浏览器直连 8090 即可**无鉴权写入知识库** | 补写路径时沿用了 `/knowledge` 的「只读免令牌」豁免 | **安全边界按方法判定，不按路径**；新增写方法一律与既有控制端点同级校验（来源白名单 + 控制令牌），并补**反向用例断言「鉴权失败不触达下游」** |
| D-30 | 未映射路由返回 500 而非 404 | 三服务 `GlobalExceptionHandler` 只有「业务/参数/兜底」三档，路由层异常全落兜底 | 见 §2.2；三服务同源，改一处要查其余 |

---

## 4. 规范化动作清单

### 4.1 新增一个端点时

- [ ] 契约 `contracts/work-platform-bff-openapi.yaml` 加 x-wp-status（implemented / planned）
- [ ] BFF：加进 `IMPLEMENTED_ENDPOINTS`（`ROUTE_GUARD` 自动派生 405 分流）
- [ ] Java：确认 `GlobalExceptionHandler` 四档齐备（业务 / 路由层 404·405·415 / 参数 / 兜底）
- [ ] 补测试：正常 + 404 + 405（+ 415 若收 body）
- [ ] 文档：`java-services/*` 端点表、`功能开发流程` 端点清单、`项目进度总览` 功能矩阵

### 4.2 新增一条**写路径**时（安全红线）

- [ ] 与既有控制端点**同级**校验（来源白名单 + 控制令牌），**先查是否误沿用了只读豁免**
- [ ] 补反向用例：无令牌 → 401、伪造来源 → 403，且**断言下游零调用**
- [ ] 契约里该端点补 401/403 响应
- [ ] E2E 手测写入 → 检索 → 指标三段

### 4.3 发现一个异常时

- [ ] 判性质：**可闭环** → 改代码 + 补测试；**超范围/需规划决策** → 登记台账（**不静默略过**）
- [ ] 判口径：`degraded=true` + 降级后端 = **可接受取舍**；返回假结果 / 静默回落 / 假装成功 = **伪实现（红线）**
- [ ] 若属「设计↔实现」差异 → 实现侧自述可自改；**设计原文先问用户**

### 4.4 每轮收尾

- [ ] 六道门禁全绿（Java / Python / wp-bff / 契约 / 文档覆盖 / 前端）
- [ ] **数字与实测输出对齐**（门禁不守准确性）
- [ ] HTML 孪生重生，STALE 为空
- [ ] 编辑落盘复验（同文件多处改动）
- [ ] 提交推 **`github`** 三分支，核对同哈希，工作区干净

---

## 5. 未闭合项与后续加固

| # | 项 | 状态 |
|---|---|---|
| 1 | **BFF 体层不可用返回 200** 的取舍 | 已登记为取舍（§2.5 ④）；若要按状态码告警需改 502 + 前端同步 |
| 2 | **gateway 未纳入路由层分流**：8080 本轮**未做运行态验证**，其未匹配路由的响应体是否统一信封**未确认** | 待验证（已如实标注，不作断言） |
| 3 | 台账待办 17：把「用例数/行数/端点数」做成自动化断言（解 D-18/D-19 的结构性防线） | 下轮加固 |
| 4 | 台账待办 16：Mock ↔ 真实响应字段一致性自动化校验（解 D-25） | 下轮加固 |
| 5 | 嵌入/重排真实模型权重未安装（DEBT-010/011） | 诚实降级，装模型后自动切换 |
| 6 | `DEBT-002` RAG 生成仍为模板拼接（`generator=template`） | Phase 4 大脑期 |

---

## 6. 变更台账（本文档同轮）

| 改动 | 文件 | 验证 |
|---|---|---|
| BFF 统一错误信封 + 路由层 405 分流 | `services/node/wp-bff/server.js` | wp-bff 测试 **29 → 34**（+5）；运行态 9 项探测全通过 |
| BFF 测试断言改 `code`/`message` | `services/node/wp-bff/test/server.test.js` | 同上 |
| nlp-service 统一错误信封（原 FastAPI `{detail}`） | `services/python/nlp-service/app/main.py` | pytest **52 → 62**（+10） |
| nlp-service **补 HTTP 层测试**（此前只有算法单测） | `services/python/nlp-service/tests/test_http_errors.py` | 新增 10 项 |
| 契约：`ErrorEnvelope` schema + 404/405 响应声明 | `contracts/work-platform-bff-openapi.yaml` | 契约检查 0 FAIL；YAML 解析通过 |
| 本文档 | `docs/异常流程归纳.md` | — |

**回归基线（本轮实测）**：Java **130/130**（gateway 2 · session 15 · sense 53 · body 60）·
Python **62/62** · wp-bff **34/34** · 契约 **0 FAIL** · 文档覆盖 **106/106** · 前端 `vue-tsc` + `vite build` 通过

> **上表为 2026-09-18 口径**，按「历史归档不回改」约定保留原样。以下为后续轮次的追加记录。

**2026-09-19 追加（超时语义与预算轮）**

| 改动 | 文件 | 验证 |
|---|---|---|
| 新增 §2.5 ⑦ 跨服务失败原因分码（`timeout`/`unreachable`/`endpoint_missing`/`http_error`/`bad_json`） | `docs/异常流程归纳.md` | 6 探针实例实测三态分明 |
| 新增本文档 §3.2 D-31 / D-32（healthcheck 自废武功、代理端口无校验）与 §3.3 D-33~D-35（超时预算三类） | `docs/异常流程归纳.md` | 门禁反向验证 4 项 |
| BFF 调用点改命名档位；`knowledge` 入库去魔法数字 | `services/node/wp-bff/server.js` | wp-bff 测试 **87/87** |
| 超时预算登记表 + 门禁 | `contracts/timeout-budget.yaml`、`scripts/timeout-budget-check.py` | `ok=16 · gap=3 · fail=0` |

**追加回归基线（2026-09-19 实测）**：Java **248/248** · Python **158/158** ·
wp-bff **87/87** · 契约 **0 FAIL** · 超时预算 **0 FAIL** · 文档覆盖 **158/158**

---

*编制：WorkBuddy · 口径时间：2026-09-18（含 2026-09-19 追加） · 代码基线：`6616616`*
