# 2026-09-21 模型代理与 P95 指标增强

## 模型接入增强

1. 模型配置支持 `proxy_url` / `no_proxy` / `trust_env`。
2. 模型探测与真实推理统一使用模型级代理配置。
3. 完整读取供应商 `/models` 响应，移除前 50 个模型截断。
4. 当供应商返回模型列表时，配置模型不在列表内明确失败。
5. OpenRouter `openrouter/free` 实测 `ok=true`、`model_present=true`。

## 指标增强

Java 服务开启：

```yaml
management.metrics.distribution.percentiles-histogram:
  http.server.requests: true
  jvm.gc.pause: true
```

Prometheus 已暴露 HTTP 与 GC histogram bucket，指标页新增 HTTP P95 和 GC P95。

## 验证

- nlp-service 模型配置测试：35 passed；
- wp-bff：120/120；
- 前端：27/27；
- TypeScript / 生产构建：通过；
- Playwright Mock：2/2；Live API：1/1；
- Java Maven package：BUILD SUCCESS；
- 契约：51 路径 / 55 方法，planned 20，0 FAIL。
