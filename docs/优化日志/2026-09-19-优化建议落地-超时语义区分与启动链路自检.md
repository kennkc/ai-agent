# 优化建议落地 · 超时语义区分与启动链路自检

> **项目**: Agent-Lifeform | **日期**: 2026-09-19
> **本轮范围**: `services/node/wp-bff`（传输层 + 工具域聚合）· `web/work-platform`（vite 代理自检 + 执行视图）· `scripts/`（启动与健康检查脚本）
> **触发**：上一轮（《工作平台真实链路打通与沙箱探测非阻塞化》）交付时留下三条优化建议，用户指示「按优化建议执行任务」，本轮逐条落地
> **代码基线**：`0921345`（开工前已核对远端三分支无新提交）

---

## 1. 结论摘要

| 维度 | 落地前 | 落地后 |
|---|---|---|
| 子请求失败语义 | 全部塌缩为 `null`（超时 = 连不上 = 404 = 坏 JSON） | 五类分明：`timeout` / `unreachable` / `endpoint_missing` / `http_error` / `bad_json` |
| 工具域分片缺失码 | `sandbox_unavailable`（**掩盖真相**） | `sandbox_timeout` / `sandbox_missing` / `sandbox_unavailable` 三码分开 |
| 前端告警文案 | 直接打印 raw code | 翻成人话，并显式标注「慢，非缺失」 |
| vite 代理目标 | 与 `WP_BFF_PORT` **互不校验**，错了静默满屏 Mock | 启动探活 + 不可达终端告警 |
| 启动脚本 | `start.sh` / `start-dev.bat` **缺 wp-bff 与前端两步** | 补齐；`WP_BFF_URL` 由 `WP_BFF_PORT` 单点派生 |
| healthcheck | `set -e` 自杀：首个不可达服务处即终止，**报不出谁挂了** | 修好并**整体重跑**：19 OK / 6 BAD（BAD 均为本轮未启动） |
| wp-bff 测试 | 73 | **81 / 0 失败**（+8） |
| Java / 契约 / 文档覆盖 | 248 / 0 FAIL / 158-158 | 248 / 0 FAIL / 158-158（本轮未动 Java） |

---

## 2. 缺陷一：上游把「慢」和「没有」合并成同一个 `null`

### 2.1 问题的本质

上一轮在服务侧把沙箱探测改成非阻塞（5.7s → 0.5s），但**表达层的问题没动**：

```js
// 修复前
function httpRequestJson(url, { ... } = {}) {
  return new Promise(resolve => {
    ...
    request.on('timeout', () => { request.destroy(); resolve(null) })
    request.on('error',   () => resolve(null))
    response.on('end',    () => { try { resolve(JSON.parse(raw)) } catch { resolve(null) } })
  })
}
```

四种完全不同的失败——**对端太慢 / 对端没起 / 端点不存在 / 返回不是 JSON**——全部 `resolve(null)`。
调用方拿到 `null` 只能写一句"不可用"。于是：

- 沙箱探测冷启动慢 5.7s → 被叙述成 **"沙箱不可用"**
- tool-executor 是旧版本、没有 `/api/tool/sandbox` → 同样被叙述成 **"沙箱不可用"**
- 服务真的没起 → 还是 **"沙箱不可用"**

**三者运维动作完全不同**（重试 / 重启服务 / 升级服务），却给出同一句话。

### 2.2 修复

新增 `httpRequestJsonMeta()`，返回结构化的失败原因：

```js
{ ok: true,  data: {...}, status: 200 }
{ ok: false, data: null,  reason: 'timeout'|'unreachable'|'endpoint_missing'|'http_error'|'bad_json', status?: number }
```

`404` 单独识别为 `endpoint_missing`（**版本不匹配的强信号**，与"服务不可达"是两回事）。
原 `httpRequestJson` 保留为丢弃原因后的薄封装 —— **既有 73 条用例零改动全绿**，向后兼容。

### 2.3 工具域聚合升级

```
partial_reasons: ['sandbox_timeout']      ← 慢
partial_reasons: ['sandbox_missing']      ← 端点不存在（版本旧）
partial_reasons: ['sandbox_unavailable']  ← 真连不上
```

同时新增三个字段：

| 字段 | 用途 |
|---|---|
| `reason_code` | 整体不可用时的机器码，供前端分支 |
| `partial_note` | 人类可读解释（"对端未在预算内返回 —— 这是慢，不是没有"） |
| `probe_budget_ms` | `{default, sandbox}` 各子请求预算，排查用 |

另外把**沙箱分片单独放宽超时预算** —— 它是唯一会触发容器探测的子请求，用与普通读端点
相同的预算本身就是设计错配。

**预算口径更正（同日复核）**：本文件初稿写"上游 2500ms 预算"，实际工具域走的是
`httpRequestJson` 默认 **5000ms**（`WP_BFF_TOOL_TIMEOUT_MS` 现在显式化为 5000，沙箱 8000）。
`httpGetJson(url, timeoutMs = 2500)` 是另一个仅 GET 的辅助函数，与工具域无关 —— 两者都在
`server.js` 里且都是超时默认值，极易混淆。**更正后余量只有 0.7s，比初稿描述的更脆弱。**
详见《2026-09-19-工作平台真实链路打通与沙箱探测非阻塞化》§3.2 的口径更正。

### 2.4 前端侧

`ExecutionView.vue` 新增 `PARTIAL_REASON_LABELS` 映射表，分片缺失告警不再打印 raw code：

```
sandbox_timeout → 「沙箱状态读取超时（慢，非缺失）」
sandbox_missing → 「沙箱端点不存在（服务版本可能较旧）」
```

整体不可用的空态面板同时展示 `reason_code`，并对 `timeout` / `endpoint_missing` 各给一句
**下一步动作**（"重试通常即恢复" / "多半是服务版本旧于本 BFF"）。

`types/index.ts` 的 `ExecutionOverview` 补 `reason_code` / `partial_note` / `probe_budget_ms`。

---

## 3. 缺陷二：vite 代理目标与 BFF 端口无交叉校验

### 3.1 为什么危险

`WP_BFF_URL`（前端代理目标）与 `WP_BFF_PORT`（wp-bff 监听端口）是**两个互不校验的环境变量**。
端口一错，页面不会报错：

```
请求 → 500 → api/provider.ts 的 safe() 兜住 → 逐 scope reportDegrade() → 回落 Mock
```

用户看到的是「满屏演示数据 + 顶栏一堆降级」，**从页面上无法判断这是环境问题**。
上一轮实测就踩到了：BFF 落在 8090，代理想指别处。

### 3.2 修复

`vite.config.ts` 新增 `bffReachabilityGate()` 插件：

- **启动期探活**：对 `WP_BFF_URL` 与 `WP_GATEWAY_URL` 各打一次 `/api/wp/healthz`（gateway 打根路径）
- **不可达 → 终端醒目告警**，直接给出 `WP_BFF_URL` 当前值与 `WP_BFF_PORT` 的排查提示
- **每条目标只响一次**，不刷屏
- 非阻塞（异步探活，不拖慢 dev server 启动）

**实测验证**：`WP_BFF_URL=http://127.0.0.1:9999` 起 vite → 终端输出告警；
经该实例打代理请求失败但**进程不崩**（无未捕获异常），3001/3099 监听正常。

---

## 4. 缺陷三：启动脚本缺服务 + healthcheck 自废武功

### 4.1 启动脚本

| 脚本 | 变化 |
|---|---|
| `scripts/start.sh` | 新增 `start_bff` / `start_frontend`；`JAVA_SERVICES` 补 `tool-executor`；**`WP_BFF_URL="${WP_BFF_URL:-http://127.0.0.1:$WP_BFF_PORT}"`** —— 单点派生，从根上消除两变量漂移 |
| `scripts/start-dev.bat` | 对齐（补 BFF 与前端步骤、同一端口派生规则） |
| `scripts/start-test.sh` | `start_java` 补 `tool-executor`（原来只起 4 个服务） |
| `scripts/healthcheck.sh` / `.bat` | 补 `8084` / `9095` / `8090` 三个探针 |

### 4.2 healthcheck 的 `set -e` 自杀缺陷（本轮新发现）

```bash
set -e
check_http() { code=$(curl -s --noproxy "*" -o NUL -w "%{http_code}" --max-time 5 "$url" 2>/dev/null); ... }
```

**问题**：命令替换里，`code=$(...)` 这个赋值的退出码 **= curl 的退出码**。
curl 连不上时以非 0 退出（如 7 = Failed to connect），配合 `set -e`
→ **脚本在第一个不可达服务处直接终止**。

也就是说：**这个脚本恰好在它最该工作的场景下失效** —— 服务挂掉时它报不出来，
只会安静地少输出一半。实测 `exit=7`，`[java services]` 之后一行都没有。

**修法**：
- 命令替换加 `|| true`，把非 0 退出码吃掉
- `-o NUL` 改为 `-o /dev/null`（`NUL` 依赖 Windows 设备名，Git Bash 下不可靠）

### 4.3 整体重跑结果

```
[infrastructure]   Redis OK · PostgreSQL OK · Qdrant OK · NATS BAD · Nacos OK · MinIO BAD · Kafka OK · Jaeger OK
[java services]    gateway BAD · session-manager OK · sense BAD · body OK · tool-executor OK
[grpc health]      gateway-grpc BAD · session-grpc OK · sense-grpc BAD · body-grpc OK · tool-grpc OK
[python+fe+bff]    nlp-service OK · work-platform OK · wp-bff OK
```

**19 OK / 6 BAD**。BAD 全部是"本轮未启动"：`gateway` / `sense-service`（含各自 gRPC）未起，
`NATS` / `MinIO` 非 Phase 5 依赖。**如实列出，不刷绿、不掩盖。**

---

## 5. 验证

| 类别 | 命令 | 结果 |
|---|---|---|
| wp-bff | `node --test` | **81 pass / 0 fail**（+8：失败原因分类 × 5、分片缺码后缀 × 2、预算字段 × 1） |
| Java | `scripts/mvn-dev.sh test` | 248 / 0 失败（本轮未动 Java，基线不变） |
| 契约（跨服务） | `python scripts/contract-check.py` | **0 FAIL**（3 WARN 为既有字典登记项） |
| 契约（工作平台） | `python scripts/contract-check.py --work-platform` | 60 端点，implemented **30 路径 / 31 方法**，**0 FAIL** |
| 文档覆盖 | `python scripts/java-doc-coverage.py` | **158/158** |
| 前端 | `vue-tsc --noEmit` + `vite build` | 均通过 |
| 端到端 | 经 3001 代理打 `/api/wp/tools` | `partial:false`，`probe_budget_ms` 生效，沙箱 `isolated=true / degraded=false` |
| 自检回归 | `WP_BFF_URL=127.0.0.1:9999` 起 vite | 终端告警出现；代理失败**不崩**；监听正常 |

---

## 6. 留下的思考

**同一类错误在本轮出现了三次，形状完全一致**：

1. `DockerSandboxBackend.available()` 在请求线程同步跑 docker CLI → **慢**被表达成**没有**
2. `httpRequestJson` 把所有失败塌缩成 `null` → **原因**被表达成**没有**
3. `healthcheck.sh` 的 `set -e` → 脚本**在"发现故障"这个动作上失败**，于是**故障**被表达成**没有**

三处的共同点：**下游只拿到一个空值，就丢失了全部可行动信息**。
这不是三个孤立的 bug，是同一个认知缺陷的三次显形 —— 值得作为后续评审的一条检查项：

> 任何 `null` / `false` / 空数组的返回点，问一句「调用方还能区分出哪几种情况？如果只剩一种，
> 那运维动作是不是就无从下手了？」

另一条更窄但更实际的：**跨服务超时预算的乘积关系没有守门**。
BFF 给子请求 5000ms，服务侧操作 5700ms —— 两个数字各自合理，相遇即错，**且只差 0.7s**。
本机无法用单测覆盖（要真起服务 + 真慢），只能靠运行态实测发现。
已记入 `docs/技术债台账.md`（跨服务超时预算）。
