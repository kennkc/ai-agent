# 业务指标、真实 E2E 与 MC-01 规模闭环（2026-09-21）

> **范围**：业务级 Prometheus 指标、告警、真实 BFF E2E、跨平台文档门禁、MC-01 多域规模、Phase 6 端点排期
> **结论**：本轮建议全部落地；Phase 6 代码仍未启动。

## 1. 业务级指标

### Collab Bus

新增 `CollabBusMetrics`：

- `lifeform_collab_active_domains`
- `lifeform_collab_heartbeat_pending`
- `lifeform_collab_messages_total`
- `lifeform_collab_consumer_retries_total`
- `lifeform_collab_deadletter_total`
- `lifeform_collab_heartbeat_reports_total`
- `lifeform_collab_heartbeat_flushed_total`

### LLM

新增 `app/observability.py`：

- `lifeform_llm_calls_total`
- `lifeform_llm_tokens_total`
- `lifeform_llm_latency_seconds`

### Tool

`ToolMetrics` 迁移到 Micrometer/Prometheus：

- `lifeform_tool_calls_total`
- `lifeform_tool_execution_seconds`
- `lifeform_tool_blocked_total`
- `lifeform_tool_circuit_open_total`

DEBT-021 已关闭。

## 2. 告警

新增业务告警：

- `LifeformCollabDeadLetters`
- `LifeformCollabRetriesHigh`
- `LifeformHeartbeatBacklog`
- `LifeformLlmFailureRatio`
- `LifeformLlmDegradedCalls`
- `LifeformToolFailureRatio`
- `LifeformToolCircuitOpen`

新增真实 webhook 生产示例：`infra/prometheus/alertmanager.production.yml.example`。

## 3. 真实 BFF E2E

新增：

- `playwright.live.config.ts`
- `e2e/live-bff.spec.ts`
- `npm run test:e2e:live`

真实启动 wp-bff 与 API 模式前端，验证：

- 总览加载；
- API 数据源标识；
- 后台服务目录与 `WP BFF` 项。

结果：**1/1 passed**。

## 4. 跨平台文档门禁

- 修复 `contract-check.py` Windows GBK UnicodeEncodeError；
- 更新 overview 旧契约统计；
- `doc-truth-check.py` 新增旧统计禁用检查；
- 新增 Phase 6 端点清单与 live E2E 资产检查。

## 5. MC-01 多域规模

使用真实 PostgreSQL + NATS JetStream + collab-bus 单实例：

| 域数 | 并发 | 创建 P50 | 创建 P95 | Consumer 增量 | 结果 |
|---:|---:|---:|---:|---:|---|
| 25 | 8 | 42.75ms | 70.80ms | 25 | pass |
| 100 | 16 | 103.34ms | 173.88ms | 100 | pass |
| 500 | 24 | 141.80ms | 254.34ms | 500 | pass |
| 1000 | 32 | 209.36ms | 384.22ms | 1000 | pass |

归档：`docs/test-reports/collab/2026-09-21/`。

边界：本次是单实例多域验证，多实例共享 consumer 行为仍待后续验证。

## 6. Phase 6 端点排期

- `contracts/phase6-endpoint-priority.yaml`
- `docs/Phase6-必需端点排期冻结稿.md`

已冻结 blocking / enabling / deferred 三层，不构成 Phase 6 开工声明。
