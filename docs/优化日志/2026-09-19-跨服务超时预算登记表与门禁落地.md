# 跨服务超时预算：登记表 + 门禁 + 生成型链路修复

> **项目**: Agent-Lifeform | **日期**: 2026-09-19
> **本轮范围**: `contracts/timeout-budget.yaml`（新建）· `scripts/timeout-budget-check.py`（新建）· `services/node/wp-bff/server.js`（分档）· `services/node/wp-bff/test/server.test.js`（+6 用例）· `scripts/start.sh`（前置门禁）
> **触发**：上一轮交付时留下的第 2 条优化建议 —— **「跨服务超时预算无人守：现在能看见是超时还是缺失，仍不能防新设的超时值再次小于对端最坏耗时」**
> **代码基线**：`0921345`（开工前已核对远端三分支无新提交）

---

## 1. 结论摘要

| 维度 | 落地前 | 落地后 |
|---|---|---|
| 超时值管理 | **一个默认值打天下**（`httpRequestJson` 默认 5000ms）+ 1 处写死的 30000 | 按**操作类型**分 8 档，命名常量，`WP_BFF_*_TIMEOUT_MS` 可覆盖 |
| 倒挂是否可发现 | 不可发现（单测全绿、契约 0 FAIL） | 权威登记表 19 条边 + 三层门禁，**倒挂即 FAIL** |
| 生成型链路预算 | 5000ms（下游 LLM 级联最坏 **22000ms**） | `GENERATE` 档 45000ms |
| 数字与文档一致性 | 靠人工核对（台账「数字级门禁未落地」长期欠账） | 锚点校验 + 魔法数字扫描，**代码实值 ≠ 表中数字即 FAIL** |
| 检查时机 | 运行期事后发现 | 提交期门禁 + `./scripts/start.sh` 前置自检 |

**一句话**：本轮把一个「靠人记得住」的隐性约束，变成了「忘了就红」的显性门禁。

---

## 2. 开工侦察：倒挂不是未来风险，而是**现在就成立**

先扫全仓所有跨服务超时值，起点状态（`0921345`）：

```text
services/node/wp-bff/server.js
  L55   function httpRequestJson(url, { ..., timeoutMs = 5000 })   ← 默认值，几乎所有调用点都用它
  L1212 timeoutMs: 30000                                           ← 唯一一处显式值（知识入库）
```

即：**BFF 的整个跨服务调用面，实质上只有两个数字**。逐条比对下游最坏耗时后：

| 边 | 调用点 | 落地前预算 | 下游最坏 | 余量 | 判定 |
|---|---|---:|---:|---:|---|
| TB-04 | `POST /api/body/knowledge`（入库） | 30000（写死） | 30000（嵌入读超时） | **1.00x** | ❌ 余量 0 |
| TB-05 | `GET /knowledge/stats`、`POST /body/retrieve` | 5000 | 10000 | **0.50x** | ❌ 小于下游 |
| TB-06 | `POST /nlp/brain/ask` | 5000 | **22000** | **0.23x** | ❌ 严重倒挂 |
| TB-07 | `memory/ingest`、`memory/compress` | 5000 | **22000** | **0.23x** | ❌ 严重倒挂 |
| TB-09 | `POST /session/{id}/ask` | 5000 | 10000 | **0.50x** | ❌ 小于下游 |

下游 22000ms 的来源（`app/brain/llm_gateway.py`）：

```python
_FALLBACK_ORDER = (L1, L2)                 # 级联顺序
_DEFAULT_TIMEOUTS = {L1: 3.0, L2: 8.0, L3: 20.0}
# max_retries = 1 → 每级最多 2 次
# 最坏 = L2 8s×2 + L1 3s×2 = 22s
```

**关键点**：这不只是「接入真实 LLM 后才会出的事」。即使只跑 `TemplateEngine`（L1），
最坏 **3s × 2 次 = 6s > 5000ms** —— 也就是说，**当前未配真实 LLM 的部署里，这条边已经倒挂**，
只是模板引擎实际毫秒级返回，所以从未触发。这类缺陷的特征是
**「纸面自洽、单测全绿，直到某次抖动才现形」**。

---

## 3. 登记表：把「两个数字」放到同一行

新建 `contracts/timeout-budget.yaml`（**权威副本**），19 条边：

```
budget_tiers:            # wp-bff 按「操作类型」分档，不按端点硬编码
  READ: 5000             # 纯查询
  RETRIEVE: 15000        # 检索型（下游读超时 10s）
  GENERATE: 45000        # LLM 生成型（下游级联最坏 22s）
  WRITE: 60000           # 写入型
  EXECUTE: 60000         # 工具执行（沙箱）
  SANDBOX_PROBE: 9000    # 容器探测
  MIDDLEWARE_START: 180000
  MIDDLEWARE_STOP: 120000
```

每条边记录：调用方 / 被调方 / 操作 / 档位 / **上游预算** / **下游最坏耗时** / 耗时依据 /
代码锚点（文件 + 正则 + 缩放）/ 状态。例：

```yaml
  - id: TB-06
    caller: wp-bff
    callee: nlp-service
    operation: "POST /api/nlp/brain/ask"
    tier: GENERATE
    upstream_budget_ms: 45000
    downstream_worst_case_ms: 22000
    downstream_basis: "LLM 级联最坏：L2 8s×2 次 + L1 3s×2 次（max_retries=1）"
    upstream_anchor_file: services/node/wp-bff/server.js
    upstream_anchor_pattern: "const GENERATE_TIMEOUT_MS = Number\(process\.env\.WP_BFF_GENERATE_TIMEOUT_MS \|\| (\d+)\)"
    status: ok
```

设计取舍（都在文件头部注释里写明）：

- **余量比 1.5**，不是 1.2。理由：跨服务还叠加网络往返、序列化、调度抖动；
  实测沙箱那条**曾经 1.4x 仍出过事**，1.2 太紧。
- **豁免两类边**：docker 启停 watchdog（超时是解除中间态的兜底，不是失败）、
  TCP 健康探针（超时 = 判定为 down，是**预期结果**）。豁免需显式写 `exempt_headroom: true`。
- **推导值不假装成常量**：LLM 级联的 22s 在代码里没有对应常量，故用
  `downstream_anchor_expected_ms: 3000` 声明「锚点指向的是 L1 单级 3s」，
  与表里的 22000ms **分开校验** —— 既防漂移，也不假装能自动推导。

---

## 4. 门禁：三层校验（`scripts/timeout-budget-check.py`）

```bash
python scripts/timeout-budget-check.py            # 默认：gap 仅列出，不算失败
python scripts/timeout-budget-check.py --strict   # 修复期：gap 也算失败
python scripts/timeout-budget-check.py --list     # 打印全表摘要（预算/余量/状态）
```

| 层 | 校验内容 | 防的是什么 |
|---|---|---|
| ① 语义 | 上游预算 ≥ 下游最坏 × 1.5 | 预算不足（倒挂） |
| ② 锚点 | 按登记表声明的正则从**真实代码**提取实值，与表中数字比对 | 「表里写 A、代码是 B」的漂移 |
| ③ 魔法数字 | 扫调用点，禁止就地写数字 | **②的盲区补丁**（见下） |

**第 ③ 层的由来（本轮踩到的真实盲区）**：②锚的是**常量定义**，
而缺陷可以藏在**调用点**。实测：`POST /api/body/knowledge` 调用点写 `timeoutMs: 30000`
（= 下游最坏，余量 0），而表里记的是常量 60000 —— **门禁全绿，缺陷仍在**。
一个锚点写得再准，也管不住「有人绕过常量直接在调用点写数字」。

配套约定：
- 仓内**不引入 PyYAML 依赖**（与 `contract-check.py` 同一路线），脚本自带行式解析器；
  解析约定（条目保持扁平、引号内反斜杠按字面保留）写在脚本 docstring 与登记表头部。
- 一条坏正则**不会让门禁崩溃**，而是报 FAIL ——
  避免重演「巡检脚本恰在故障时失效」（上一轮 healthcheck 的老毛病）。

---

## 5. 修复：生成型链路改用命名档位

`services/node/wp-bff/server.js` 新增档位常量，并逐处应用：

```javascript
const READ_TIMEOUT_MS     = Number(process.env.WP_BFF_READ_TIMEOUT_MS     || 5000)
const RETRIEVE_TIMEOUT_MS = Number(process.env.WP_BFF_RETRIEVE_TIMEOUT_MS || 15000)
const GENERATE_TIMEOUT_MS = Number(process.env.WP_BFF_GENERATE_TIMEOUT_MS || 45000)
const WRITE_TIMEOUT_MS    = Number(process.env.WP_BFF_WRITE_TIMEOUT_MS    || 60000)
const EXECUTE_TIMEOUT_MS  = Number(process.env.WP_BFF_EXECUTE_TIMEOUT_MS  || 60000)
```

| 调用点 | 落地前 | 落地后 | 余量变化 |
|---|---:|---:|---|
| `POST /nlp/brain/ask` | 5000（默认） | `GENERATE` 45000 | 0.23x → **2.05x** |
| `POST /session/{id}/ask` | 5000（默认） | `GENERATE` 45000 | 0.50x → **4.50x** |
| `POST /brain/memory/ingest` | 5000（默认） | `GENERATE` 45000 | 0.23x → **2.05x** |
| `GET /knowledge/stats`（第 2 处） | 5000（默认） | `RETRIEVE` 15000 | 0.50x → **1.50x** |
| `POST /body/knowledge`（入库） | **30000 写死** | `WRITE` 60000 | 1.00x → **2.00x** |

**同时刻意不做的两件事**：

1. **不把读型端点一起拉长**。把 `session/stats` 这类 Redis 单次往返也提到 45s，
   会让真实故障等 45s 才暴露降级 —— 那是把「快速失败并降级」换成「长时间等待后仍降级」。
   用例 `读型端点必须保持短预算` 显式守住这条。
2. **不给所有档位用同一个值**。用例 `生成档与读型档必须真的分开` 断言
   `gen.timeoutMs > read.timeoutMs` —— 防的是「图省事把两档合并」。

---

## 6. 验证

### 6.1 新增回归用例（wp-bff，+6 条 → **87 / 0 失败**）

| 用例 | 守的不变量 |
|---|---|
| 生成型链路（brain/ask）使用生成档预算 | 必须**显式**声明且 = GENERATE（不得依赖读型默认值） |
| 生成型链路（session/{id}/ask）同样使用生成档 | 会话问答内部转调 LLM，同样受约束 |
| 生成型链路（memory/ingest）使用生成档 | 记忆抽取走 LLM |
| 检索型链路（body retrieve）预算覆盖下游读超时 10s | RETRIEVE 档 ≥ 下游读超时 |
| 读型端点必须保持短预算 | **防一刀切拉长**（`explicit === false` 且 ≤ READ） |
| 生成档与读型档必须真的分开 | 两档被合并立即失败 |

与门禁脚本**互补**，缺任一方都有盲区：

- 脚本守「**登记表数字 ↔ 代码常量**」→ 防的是「表里写 A、代码是 B」；
- 用例守「**代码行为 ↔ 操作类型**」→ 防的是「改了行为却忘了常量 / 分档被合并」。

用例断言一律用 `TIMEOUT_TIERS` 常量与下限/上限，**不用精确字面值** ——
运维通过环境变量调档不会误伤用例。为此把档位以 `TIMEOUT_TIERS` 导出，
测试替身**镜像生产默认值**（否则读型调用录到 `undefined`，用例就只能断言「有没有传」，
而断言不了「档位对不对」—— 这正是首轮 5 条用例失败的原因）。

### 6.2 反向验证（门禁必须真能抓违规，否则只是绿着好看）

| 实验 | 构造 | 结果 |
|---|---|---|
| ① 预算不足 | 复制登记表，把 `GENERATE: 45000` 改成 `1000` | **fail=3、exit=1** ✅ |
| ② 锚点漂移 | 表里把 `READ` 写 7000、代码仍是 5000 | **报「上游锚点漂移」** ✅ |
| ③ 魔法数字 | 把调用点的 `WRITE_TIMEOUT_MS` 换回 `30000` | **FAIL MAGIC server.js:1321、exit=1** ✅ |
| ④ strict 模式 | `--strict` | gap 计入失败 ✅ |

实验 ③ 同时暴露了首版门禁的**盲区**（只锚常量），故补第 ③ 层扫描 —— 这是本轮
「先验证门禁有效性、才发现门禁本身不够」的顺序价值。

### 6.3 门禁现状

```text
── 超时预算门禁：contracts\timeout-budget.yaml ──
  GAP  TB-11 — 预算不足：上游 10000ms < 下游最坏 22000ms x 1.5 = 33000ms（实际余量 0.45x）
  GAP  TB-12 — 预算不足：上游 10000ms < 下游最坏 10000ms x 1.5 = 15000ms（实际余量 1.00x）
  GAP  TB-13 — 预算不足：上游 30000ms < 下游最坏 30000ms x 1.5 = 45000ms（实际余量 1.00x）
  GAP  TB-15 — 预算不足：上游 20000ms < 下游最坏 20000ms x 1.5 = 30000ms（实际余量 1.00x）
  GAP  TB-16 — 预算不足：上游 5000ms < 下游最坏 10000ms x 1.5 = 15000ms（实际余量 0.50x）

合计: ok=14 · gap=5 · fail=0
```

**5 条 gap 是「已声明、未修复」**，其中 4 条不在 wp-bff 内（属 session-manager /
body-service / sense-service / nlp-service）。门禁**单独列出但不判失败** ——
目的是让它们可见、可追踪，而不是靠「门禁绿了」把问题掩盖成不存在。修复期用 `--strict`。

### 6.4 接入启动前自检

`scripts/start.sh` 新增 `gate` 模式与前置步骤（`[0/6]`）：

```bash
./scripts/start.sh gate        # 只跑门禁
SKIP_BUDGET_GATE=1 ./scripts/start.sh all   # 显式跳过（会打印"请勿把本次结果当作可信基线"）
```

失败**不静默忽略、也不硬拦**：需要显式环境变量才能跳过，跳过时打印免责提示。

---

## 7. 遗留与建议

| # | 项 | 状态 |
|---|---|---|
| 1 | **GAP-01**：session-manager 问答读超时 10s < LLM 级联 22s | 登记未修（跨服务，属 P6 编排预算命题） |
| 2 | **GAP-02**：session→body 检索上游 = 下游读超时（余量 0） | 登记未修 |
| 3 | **GAP-03 / GAP-04**：body→nlp 嵌入、sense→nlp OCR **下游无独立超时** | 登记未修（需 Python 侧加超时，否则「覆盖」无法证明） |
| 4 | **GAP-05**：nlp→body 检索 5s < 下游 10s | 登记未修（跨语言） |
| 5 | **REC-01**：LLM 级联缺总预算上限（deadline propagation） | 建议项，P6 决策 |
| 6 | **REC-03**：单服务内的线程池/队列等待同类错配未扫 | 建议项 |

**根本方向（REC-01）**：当前 `max_retries=1` 且逐级放宽超时（L1 3s → L2 8s → L3 20s），
最坏耗时是「各级超时 × 重试次数」的乘积，**随层级与重试数增长而膨胀**。
正确做法是给一次 `generate()` 调用一个**总 deadline**，级联只在该 deadline 内调度 ——
这样上游预算才能可靠覆盖下游。否则只能靠不断调大上游超时，
而那会把「快速失败并降级」变成「长时间等待后仍降级」，损害体验且掩盖故障。

---

## 8. 认知沉淀

**同一形状的缺陷，这一天出现了第四次**：

| # | 场景 | 表现 |
|---|---|---|
| 1 | 沙箱探测慢 | 5.7s 被写成「沙箱不可用」 |
| 2 | BFF 四种失败 | 塌缩成同一个 `null` |
| 3 | healthcheck | 恰在「发现故障」这个动作上失败 |
| 4 | **知识入库调用点** | 写死 30000 = 下游最坏，**门禁看不见** |

共同点：**下游只拿到一个空值 / 一个裸数字，就丢失了全部可行动信息**。
本轮的应对不是「再小心一点」，而是**把约束搬到机器能检查的地方**：
登记表让两个数字必须同框，门禁让不一致必须变红。

> 附带一条：门禁自身也会犯同样的错 —— 首版只锚常量定义，
> 对调用点的魔法数字**视而不见**。所以「加门禁」之后必须**反向验证门禁有效**，
> 否则只是多了一个绿着好看的东西。

---

## 9. 附带收获：本轮自己踩了第五次同形状的坑

本轮写文档时给 `docs/异常流程归纳.md` 追加异常条目，取了 `D-17`~`D-21` ——
而这三个号（以及 17/18）在**另一个章节早已被占用**。结果：

```text
文档照常渲染 ✅   门禁全绿 ✅   撞号静默存在 ❌
（§3.2 里是 D-17~D-21，§3.4 里也是 D-17~D-22）
```

发现方式：人工 `grep -o '^| D-[0-9]*' | sort | uniq -d`。
**又是"约束写在文档里、没人守"的同一形状**（第 5 次）。

### 9.1 修正

编号改为接续全局最大号：新增 5 条 → `D-31`~`D-35`；
新增的「超时预算类」独立成 §3.3，其后章节顺延（原 3.3→3.4、3.4→3.5、3.5→3.6、3.6→3.7）；
交叉引用 `docs/功能开发流程.md` 的 `§3.1 D-17` → `§3.2 D-31`。
并在 §3 开头**写明编号约定**（按新增时间递增，与章节顺序不同步；唯一硬约束是全局唯一）。

### 9.2 补门禁：`scripts/doc-consistency-check.py`（新增）

同一个坑不能靠"下次小心"。三类文档约束上机器：

| 检查 | 防的是什么 |
|---|---|
| ① 异常编号唯一 | `D-xx` 撞号（本轮实例） |
| ② 章节号唯一 | `### 3.3` 出现两次（插入新章节忘记顺延） |
| ③ md/html 孪生同步 | 改了 `.md` 忘了重生 `.html`（页面停在旧口径） |

反向验证（同样是"先证明门禁有效"）：

| 实验 | 构造 | 结果 |
|---|---|---|
| ① 植入撞号 | 把 `D-35` 改成 `D-31` | **FAIL 异常编号撞号 D-31 出现 2 次、exit=1** ✅ |
| ② 植入重复章节 | 把 `### 3.7` 改成 `### 3.3` | **FAIL 章节 §3.3 出现 2 次、exit=1** ✅ |
| ③ 孪生过期 | 只改 md 不重生 html | **FAIL 孪生过期** ✅ |

已接入 `./scripts/start.sh gate`（与超时预算门禁同批，`--no-twin` 跳过孪生以免 CI 误报）。

> **门禁自身的盲区也顺手修了一个**：首版章节检查只取小数点后的位（`\d+\.(\d+)`），
> 会把 `§1.1 / §2.1 / §3.1` 全判成同一个「1」→ 一上来就报 7 条假失败。
> 改为取完整号（`\d+(?:\.\d+)+`）。**"加门禁"之后必须跑一次看它报的是不是真问题** —— 又一次印证。

---

## 10. 后续（同日 REC-01 轮已处理）

> 本节为**事后回填**，用于避免读者把 §7 的遗留表当成现状。原文数字保留不改，历史即历史。

同日稍后的 REC-01 轮实现了 deadline propagation（`nlp-service/app/budget.py`、
`llm_gateway.generate(deadline_ms=…)`、`session-manager/RequestBudget`），并**按本表第 23–27 行的约定**
同步了登记表：

| §7 中的项 | 现状（2026-09-19 REC-01 轮后） |
|---|---|
| GAP-02 / GAP-03 / GAP-04 | ✅ **已闭合**（上游具名档 15s；Python `/embed`、`/ocr` 各接入 12s 预算） |
| GAP-01 / GAP-05 | 数值随代码上移，余量比 0.45x→0.91x、0.50x→0.80x，**仍未达标** |
| REC-01 | ✅ 主体已落地（见上）；`BRAIN_TOTAL_BUDGET_MS` 25s 尚大于级联推导最坏 22s，上限未真正收紧 |
| — | ⚠️ **新增 GAP-06**：session ask 总预算 35s 把 wp-bff `GENERATE` 45s 的余量摊薄到 1.29x |
| §3 的锚点机制 | ⚠️ 发现**子串命中**缺陷：`READ_TIMEOUT = …` 会命中 `DEFAULT_READ_TIMEOUT`，已改为具名档锚点 |

门禁状态：**`ok=14 · gap=5 · fail=0`（本轮）→ `ok=16 · gap=3 · fail=0`（REC-01 轮后）**。
详见 `docs/优化日志/2026-09-19-REC-01-LLM级联总预算与跨服务预算同步.md`。
