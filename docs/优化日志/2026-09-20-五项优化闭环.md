# 五项优化闭环（2026-09-20）

> **范围**：文档真相源、生产路由实测、Prometheus/告警、Playwright E2E、DEBT-006/009
> **结论**：五项优化均完成；未启动 Phase 6 业务开发。

## 1. 文档真相源扩展

`scripts/doc-truth-check.py` 新增检查：

- OpenAPI implemented / planned 与实际契约一致；
- `DEBT-006` / `DEBT-009` 关闭证据；
- “真实 LLM 引擎未接入”“31 个 planned”“92a46c1”等废弃口径；
- Prometheus、Alertmanager、Playwright 资产存在。

## 2. 生产路由实测

- gateway-service 路由：`/api/wp/** -> WP_BFF_URI`。
- 新增 `WpBffRouteIntegrationTest`：
  - 启动本地 HTTP 假 wp-bff；
  - 通过真实 Spring Cloud Gateway 转发；
  - 验证 JWT tenant 注入到 `X-Tenant-Id`；
  - 验证 `/actuator/prometheus` 返回 JVM 指标。
- 结果：**3/3 passed**。

## 3. Prometheus 与告警

- 所有 Java 服务加入 `micrometer-registry-prometheus`，暴露 `/actuator/prometheus`。
- nlp-service 增加 `prometheus-client` 与 `/metrics`。
- wp-bff 增加 `/metrics`。
- `infra/prometheus/` 新增抓取配置、告警规则与 Alertmanager 配置。
- Compose 新增 Prometheus / Alertmanager。
- Alertmanager 宿主机端口使用 **19093**，避免与 sense-service gRPC 9093 冲突。
- `docker compose config --quiet` 通过。
- 容器实测：Prometheus `/-/ready` = Ready，Alertmanager `/-/ready` = OK，3 条规则已加载。

## 4. Playwright E2E

- 新增 `@playwright/test`、Chromium 运行时、`playwright.config.ts` 和 2 条冒烟用例。
- `.env.test` 固定 Mock 数据源，E2E 不依赖后端。
- 发现并修复 `getServices()` 在 Mock 模式下仍请求 BFF 的缺口。
- 结果：**2/2 passed**。

## 5. 红色技术债关闭

### DEBT-006

新增五渠道测试：

- 五渠道全部注册；
- 健康快照覆盖五渠道；
- 四渠道不可用时降级到 TOUCH；
- 不可用渠道不得被调用。

结果：sense-service **54/54 passed**。

### DEBT-009

新增真实链路测试：

- NATS 请求/回应经 `BusProxy`；
- Kafka 发布/消费真实 broker；
- CI 增加 Kafka 服务与 `SESSION_BUS_IT=true`。

本地真实容器结果：**2/2 passed**。

wp-bff 全量回归：**114/114 passed**（新增 `/metrics` 用例）。

## 6. 复现命令

```bash
# gateway 生产路由与 Prometheus
cd services/java
mvn -B -ntp -pl gateway-service -am test

# 五渠道
mvn -B -ntp -pl sense-service -am test

# NATS/Kafka
SESSION_BUS_IT=true NATS_URL=nats://127.0.0.1:4222 KAFKA_BOOTSTRAP=127.0.0.1:9092 \
  mvn -B -ntp -f session-manager/pom.xml -Dtest=SessionBusLinkTest test

# 前端
cd web/work-platform
npm run test:report
npm run typecheck
npm run build
npm run test:e2e
```
