# 优化日志 · 2026-09-18 全局文档对齐与基线数字回填

> **轮次**：异常流程轮之后的**文档对齐 API 复审**（非新阶段、非新功能）
> **触发**：用户提问「当前设计文档空间配置暂不做远程提交，仅针对开发项目目录内的内容进行远程更新，
> 开发项目内的文档相关是否已经对齐到最新开发阶段内容」
> **代码基线**：`d4a114b`（本轮**不改任何代码**，只修文档与归档）
> **依据**：实测输出 —— Java 130 / Python 62 / wp-bff 34 / 契约 0 FAIL / 覆盖 106/106
> **结论**：**没有对齐**。三份高层入口文档滞后于最新阶段，其中根 `README.md`
> **完全停留在 Phase 2 口径**；全部已回填，并新登记一条真缺口（待办 21）

---

## 1. 约束确认

用户明确「设计文档空间暂不做远程提交，仅针对开发项目目录内的内容进行远程更新」。
先核实边界：

| 目录 | 是否 git 仓库 | 处置 |
|---|---|---|
| `E:\AI\核心知识`（设计文档空间） | **否**（无 `.git`） | 本就没有远端可推，约束天然成立；本轮**未做任何提交** |
| `E:\AI\ai-agent`（开发项目） | 是（`github` SSH + `origin` 只读镜像） | 本轮改动全部在这里，推送三分支 |

---

## 2. 本轮目标

回答一个可验证的问题：

> `ai-agent` 仓库内的文档，是否已反映**最新开发阶段**的内容 ——
> 包括 Phase 3 落地之后的四轮改动（审核补全 / Mock-API 对齐 / 文档口径同步 / 异常流程归纳）？

判定标准同上几轮：**每个数字必须与实测命令输出一致，每处描述必须能在源码里找到落点。**

---

## 3. 先看谁的"已经同步"是可信的

### 3.1 先取实测真值（不凭记忆）

```
Java    scripts/mvn-dev.sh test .. 130 passed / 0 failed
                                  (gateway 2 · session-manager 15 · sense-service 53 · body-service 60)
Python  pytest -q ................ 62 passed
wp-bff  node --test .............. 34 pass / 0 fail
契约    contract-check ........... 0 FAIL (implemented 8 路径 / 9 方法 · planned 31)
覆盖    java-doc-coverage ........ 106/106
```

**取数踩到一个坑**：从 `target/surefire-reports/*.txt` 汇总得到 **131**，
与 Maven 日志里 body-service 打印的 `Tests run: 60` 对不上。
原因是 `body-service/target/surefire-reports/com.agent.body.store.BodyStoreTest.txt`
停留在 **2026-09-13** —— 对应测试类早已移除，报告文件却没被清理。
**收口一律以 Maven 日志为准；从报告汇总时必须过滤时间戳。**（已写入上一轮日志 §9）

### 3.2 门禁全绿，但文档没跟上

门禁运行结果：`java-doc-coverage` **106/106**、`contract-check` **0 FAIL**、HTML `STALE` 为空。

**然后人工核对数字，仍然查出 4 份文档滞后** —— 又一次验证：
> **门禁只守「存在性」，不守「准确性」。**

---

## 4. 处置明细

### 4.1 根 `README.md`（**最严重，完全停在 Phase 2 口径**）

| 位置 | 原内容 | 修正后 |
|---|---|---|
| 头部基线 | "Phase 0/1/2 开发完成…（**61 Java + 19 Python**）" | "Phase 0/1/2/**Phase 3（躯体期）** 已交付…（**130 Java + 62 Python + 34 wp-bff**，契约 0 FAIL，覆盖 106/106）"，并写明下一阶段为 Phase 4 |
| 架构描述 | 只有 Phase 2 感官回路 | 增补 **Phase 3 躯体回路**（解析→分块→嵌入→三层存储→语义检索→重排→RAG→躯体视图） |
| 技术栈表 | "服务端缓存层**待 Phase 3** 引入 TanStack Query" | 改为"待**后续阶段**" |
| 目录结构 | `body-service` 注为"本地知识检索 MVP"；`wp-bff` 只提到最小 Ops；`docs/demo` 写 Phase0/1/2 | 分别改为躯体知识管道、含知识统计/写入/检索代理、Phase0/1/2/**3** |
| §7 验证命令 | "61 tests"、"14 passed"、"15 passed / 4 passed" | 全部替换为实测：130（含模块分解）/ 34 / 62，并补 `java-doc-coverage` |
| 里程碑清单 | `- [ ] Phase 3：Qdrant/pgvector 正式知识库`（**已交付却仍勾选未完成**） | 改为已勾选的两行，且**去掉 `pgvector`** —— 实现是 PostgreSQL 关系元数据 + Qdrant 向量（§4.6 已收口） |

> 「Phase 3 已完成但清单仍打空勾」是最容易被一眼看穿的错误；它的成因是
> **Phase 3 交付时改了 `PROGRESS.md` 与 `docs/` 下的各级概览，却漏了仓库根 README**。

### 4.2 `PROGRESS.md`

| 位置 | 修正 |
|---|---|
| `Current milestone` | 基线 114/52/27/103 → **130/62/34/106**；补注 Java 模块分解；链接到新增章节 |
| **新增** `## Phase 3 post-closure hardening (2026-09-18)` | 补上此前**完全未记录**的三轮：①Mock/API 对齐（`bce7921`）②文档口径同步（`8ba5544`）③异常流程归纳与错误信封统一（`6616616`+`d4a114b`），各含改动要点与 Verification 块 |

> Phase 3 段原本只记到"requirement re-audit"，之后三轮在项目的最高进度文档里**查无此人**。

### 4.3 `docs/demo/Phase3-DEMO.md`

演示脚本是**要照着跑的**，其"预期（2026-09-18 实测）"表必须与实际输出一致：

| 位置 | 原值 | 修正后 |
|---|---|---|
| Java 模块表 | session 10 · sense 48 · body 54 · 合计 114 | session **15** · sense **53** · body **60** · 合计 **130**（分项之和已复算 = 130 ✓） |
| body 测试类清单 | 9 个类 | **10 个类**，补 `GlobalExceptionHandlerTest`(6)（分项和已复算 = 60 ✓） |
| Python 用例表 | 5 个文件 52 项 | **6 个文件 62 项**，补 `test_http_errors.py`(10)（分项和已复算 = 62 ✓） |
| Python 预期输出 | `52 passed in 0.41s` | `62 passed, 2 warnings in 0.90s`（实跑原文） |
| wp-bff 预期输出 | `# tests 27` | `# tests 34` |
| 契约注释 | "实现端点 9 ↔ BFF 9" | "implemented **8 路径** ↔ BFF 8 路径（**9 个方法**）"（路径数与法数此前被混写） |
| 覆盖注释 | body 36/36 · 103/103 | body **37/37** · **106/106** |
| 结论与验收清单 | 114 / 29 / 52 | 130 / 34 / 62 |

### 4.4 本轮缺失的日志

上一轮（异常流程轮）改变了 5 个源码/契约文件 + 11 个文档文件，
但 `docs/优化日志/` 里**没有留痕** —— 而同日的文档口径同步轮是留了的。
已补 `docs/优化日志/2026-09-18-异常流程归纳与全平台错误信封统一.md`（+ HTML 孪生）。

---

## 5. 新发现的一条真缺口（待办 21，未擅自改动）

核对"统一错误信封"是否真的闭环时，向客户端侧查了一步：

**前端没有消费它。** `web/work-platform/src/api/provider.ts` 的 `catch` 只取
`(error as Error)?.message`（axios 的 `Request failed with status code 404` 之类），
全仓 `grep -rn "AGENT_"` **零命中**，也没有任何地方解析 `error.response.data` 的
`code` / `message` / `details`。

含义：`docs/异常流程归纳.md` §2.1 写的「客户端只需一套解析逻辑」目前是**设计目标而非现状** ——
后端统一了形状，前端还停留在"按 HTTP 状态码文案猜"。

按既有规矩，**这是新发现而非本轮该擅自改的东西**：改前端需要连带 `vue-tsc` + `vite build` 验证，
且属于下轮加工范围。已登记为 **待办 21**（建议抽 `extractApiError(error)`，
并据此区分 404(planned) / 4xx(用错) / 5xx(故障) 三种降级文案）。

---

## 6. 历史留痕（**刻意不改**）

以下位置保留了旧数字，因为它们是**当时那一次运行的事实**，改掉等于篡改历史：

- `docs/项目进度日志报告/Phase3-*.md` 三件套中的 114 / 103/103 / 195/195 —— 阶段交付快照
- `docs/优化日志/2026-09-18-*.md` 中各自轮次的当时数字
- `PROGRESS.md` `### Verification (2026-09-18)` 块 —— Phase 3 交付时的验收实测

它们与新值是**链式递进**关系（`Phase 3 post-closure hardening` 章节的 Verification 块给出最新值），
而不是互相矛盾。

---

## 7. 验证与提交

```
java-doc-coverage ... 106/106        contract-check ... 0 FAIL
STALE 检查 .......... 空              HTML 孪生 ........ 已全部重生成
```

改动清单：

| 文件 | 类型 |
|---|---|
| `README.md` | 修改（8 处） |
| `PROGRESS.md` | 修改（新增一个章节 + 基线） |
| `docs/demo/Phase3-DEMO.md`（+ HTML） | 修改（14 处） |
| `docs/技术债台账.md`（+ HTML） | 修改（新增待办 21） |
| `docs/优化日志/2026-09-18-异常流程归纳与全平台错误信封统一.md`（+ HTML） | **新增** |
| `docs/优化日志/2026-09-18-全局文档对齐与基线数字回填.md`（+ HTML） | **新增（本文件）** |

---

## 8. 关键教训

**"文档已更新"是一个需要分层回答的命题。**

同一个仓库里，三个阶段的分级文档可以同时处于三种状态：

| 层 | 例子 | 本轮状态 |
|---|---|---|
| 阶段内模块文档 | `docs/java-services/04~07` | ✅ 上轮刚回填，准确 |
| 项目级概览 | `docs/项目进度总览.md` | ✅ 上轮已改，准确 |
| **仓库最高层入口** | `README.md` / `PROGRESS.md` | ❌ **本轮才补齐** |

教训在于：沿着「模块文档 → 项目概览 → 仓库根 README」这条汇报链一路往下补细节时，
**最顶层、最容易被人第一眼看到的入口反而最容易被跳过** ——
因为它"看起来不需要改"（没人去看它是否还写着三个月前的结论）。

推论：**每次改动收尾时，先问一句"仓库 README 还成立吗"**，再问子目录文档。
