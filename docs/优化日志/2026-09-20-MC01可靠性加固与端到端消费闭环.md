# MC-01 可靠性加固与端到端消费闭环（2026-09-20）

> **范围**：MC-01 协作总线独立切片（Phase 6 前置） · **性质**：可靠性缺陷修复 + CI 门禁补强  
> **结论**：R-MC01-01~04 从“基础能力完成”推进到“真实 durable consumer 可恢复、可重投、可死信、可 CI 验证”

---

## 1. 本轮修复的问题

| 问题 | 修复前风险 | 本轮处置 |
|---|---|---|
| 真实 JetStream 消费循环缺失 | `IdempotentConsumer` 只是 helper，消息不会被 ack/nak | 新增每域 durable pull consumer + 虚拟线程拉取循环 |
| 失败重投语义冲突 | 先标 seen 且失败不回滚，重投被误判重复，无法进 DLQ | 改为 `pending / processed / failed / dlq` 状态机 |
| 关键集成测试在 CI 跳过 | 离线恢复、幂等、DLQ 只在本地手工验证 | 新增 `collab-integration` job（PostgreSQL + NATS） |
| 心跳限流只打标记 | 高频心跳仍每次写 PG，防风暴未真正生效 | 5 秒窗口合并，pending 查询可见，窗口 flush 落库 |
| 域创建半成品 | PG 已写 active、stream/consumer 创建失败时留下孤儿域 | 引入 `creating → active / failed`，失败补偿 + 启动对账 |
| 旧 push consumer 无法恢复 | 重启时配置漂移导致无法绑定 | 安全迁移为 durable pull；有未处理消息时拒绝自动迁移并显式暴露 |

## 2. 关键实现

- **发布协议**：NATS headers 携带 `X-Tenant-Id / X-Request-Id / X-Member-Id / X-Message-Type`，JetStream message-id 使用 request_id。
- **消费状态机**：首次 `CLAIMED`；失败 `RETRY`；成功 `processed`；达到 `retry-max + 1` 后自动发布 `deadletter` 并 ack 原消息。
- **消费者恢复**：durable pull consumer 从未 ack 位置继续；每域后台拉取，不用固定 deliver subject，重启可重新绑定。
- **心跳窗口**：窗口内只覆盖 pending 中最后值；查询合并 pending；默认每 5 秒最多 flush 一次。
- **健康可见**：`GET /api/collab/health` 增加 `consumer` 与 `heartbeat` 运行态。

## 3. 验证证据

| 项目 | 实测结果 |
|---|---|
| collab-bus 真实集成 | `COLLAB_IT=true`：**38/38 passed** |
| 消费者离线恢复 | 停止 consumer → 发布 50 条 → 恢复后 50/50 进入 `processed` |
| 持续失败收口 | heartbeat 非法 JSON 重投至上限后进入 `dlq`，原消息 ack |
| 全量 Java | **289 测试 / 0 失败**（含 collab-bus 38） |
| R-MC01-04 P99 | 10000 次，**P99 = 4.231ms < 10ms** |
| 契约 | collab-bus 5 端点 + 5 主题模板；work-platform 63 端点；均 0 FAIL |
| wp-bff | **106 passed / 0 failed** |
| 文档一致性 | MD/HTML 孪生过期 0 组 |
| Ruff | All checks passed |

## 4. 明确保留的后续项

- **R-MC01-05**：工作平台协作视图从固定 `DOM-2048` Mock 切换到真实域名选择 + 聚合数据；当前不得以 Mock 冒充完成。
- **多实例 consumer 规模验证**：当前是“每域一个虚拟线程拉取循环”，域数量很大时需要验证线程/连接规模，必要时改为共享调度器或批量拉取。
- **业务处理器**：dispatch / result / negotiate 的传输已消费，但 Phase 6 业务处理器仍待注册，日志不会把它们描述为业务已完成。
- **Prometheus 指标**：consumer lag、重投次数、DLQ 数量、心跳 coalesce 率目前通过 health/日志可见，尚未接 Prometheus。

## 5. 复现命令

```bash
# 真实集成（需要 PG + NATS）
cd services/java
COLLAB_IT=true mvn -B -ntp -pl collab-bus -am test

# P99 门禁
python scripts/collab-bus-check.py --require-jetstream

# 契约
python scripts/contract-check.py --collab --collab-openapi contracts/collab-bus-openapi.yaml
python scripts/contract-check.py --work-platform --openapi contracts/work-platform-bff-openapi.yaml
```