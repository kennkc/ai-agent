# Agent-Lifeform 项目进度总览

> **口径时间**：2026-09-21（五项最终优化收口） · **代码基线**：`codex/main`（与 `dev`、`workbuddy/main` 同步）
> **数据来源**：仓库 `PROGRESS.md`、`docs/项目进度日志报告/`、`docs/优化日志/`、各服务源码实测统计
> **诚实声明**：凡未经实测的数字均标注口径时间；历史归档中的旧口径（如 54/73/61/106/162 项测试）**不回改**，以本文件为准。

---

## 一、当前状态一句话

**Phase 0 / 1 / 2 / 3 / 4 / 5 已交付并验收**，项目当前处于 **Phase 5 四肢期收口后**（下一步 Phase 6 小脑期）。
**Phase 5 四肢期**新增独立服务 **`tool-executor`（8084 / gRPC 9095）**：工具注册表（R5-01）+ 执行引擎
（R5-02，白名单 → 参数 Schema → 敏感参数 → 熔断 → 带超时执行 → 结果标准化 → 审计）+ 沙箱隔离
（R5-03/R5-05，Docker 真隔离 + 受限子进程降级）+ 安全三闸（R5-07）+ 全量审计（R5-08，PG 真相源 +
Kafka 旁路）+ **IN-06 工具契约治理**（semver + schema 指纹 + 变更影响分析 + 契约基线门禁），
并在工作平台落地**执行视图**（工具卡片 / 试运行 / 沙箱状态灯 / 审计流水 / 熔断徽标 / **影响面抽屉**）。
> **2026-09-20 MC-01 协作总线独立切片（Phase 6 前置）已落地并完成可靠性加固**：
> `services/java/collab-bus` 完成 R-MC01-01~04 —— 域生命周期 `creating/active/failed`、
> JetStream **durable pull consumer**、手工 ack/nak、有限重投与自动 DLQ、
> 心跳 5 秒窗口合并/stale、P99 < 10ms 门禁；契约登记在 `contracts/collab-bus-openapi.yaml`。
> 实测 38 项测试（29 执行 / 9 个条件集成用例跳过，0 失败）通过，P99 = 4.231ms；GitHub Actions 已加入
> `collab-integration` 与 `collab-bus-check` 两个 job。R-MC01-05 已闭合：wp-bff 代理协作域列表/
> 详情，工作平台通过 `VITE_COLLAB_DOMAIN_ID` 或真实列表首项选择协作域，上游不可用时显式降级。


> **2026-09-20 工程化补强**：前端新增 Vitest 功能测试基线（27/27）并归档 JUnit/覆盖率报告；
> wp-bff 新增应用服务监控/受控启停、协作域代理、Alertmanager 接收与 Prometheus `/metrics`（118/118 测试）；OpenRouter 固定上游模型
> 通过非降级验证，free router 仅保留 smoke 用途；`versions.lock.json` 与部署版本基线文档已落地。
> 后台服务控制页、总览真实协作域选择与降级展示已加入工作平台；R-MC01-05 已闭合。
> Java 服务已接入 `/actuator/prometheus`，nlp-service / wp-bff 已提供 `/metrics`，基础设施新增 Prometheus + Alertmanager。
> Playwright 已覆盖 Mock UI 与真实 wp-bff/API E2E；DEBT-006、DEBT-009、DEBT-021 已关闭。
> MC-01 单实例 25/100/500/1000 域规模验证通过；Phase 6 必需端点优先级已冻结。
> 生产路由已补 gateway-service `/api/wp/** -> WP_BFF_URI` 与 Nginx 模板；模型配置新增生产 fail-closed 开关（`MODEL_CONFIG_REQUIRE_PERSISTENCE=true`），
> 生产禁止本地密钥文件并以 503 拒绝内存伪成功。前端覆盖率已设置 25%/20%/65% 硬门禁；新增 `scripts/doc-truth-check.py`
> 校验契约计数、分支、文档和当前测试产物。Phase 6 完整开发仍冻结；Blocking 端点已完成最小闭环。
>
> **2026-09-21 五项最终优化**：① collab-bus PG 租约支持多实例单 owner 与故障接管，并改为租约不可用 fail-closed；② 技术债详细章节状态一致性增加机器门禁；③ 完整服务栈 8/8 Prometheus target 实测 `up`；④ Alertmanager → wp-bff → JSONL 真实本地投递链通过唯一 `run_id` 验证，生产外部通知仍要求替换 webhook；⑤ Phase 6 Blocking 端点完成真实派生 / 显式降级，审批写路径 503 且无副作用。证据见 `docs/test-reports/collab/2026-09-21/`。
> **2026-09-21 前台菜单优化**：左侧菜单由 21 个平铺入口调整为 4 个常用入口 + 四个可折叠分组；新增“可用 / 部分 / 规划”成熟度徽标，Phase 6/7/8 未接入能力收进“规划与演示”；侧栏 Agent 在线改接真实 `/api/wp/agents/online`，不可用时显式说明而非继续展示固定 Mock。
> **2026-09-21 多模型运行态去 Mock**：新增 `GET /api/wp/models/runtime`，聚合真实模型配置、连通性探测、Token 用量和 Prometheus LLM 指标；原“模型池运行态 / 路由策略”演示卡片已替换为真实运行态，成本、质量、配额、队列无数据源时显式标未接入；真实验收归档见 `docs/test-reports/model/2026-09-21/runtime-overview.json`。
> **2026-09-21 指标监控接入**：新增 `GET /api/wp/metrics/overview` 与观测区“指标监控”页面，固定查询 Prometheus，展示 target、HTTP、JVM / Micrometer、GC、HikariCP、LLM / Collab / Tool 业务指标和活动告警；前端不接触任意 PromQL，真实响应归档见 `docs/test-reports/metrics/2026-09-21/overview.json`。
> **2026-09-19 Phase 5 复审结论**：**未触碰伪实现红线**。复审时唯一需留意的是**沙箱降级被当作"沙箱验收通过"
> 叙述的风险** —— 当时实测 `docker_available=false`、`active_backend=process-restricted`、`degraded=true`。
> **该缺口已于同日收口轮闭合**：构建 `agent-sandbox:latest` 后重启服务，`GET /api/tool/sandbox` 回到
> `active_backend=docker / isolated=true / degraded=false`，逃逸样本取得**内核级证据**
> （出网 `Errno -3`、写盘 `Errno 30`）。详见《Phase5-测试验收报告》§5.2。
> 逐条对账与缺口清单见 [`docs/优化日志/2026-09-19-Phase5四肢期进度复审与缺口清单.md`](./优化日志/2026-09-19-Phase5四肢期进度复审与缺口清单.md)。


> **2026-09-18 追加一轮需求审核与补全**：逐条对账需求文档、扫描生产代码伪实现、核对 DEBT 记账、
> 核查全链路可达性。结论：**无伪代码实现**，但发现 4 项阶段范围内缺失（工作平台入库链路断裂、
> 格式解析步缺失、IN-05 参数未预留、对账能力缺失）—— **已全部补全**；另发现 **IN-01 / WB-03 /
> WB-05 / MC-01 四项服务级需求从未进入本阶段交付范围**，已如实登记待规划侧决策。
> 记录见 [`docs/优化日志/2026-09-18-Phase3需求审核与补全.md`](./优化日志/2026-09-18-Phase3需求审核与补全.md)。

阶段间还经历过一段 **2026-09-15 ~ 09-16 的工程化加固期**（观测区接真数据、契约与实现对齐、
控制面加鉴权、数据源降级可见），那段不新增阶段能力。

> **2026-09-19 Phase 4 大脑期已交付并验收（含复审补全）**：nlp-service 新增 `app/brain/`
> （LLM Gateway / 规划器 / 语义缓存 / RAG 决策链 / 缺口检测 / 来源标注 / IN3 审计 / IN-02 记忆图谱）、
> session-manager 新增会话 FSM 与 Redis 持久化并接线大脑层、wp-bff 新增 `/api/wp/brain*` 与会话代理、
> work-platform 对话界面与 Console 大脑视图/交互终端均切真实数据。
> 测试 **Java 162 · Python 124 · wp-bff 55**（共 341），契约 0 FAIL，文档覆盖 113/113。
> 三件套见 `docs/项目进度日志报告/Phase4-*`；演示脚本见 `docs/demo/Phase4-DEMO.md`。
> **Phase 6 主体尚未开工；MC-01 Phase 6 前置已完成并进入 CI。**

> **2026-09-19 端口冲突修复**：`session-manager` 的 gRPC Health 默认端口由 `9092` 改为
> **`19092`**（原值与 Kafka 宿主端口 `9092` 冲突，导致启动失败），`healthcheck.sh` 同步改探 19092；
> 起 jar 已**无需再传 `GRPC_PORT` 覆盖**。潜在项 `sense-service 9093 ↔ Alertmanager(P7) 9093`
> 已登记为 **DEBT-018**（见《技术债台账》§4.17）。服务注册表 §5 与相关文档口径已同步。

> **2026-09-20 WB-10 多模型接入配置落地（前后端全链路）**：模型配置从环境变量搬进
> PG 表 **`llm_model_config`**，前台可增删改查；凭据 **AES-256-GCM 加密落库**，
> 对外只回 `sk-***last4`；按 **6 个功能角色**（intent / embed / rerank / generate / plan / code）
> 组装引擎，调用时**角色优先、层级兜底**；支持**不重启进程热重载**。
> 新增 `work-platform/ModelsView` 独立视图（从 `ModuleView` 通用模板迁出）、
> wp-bff `/api/wp/models*` 6 个方法、契约 4 条路径转 `implemented`。
> 同轮修复两处既有缺陷：**`llm_model_config` 主键分配错位**（id 取自进程内计数器，
> 库中已有行时必撞主键并**静默降级导致"保存成功、重启丢失"**；已改由序列 `RETURNING id` 分配
> 并补幂等序列校正）、**测试隔离失效**（缺 `tests/conftest.py`，`BRAIN_PG_ENABLED=false` 的设置
> 因导入顺序永远晚一步，测试实际连了真实库并污染数据）。
> 功能逻辑全文见 [`docs/模型接入配置-功能逻辑.md`](./模型接入配置-功能逻辑.md)。
> 测试基线随之更新为 **Java 251 · Python 199(+1 skip) · wp-bff 103**（共 553；Python/wp-bff 为
> 2026-09-20 09:39 复测口径 —— 较本轮文档首稿的 186 / 100 多出 `tests/test_token_usage.py` 13 项
> 与 wp-bff 补测 3 项，文档首稿成于该两项落地之前）。

> **⚠️ Phase 4 复审补全（2026-09-19）**：首次验收后**通读实现**又查出 5 项"单测与验收报告都抓不到"的
> 问题 —— 2 项**语义级伪实现**（语义缓存声称 Redis 后端实只写内存；决策链回放端点对任意 id 都返
> `200 + 空链`）+ 1 项跨服务租户口径不一致 + 1 项入库不失效检索热缓存 + 1 项记忆图谱"有边无点"。
> 用户确认"按完整需求补齐，可优化不可简化"后**全部修复并补测**，逐条证据见
> `docs/优化日志/2026-09-19-Phase4大脑期需求审核与补全.md`，登记见《技术债台账》§4.16。

Phase 4 的 LLM 接入已完成 **OpenAI 兼容 API 路径**：WB-10 `llm_model_config` 装配真实 HTTP 引擎，OpenRouter 固定模型验证通过；模板后端仅作为未配置或故障时的可观测降级。
嵌入/重排权重未安装时仍走确定性降级后端（DEBT-010/011）。
另有 Console「会话趋势（近 7 日）」一图为演示值（缺时序聚合接口，界面已标注）。

唯一未闭合的验收项是 **OCR 端到端**，阻塞原因是需要外部引擎（PaddleOCR / Tesseract）尚未安装 ——
系统对此**诚实上报** `available=false`，不伪造结果。同理，真实 BGE-M3 嵌入与交叉编码器重排权重
未安装时走**确定性降级后端**并上报 `degraded=true`，同样不伪造。

---

## 二、阶段进度

| 阶段 | 名称 | 状态 | 交付要点 | 归档 |
|---|---|---|---|---|
| Phase 0 | 胚胎期 | ✅ 已验收（08-30，09-15 追溯补档） | 单仓结构 / Maven 多模块 / FastAPI 骨架 / compose 基础设施 / Nacos 注册 / gRPC Health(9091-9094) / proto 双语言生成 / CI + 契约校验 | `docs/demo/Phase0-DEMO.md` |
| Phase 1 | 神经期 | ✅ 已验收（09-11 收尾，09-13 动态验证） | 网关路由 + JWT + 租户隔离 / BusProxy(NATS) / Kafka 基线 / OTLP+Jaeger / 统一错误模型 / SSRF 防护 / **VS1 追问闭环** | `docs/demo/Phase1-DEMO.md` |
| Phase 2 | 感官期 | ✅ 已验收（09-12） | **R2-01~R2-10** 全覆盖：五感渠道、R0 调度、R1 动态处理、健康监控+自动隔离+死信、质检暂存与回滚、规则→L0 意图级联、控制台感官视图 | `docs/demo/Phase2-DEMO.md` + 三份阶段报告 |
| 加固期 | 工程化（非独立阶段） | ✅ 已收口（09-15 ~ 09-16） | 观测区 + 最小 Ops BFF、控制面鉴权与 CORS 收紧、**契约↔实现双向对齐（37 端点）**、总览聚合端点、数据源降级可见化、CI 补 Python/wp-bff 测试、**契约↔生成物同步门禁（2026-09-17）**、前端路由懒加载 | `docs/优化日志/` |
| Phase 3 | 躯体期 | ✅ **已验收（09-17）**，09-18 审核补全 | **R3-01~R3-09 + R-C03** 全覆盖：三级存储抽象、文档格式解析、文档分块、嵌入服务、Qdrant 向量入库、语义检索、结果重排、RAG 管道、缓存优先、知识管理 API、三层对账；感官→躯体闭环；**DEBT-001 销项**；09-18 补全工作平台写路径 + IN-05 预留接口 | `docs/demo/Phase3-DEMO.md` + 三份阶段报告 + `docs/优化日志/2026-09-18-*` |
| Phase 4 | 大脑期 | ✅ **已验收（09-19）**，同日复审补全 | **R4-01~R4-09 + R-C04 + IN-02 + X4/D5** 全覆盖：会话 FSM 与 Redis 持久化（重启可恢复）、LLM Gateway（L1/L2/L3 路由+超时重试）、**语义缓存真接 Redis**（实测命中率 20%~67%）、任务规划器、LangGraph RAG 决策链（**6 主步** intent→plan→retrieve→gap→generate→verify→annotate）、覆盖度缺口检测、来源标注、**IN3 决策链审计（PG 持久化 + `decision_id` 回放 + 三卡）**、**IN-02 记忆图谱（PG 邻接表 + 递归 CTE，压缩率 0.883）**、**50 条意图评测集（级联 98.33%）**、会话链路端到端贯通；wp-bff 大脑端点+会话代理+驾驶舱 + 契约落标；work-platform 对话界面与 Console 大脑视图/交互终端切真实数据 | `docs/项目进度日志报告/Phase4-*` + `docs/demo/Phase4-DEMO.md` + `docs/优化日志/2026-09-19-*` |
| Phase 5 | 四肢期 | ✅ **已验收（09-19）**，同日复审收口 | **R5-01~R5-08 + R-C05(预) + IN-06** 全覆盖：新增独立服务 **`tool-executor`（8084/9095）** —— 工具注册表（注册/列表/详情/卸载/废弃）、执行引擎（回执标准化 + 连续失败熔断 3 次/30s）、沙箱隔离（Docker 真隔离主路径 + 受限子进程**显式降级**）、三内置工具（calculator 无 eval / http 域名白名单+SSRF 拦截 / code 沙箱执行）、安全三闸（白名单 / Draft-07 参数 Schema / 12 条敏感规则）、全量审计（PG `tool_audit_log` 真相源 + Kafka `lifeform.tool.invoked` 旁路 + 内存降级）、**IN-06 契约治理（semver + schema 指纹 + 影响分析 + `contracts/tool-schema-baseline.json` 门禁）**；wp-bff `/api/wp/tools*` 9 个方法 + work-platform **执行视图（R-C05）** | `docs/项目进度日志报告/Phase5-*` + `docs/demo/Phase5-DEMO.md` + `docs/优化日志/2026-09-19-Phase5四肢期进度复审与缺口清单.md` |
| Phase 6-8 | 小脑 / 免疫 / 生命期 | ⏸ 冻结 | 编排、免疫、自进化 | `AGENT_CONTEXT/Phase5~8-*/` |
| V1 | 验证期 | 📄 设计就绪 | 性能基准实验室 / STRIDE 威胁建模 / 容灾演练 / 数据分级矩阵 / LLM 双轨评估 / 知识治理 | `AGENT_CONTEXT/V1-验证期设计/` |

---

## 三、功能交付矩阵

### 3.1 后端 Java（157 个手写源文件 = 116 主 + 41 测；另有 90 个 proto 生成文件）

| 服务 | 端口 | 已交付功能 | 主要入口 | 测试 | 状态 |
|---|---|---|---|---|---|
| `gateway-service` | 8080 | 统一入口路由（`/api/{session,sense,nlp,body,tool}/**`）、JWT 校验、**网关注入 `X-Tenant-Id`**、跨租户访问防护、统一错误响应模型 | `GatewayApplication` · 路由见 `application.yml` | 2 | ✅ |
| `session-manager` | 8081 | 会话 CRUD、**VS1 追问闭环**（session → intent → **语义检索** → 回答）、Redis 消息存储、Kafka 事件、**HTTP/1.1 出站客户端 + 优雅降级**、**路由层错误语义（404/405/415）**、**R4-01 会话 FSM + R4-02 Redis 持久化（重启可恢复）+ R-C04 会话统计** | `SessionController` · `SessionFsm` · `SessionStore` · `OutboundHttp` · `BrainClient` | 43 | ✅ |
| `sense-service` | 8082 | 五感渠道（Touch 实装 + Visual/Nose/Audio/Taste 保留位）、R0 规则调度、R1 指令处理、渠道健康监控与自动隔离、指数退避重试 + 死信、质检暂存（MinIO/本地双后端）+ 批量回滚、`SenseAdminController` 管理端点、**采集事件携带正文发布**、**路由层错误语义（404/405/415）** | `SenseChannel` · `ChannelRegistry` · `R0Scheduler` · `R1DynamicHandler` · `ChannelHealthMonitor` · `StagingStore` · `SenseEventPublisher` | 53 | ✅ |
| `body-service` | 8083 | **躯体期知识管道**：**格式解析（md/text/html）** → 分块 → 嵌入 → 三层存储（Redis 热 / Qdrant 温 / PostgreSQL 冷）→ 语义检索 → 重排 → RAG；知识管理 REST API（含**三层一致性对账**与 **IN-05 迭代检索预留接口**）；**消费 `lifeform.sense.collected` 形成感官→躯体闭环**；**写路径失效检索热缓存**；统一错误码 + OTLP 链路 + **路由层错误语义（404/405/415）** | `KnowledgeController` · `DocumentParser` · `ChunkProcessor` · `IngestService` · `RetrievalService` · `RagPipeline` · `StorageFacade` · `QdrantClient` · `SenseCollectedConsumer` | 64 | ✅ |
| **`tool-executor`** | **8084** | **四肢期工具执行**：注册表（R5-01，注册/列表/详情/卸载/废弃 30 天期）、执行引擎（R5-02，白名单→Schema→敏感参数→熔断→带超时执行→标准化→审计）、沙箱（R5-03/05，Docker `--network none`/只读 rootfs/去能力 + 受限子进程**显式降级**）、三内置工具（R5-04/05：calculator 手写解析器**无 eval**、http 域名白名单+SSRF 拦截、code 沙箱执行）、安全三闸（R5-07）、全量审计（R5-08，PG 真相源 + Kafka 旁路 + 内存降级）、**IN-06 契约治理（semver + schema 指纹 + 影响分析）**、路由层错误语义（404/405/415） | `ToolController` · `ToolRegistry` · `ToolGuard` · `ToolExecutor` · `SandboxExecutor` · `ToolAuditLog` · `ToolBootstrap` | **82** | ✅ |
| `proto-contracts` | — | 6 份 proto（session/sensor/brain/limb/body/common）→ **90 个生成源文件已入库**，默认生命周期不再调 protoc；**6 份契约、15 个 RPC 当前均无运行时调用方**（服务间走 HTTP + NATS/Kafka，9091-9095 用的是 grpc-services 自带健康服务） | `proto/` + `proto-contracts` | — | ✅ 仅编译期 |

### 3.2 Python（`services/python/nlp-service`，10 个业务 py + 12 个 proto 生成文件）

> 12 个生成文件位于 `generated/`，**已 gitignore、不入库**（详见 `docs/proto契约使用说明.md`）。

| 功能 | 文件 | 指标 | 状态 |
|---|---|---|---|
| 意图规则引擎（12 场景） | `app/intent/rules.py` | 覆盖 12 场景（要求 ≥10） | ✅ |
| L0 轻量分类器 | `app/intent/l0_model.py` | 留出集准确率 **90.0%**（要求 ≥85%） | ✅ |
| 规则→L0 级联 | `app/intent/cascade.py` | P99 **0.052 ms**（要求 <50 ms） | ✅ |
| OCR 服务 | `app/ocr.py` | 引擎懒初始化；无引擎时 `available=false` | ⚠️ 待装引擎 |
| **嵌入服务（R3-03）** | `app/embedding.py` | BGE-M3 可用则用，否则确定性 hash n-gram **768 维**后端；`backend`/`degraded` 如实上报 | ✅ |
| **重排服务（R3-06）** | `app/reranker.py` | 交叉编码器可用则用，否则词项重合度重排；不可用降级不阻断 | ✅ |
| **分块器（R3-02）** | `app/chunking.py` | 与 Java `ChunkProcessor` **同算法**（供跨语言一致性校验） | ✅ |

### 3.3 前端（`web/work-platform`，Vue 3 + Vite + TS + Element Plus，9 vue + 8 ts）

| 功能 | 状态 |
|---|---|
| 工作平台骨架：路由 / Pinia / `PlatformLayout` / 全局搜索 / 通知中心 / 偏好持久化 | ✅ |
| 总览驾驶舱：生命群落并发拓扑、模型调用趋势、运行时拓扑、优化建议**真实执行回路** | ✅ |
| 任务（创建/重试/归档/排序）与对话工作区（产物/文件/diff/预览） | ✅ |
| 生命体征、器官报告、大脑追踪、感官下钻、进化趋势 | ✅ |
| 多 Agent 总线拓扑（模式卡 / 消息轨 / 工件堆叠 / 闸门） | ✅ |
| 自动化 cron 校验、用例编排、L3/L4 审批流展示 | ✅ |
| **观测区**：中间件监控（真实 TCP 探针 + 单卡/一键串行启停）、链路追踪（真实 Jaeger） | ✅ |
| **躯体视图（R-C03）**：知识总量 / 检索质量（P99·命中率·缓存命中率）/ 三层存储健康度 / **文档入库（粘贴或文件读取）** / 检索测试（高亮片段 + 召回·重排分对照） | ✅ |
| **数据源降级可见化**：`API · 降级 N` 徽标 + 横幅 + 逐模块提示；读端点回落演示数据须标注，**写端点一律不伪造成功** | ✅ |
| 路由动态 `import()` 懒加载 | ✅ |
| `ModuleView.vue` 拆分、Element Plus 按需引入 | ⏸ 待视觉回归基线 |

### 3.4 Ops BFF（`services/node/wp-bff`，零依赖 Node，端口 8090）

| 功能 | 状态 |
|---|---|
| `GET /api/wp/middleware` —— 8 个中间件真实 TCP 探针（状态/时延/指标） | ✅ |
| `POST /api/wp/middleware/:key/start\|stop` —— 真实 `docker compose up -d / stop`，8 键白名单 + 固定命令形状 + 审计日志 | ✅ |
| `GET /api/wp/tracing` —— 真实 Jaeger 数据（服务清单 + 采样口径 `sample_size/sample_limit/p99_basis`） | ✅ |
| `GET /api/wp/overview` —— 只聚合**有真实来源**的域，未实现域进 `gaps`（不静默填充） | ✅ |
| `GET /api/wp/healthz` —— 只读自检（令牌来源、来源白名单、键清单） | ✅ |
| **`GET /api/wp/knowledge`** —— 躯体视图数据（知识统计 + 检索质量 + 三层存储状态） | ✅ |
| **`POST /api/wp/knowledge`** —— **知识入库写路径**（单篇 / 批量导入；`format` 透传；体层拒绝时上报错误码而不算成功） | ✅ |
| **`POST /api/wp/knowledge/search`** —— 检索测试（查询词解析 + 命中片段高亮 + 召回/重排分对照） | ✅ |
| **`GET /api/wp/tools`** —— 执行视图聚合（工具卡片 + 沙箱状态，R-C05 预） | ✅ |
| **`GET /api/wp/tools/registry`** —— 工具注册表摘要 + 变更历史（IN-06） | ✅ |
| **`GET /api/wp/tools/{name}`** / **`/tools/{name}/impact`** —— 单工具详情 / **变更影响分析（IN-06）** | ✅ |
| **`GET /api/wp/tools/audit`** / **`/tools/audit/stats`** —— 审计明细与汇总（R5-08，租户隔离） | ✅ |
| **`GET /api/wp/tools/metrics`** / **`/tools/sandbox`** —— 成功率·分位·熔断状态 / 沙箱状态 | ✅ |
| **`POST /api/wp/tools/execute`** —— 工具执行（**控制端点**：来源白名单 + 控制令牌） | ✅ |
| **控制面鉴权**：来源检查 + `X-WP-Control-Token`（`timingSafeEqual`）；无鉴权后门 | ✅ |
| **路由层错误语义 + 统一错误信封**：路径不存在 → 404 / 方法不支持 → 405（附 `Allow`）/ 全部错误体为 `{code,message,details}`（2026-09-18 对齐全平台，见 `docs/异常流程归纳.md`） | ✅ |
| 回归测试 `node --test` | ✅ 34 项 |

### 3.5 基础设施与工具链

| 项 | 内容 | 状态 |
|---|---|---|
| `docker-compose.yml` | 8 个中间件：redis / postgres / qdrant / nats / nacos / minio / jaeger / kafka（+ 5 个数据卷） | ✅ |
| `scripts/` | `mvn-dev.sh`（绕过损坏的 `mvn.cmd`）、`contract-check.py`、**`proto-sync-check.sh`**（契约↔生成物同步门禁）、`healthcheck.sh`、`start.sh`、`md2html-report.py`、`java-doc-coverage.py`、`proto-gen.sh` | ✅ |
| CI（`.gitlab-ci.yml`） | `java-test` / `frontend-build` / `contract-check` / **`proto-check-python` · `proto-check-java`** / `python-test` / `wp-bff-test` / `docker-smoke`（后者 `allow_failure`，需 dind runner） | ✅ |

---

## 四、测试与验证基线

**口径：2026-09-20 WB-10 模型接入配置落地轮的实测值**（历史归档中的 54 / 73 / 61 / 114 / 162 / 244 / 158 / 87 项是当时口径）

| 类别 | 命令 | 结果 |
|---|---|---|
| Java | `cd services/java && ../../scripts/mvn-dev.sh test` | **289 测试 / 0 失败**（gateway **2** · session-manager **43** · sense-service **53** · body-service **67** · tool-executor **86** · **collab-bus 38**；其中 9 个为真实 PG/NATS 集成用例） |
| Python | `../venv/Scripts/python.exe -m pytest`（nlp-service 下） | **199 通过 / 1 跳过**（意图 15 + HTTP 层错误语义 10 + OCR 4 + 分块 12 + 嵌入 13 + 重排 8 + 函数调用 15 + 大脑层（含记忆图谱）56 + 跨服务预算/预算传播 20 + Phase4 评测集 6 + **WB-10 模型接入配置 28** + **WB-10 用量计量 13**） |
| 契约（跨服务） | `python scripts/contract-check.py` | **0 FAIL** |
| 契约（工作平台） | `python scripts/contract-check.py --work-platform --openapi contracts/work-platform-bff-openapi.yaml` | 71 端点，implemented **51 个路径 / 55 个方法** / planned 20，**0 FAIL**；**工具契约基线：3 个工具 ↔ `ToolBootstrap` 注册 3 个** |
| wp-bff | `cd services/node/wp-bff && node --test test/server.test.js` | **118 通过**（新增 Alertmanager 独立令牌投递 + Phase 6 Blocking 真实派生 / 降级 / fail-closed 用例；既有鉴权、超时预算、工具、模型与协作回归全绿） |
| 前端 | `cd web/work-platform && npm run test:report && npm run typecheck && npm run build` | **27/27 功能测试通过**；覆盖率 31.72% 行 / 70.09% 分支；typecheck/build 通过 |
| **超时预算（跨服务）** | `python scripts/timeout-budget-check.py` | **0 FAIL**：19 条边 `ok=19 · gap=0` —— **倒挂全部清零（2026-09-19 GAP 收尾轮）**；三层校验（语义 / 代码锚点 / 调用点魔法数字），锚点漂移反向验证通过（植入 19999 → fail=1 / exit=1）；详见 §6.1-9 |
| **文档一致性** | `python scripts/doc-consistency-check.py` | **0 FAIL**：异常编号 35 条无撞号 · 章节号 20 个无重复 · md↔html 孪生 57 组无过期（无需 `--no-twin`） |
| 端到端（Docker） | `.workbuddy/tmp/phase3-e2e.py`（入库→检索→缓存→隔离→RAG→指标→重入库/删除） | **35 PASS / 0 FAIL** · P99 **366ms** · 命中率 **0.925** · 缓存命中率 **0.45**（2026-09-18 口径；**未随 Phase 5 重跑**） |
| 工具执行实测（8084） | 注册表 / 执行 / 沙箱 / 审计 / 指标 五组接口 | ✅ 通过 —— calculator `sqrt(2)+pow(2,10)=1025.414…`；`code` 输出 `45`（`sandboxed=true, sandbox_backend=docker`）；出网样本 **403 `AGENT_TOOL_ARGS_BLOCKED`**；写盘样本 **400 + 内核 `Errno 30`**；`GET /sandbox` = `docker_available=true` / `active_backend=docker` / `isolated=true` / `degraded=false`（**Docker 真隔离已复验通过**，见 §6.1-8）；**探测耗时 5.7s → 0.5s**（非阻塞化后，见《2026-09-19-工作平台真实链路打通与沙箱探测非阻塞化》） |
| 感官→躯体闭环 | Kafka 发布带正文事件 → 消费入库 → 语义召回 | ✅ 通过 |
| 中间件/链路 | `docker compose config` · `./scripts/healthcheck.sh` | **已整体重跑（2026-09-19）**：脚本补 8084 / 9095 / 8090 三探针后实测 **19 OK / 6 BAD**，BAD 全部是"本轮未启动"（`gateway` / `sense` 未起，`NATS` / `MinIO` 非 Phase 5 依赖）——**不掩盖、不刷绿** |
| 文档覆盖率 | `python scripts/java-doc-coverage.py` | **160/160** 业务源文件已登记（GAP 收尾轮登记 `BudgetGuard.java`；tool-executor **43/43**） |

> **注意**：`mvn` 直接调用在本机会卡死（`mvn.cmd` 损坏 + protobuf 插件清理目录阻塞），
> 必须走 `scripts/mvn-dev.sh`。生成 HTML 镜像本机**已改用托管 Python + `PYTHONPATH` 指向 `pylibs`**
> （anaconda 已于 2026-09-18 卸载，本机 `E:/software/python/python312` 无 `markdown` 包）。
> **body-service 启动需显式 `--server.port=8083`**
> （本机环境注入了会被 Spring 松散绑定误读的路由变量）。

---

## 五、分支与远端拓扑

### 5.1 分支

| 分支 | 角色 | 当前 |
|---|---|---|
| `codex/main` | Codex 本机主开发分支 | 当前 HEAD；本轮 R-MC01-05 与优化提交落点 |
| `dev` | 另一台开发机协作分支 | 与 `codex/main` 同步；推送前必须先 fetch，避免覆盖对方改动 |
| `workbuddy/main` | WorkBuddy 协作分支 | 与 `codex/main` 同步；仅在明确需要时推送 |
| `main` | 仓库初始提交（仅含一个 `README`），**历史遗留、不维护** | 不做开发 |

> 表内角色描述以当前远端配置为准；提交号不写死在文档里，使用 `git branch -vv` / `git rev-parse` 查询。

**远端（本机）**：

| remote | 地址 | 用途 |
|---|---|---|
| `origin` | `https://ghfast.top/https://github.com/kennkc/ai-agent.git` | 加速镜像（拉取） |
| `github` | `git@github.com:kennkc/ai-agent.git` | SSH 直达 |

**约定**：跨分支吸收按「**时间 + 语义**逐项合并」，**禁止整文件覆盖**。

### 5.3 变更前置约定（强制）

任何仓库内容变更之前，先执行 **`README.md` §8.0**：`git fetch` → 判落后/领先 → **解读**对方改动
→ 再决定集成 → 才开工。已登记为技能 `git-sync-before-change`
（WorkBuddy 侧 `~/.workbuddy/skills/`、Codex 侧 `~/.agents/skills/`，两份必须保持一致）。

---

## 六、剩余项与已知缺口

### 6.1 阻塞 / 待验收

| # | 项 | 原因 | 影响 |
|---|---|---|---|
| 1 | **OCR 端到端验收** | 需安装 PaddleOCR 或 Tesseract | 视觉渠道维持 `DEGRADED`，`available=false` |
| 2 | **真实嵌入（BGE-M3）/ 交叉编码器重排权重未装** | 需下载模型权重 | 走确定性降级后端并上报 `degraded=true`（**不伪造**），装模型后自动切换 |
| 3 | **BFF 契约 20 个 planned 端点** | 已实现 49 个路径 / 53 个方法；Phase 6 Blocking 已完成最小闭环，完整注册表 / 编排仍待解冻 | 已实现域接真实数据；未接入注册表显式 unavailable 或 503，不伪装成功 |
| 4 | `ModuleView.vue` 拆分 / Element Plus 按需引入 | 需先建视觉回归基线 | 首屏包体偏大（element chunk 952 kB） |
| 5 | Phase 6+ | 团队节奏控制 | 小脑期（编排）暂不开工，只做前置设计；大脑期已验收并支持 `llm_model_config` 外部模型装配，生产仍需结构化输出、预算与供应商降级策略（DEBT-015/016） |
| 6 | ~~**Phase 2 两项验证方法仍"待验"**~~ → **已闭合** | C09 数据总线代理、C16 感官渠道管理器已完成测试 | ✅ DEBT-009 已用真实 NATS/Kafka 验证；DEBT-006 已补五渠道注册/降级测试 |
| 7 | **Phase 3 阶段范围缺口：IN-01 / WB-03 / WB-05 / ~~MC-01~~ 仍未实现（MC-01 已闭合）** | Phase 3 需求文档 §8/§9 列有其需求与验收标准，但阶段报告「需求范围」只声明 `R3-01~R3-09 + R-C03` | 技能包模型（MinIO+etcd）、专家中心、技能市场仍各需独立服务；**MC-01 已于 2026-09-20 作为 Phase 6 前置独立切片完成 R-MC01-01~05**（含工作平台真实协作域接线） |
| 8 | ~~**R5-03 沙箱真隔离未验证**~~ → **已闭合** | 复审时 `docker_available=false`、`agent-sandbox:latest` 镜像未构建，走降级后端 | ✅ **2026-09-19 收口轮已复验**：构建镜像后重启，`active_backend=docker` / `isolated=true` / `degraded=false`；逃逸样本取得内核级证据（出网 `Errno -3`、写盘 `Errno 30`），见《Phase5-测试验收报告》§5.2。**唯一未做**：`healthcheck.sh` 未整体重跑 |
| 9 | ~~**跨服务超时预算 3 条倒挂未修复（GAP-01 / GAP-05 / GAP-06）**~~ ✅ **2026-09-19 GAP 收尾轮全部闭合** | `contracts/timeout-budget.yaml` 现为 `ok=19 · gap=0`，跨服务余量全部 ≥ 1.5x。闭合方式统一为**内层收敛、外层覆盖**：GAP-01 收紧 nlp 总预算 25s→20s（原上限从不构成约束，session 30s 恰 1.5x）；GAP-05 给 body 检索加端到端预算 10s（`BudgetGuard`，顺带修正登记表"出站读超时 10s"冒充端到端最坏的失真）+ 上游 8s→15s 并回收第二套口径；GAP-06 新增 wp-bff ASK 档 53s（GENERATE 45s 保持不动）。锚点漂移反向验证通过（植入 19999 → fail=1 / exit=1），见 `docs/优化日志/2026-09-19-GAP收尾-三条倒挂一次闭合与登记表失真修正.md` |

### 6.2 已登记的工程缺口（源自 `docs/java-services/07-运行时配置与横切约定.md`）

- wp-bff **无网关路由**（`/api/wp/**` 生产路径缺失，前端仅靠 Vite 代理）
- ~~body-service **缺 OTLP 配置**（不在 Jaeger 链路），也未接入统一错误码体系~~ ✅ **2026-09-18 Phase 3 已补齐**（加 OTLP endpoint + 建 `ErrorCode`/`BizException`/`GlobalExceptionHandler`）
- `ErrorCode` 在 `session-manager` / `sense-service` / `body-service` **各有一份**（三份），需人工同步
- ~~未映射路由返回 500 而非 404~~ ✅ **2026-09-18 已修**（三服务 `GlobalExceptionHandler` 补路由层 404/405/415 分流 + `AGENT_METHOD_NOT_ALLOWED`，各补回归用例）
- 租户头 `defaultValue="default"` → 绕过网关直连时**静默落入 default 租户**
- HTTP 端口**写死**，未做环境变量占位
- `TextExtractorTest` 有用例名拼写错误 `decodesGbkWitoutHeader`（未擅改测试代码）

### 6.3 技术债（`DEBT-00X`）

> **权威台账：[`技术债台账.md`](./技术债台账.md)** —— 含逐条源码证据、触发点与一致性问题登记。
> 下表仅摘要，**以台账为准**。

| 债ID | 模块 | 简化实现 | 正式目标 | 触发点 | 状态 |
|---|---|---|---|---|---|
| DEBT-001 | body-service | ~~内存 Map + 字符命中打分~~ → **已换真三层存储**（PG 元数据 + Qdrant 向量 + Redis 热缓存） | Qdrant 向量检索 | VS3（Phase 3） | ✅ **已闭合** |
| DEBT-002 | session-manager | 字符串模板拼接回答 | LLM 生成 | VS2 | 🟡 未闭合 |
| DEBT-003 | session-manager | ~~内存 Map~~ → **已换真 Redis** | Redis 持久化 | P4 | ✅ **已闭合** |
| DEBT-005 | gateway-service | 最小 HMAC JWT | OAuth2 / OIDC | P7 | 🟡 未闭合 |
| DEBT-006 | sense-service（测试） | ~~仅单渠道单测~~ → 已补五渠道注册/降级测试 | 五渠道集成测试 | P2 | ✅ **已闭合** |
| DEBT-007 | nlp-service | TF-IDF + 最近质心 | BERT 级模型 | P4 | 🟡 未闭合 |
| DEBT-009 | session-manager（测试） | ~~仅总线代理单测~~ → 已补真实 NATS/Kafka 链路测试 | 消息链路测试 | P2 | ✅ **已闭合** |
| DEBT-010 | nlp-service（嵌入） | 字符 n-gram 哈希（768 维，**非语义相似**） | BGE-M3（1024 维） | 装权重 | 🟡 未闭合 |
| DEBT-011 | nlp-service（重排） | 词法覆盖率打分 | bge-reranker-v2-m3 | 装权重 | 🟡 未闭合 |
| DEBT-012 | sense-service（事件） | 事件携带正文；超 1MB 无回读路径 | 按 `staging_ref` 回读 MinIO | 正文 > 1MB | 🟡 未闭合 |
| DEBT-013 | body-service（解析） | **不支持 PDF/DOCX**（显式拒绝，不静默当文本） | 接入 PDFBox/Tika 或 OCR 通道 | 需上传 PDF 时 | 🟡 未闭合 |
| DEBT-014 | session-manager（持久化） | 消息列表全量保留 + 读取时截断 | 会话压缩 + 窗口上限落库 | 单会话 > 100 轮 | 🟡 未闭合 |
| DEBT-015 | nlp-service（LLM） | ~~未接真实 LLM~~ → WB-10 OpenAI 兼容 API 路径已接通 | OpenAI 兼容 / 本地推理 | 接入权重或 API Key | ✅ **API 路径已闭合** |
| DEBT-016 | nlp-service（Token 计量） | 字符估算（中文 1.5 char/token） | tokenizer 精确计数 | 接入真实模型后 | 🟡 未闭合 |
| DEBT-017 | nlp-service（任务规划） | 规则模板映射 `planner=rule` | LLM 规划 / 多步规划器 | P6 多模型策略 | 🟡 未闭合 |
| DEBT-018 | 跨服务（gRPC 端口） | session 已外移 19092；Alertmanager 宿主机端口外移 **19093** | 端口块与基础设施不重叠 | P7 部署前 | ✅ **已闭合** |
| DEBT-019 | tool-executor（注册表） | 注册表 + 版本历史**仅进程内**（内存）；影响分析只覆盖**显式登记**的消费方 | etcd/DB 持久化 + 跨实例共享 + 消费方自动发现 | 多实例部署或需重启保留 | 🟡 未闭合 |
| DEBT-020 | tool-executor（校验器选型） | 实装 **networknt**（Draft-07），设计写 everit | 与设计文档选型一致 | 需严格对齐选型时 | 🟡 未闭合 |
| DEBT-021 | tool-executor（指标） | ~~工具专属指标仅进程内~~ → 已同步 Micrometer/Prometheus | 工具指标 Micrometer/Prometheus 化 | 需跨实例聚合或告警时 | ✅ **已闭合** |
| — | body-service（口径） | 冷层为纯 PG 表（**未用 pgvector**）；缓存为**精确指纹**（非向量语义缓存） | 见台账 §4.6 | ✅ 已完成 | ⚠️→✅ 2026-09-18 用户确认后**改设计文档对齐实况**（需求/开发设计文档 + HTML 孪生已修订） |

> 上表为节选。**2026-09-19 Phase 5 新增 DEBT-019~021**（注册表仅进程内 / 校验器选型差异 / 指标未接
> Prometheus），逐条见《技术债台账》§二与 §4.18。登记率保持 **100%**。

**红线状态**：DEBT-006 与 DEBT-009 已于 2026-09-20 关闭，当前无“触发点已到但未闭合”的红色债务。
DEBT-007 虽未闭合，但实测已满足 R2-08 指标（90.0% / 0.035ms），**不构成验收阻塞**。

### 6.4 2026-09-17 闭合的工程项

| 项 | 原缺口 | 闭合方式 |
|---|---|---|
| **契约↔生成物同步门禁** | 改了 `proto/*.proto` 却忘重新生成 / 提交产物 —— 此前**没有任何检查**能发现 | `scripts/proto-sync-check.sh`（本地双向比对：未入库 / 内容不同 / 契约已无）+ CI job `proto-check-python` · `proto-check-java`（失败即阻断） |
| **Python 生成物无生成步骤** | `generated/` 已 gitignore，换机后可能整个缺失而无人察觉 | CI `proto-check-python` 每次重新生成 + 12 文件数断言 + 6 模块导入自检 |
| 台账记账修正 | `grpcio-tools` 曾被登记为「未声明」，实际一直在 `requirements-dev.txt`（构建期依赖） | 修正 `技术债台账.md` §6-7 与 `proto契约使用说明.md` §4.2 |

> **待首跑确认**：Java 侧 CI 门禁的**真实 `mvn -Pproto-gen` 生成路径**尚未在 runner 上跑过
> （比对逻辑已用合成生成物验证 90/90，三类偏差均可报出）。详见台账待办 §6-9。

---

## 七、docs 归档索引

| 路径 | 用途 | 维护时机 |
|---|---|---|
| `docs/README.md` | docs 目录地图与阅读顺序 | 新增目录时 |
| `docs/项目进度总览.md` | **本文件** — 当前进度、功能矩阵、测试基线、分支拓扑、剩余项 | 每次收口 |
| `docs/功能开发流程.md` | 每个功能的开发流程与验收标准 | 流程或功能变化时 |
| `docs/proto契约使用说明.md` | **6 份 proto 的使用说明与当前使用现状**（契约清单 / 引用矩阵 / 双侧生成链路 / 变更流程 / 与 OpenAPI 分工 / 缺口登记） | 契约增删改或接入方变化时 |
| `docs/技术债台账.md` | **`DEBT-00X` 权威台账**（简化实现 / 正式目标 / 触发点 / 状态 + 源码证据） | 创建或关闭任何债务时 |
| `docs/异常流程归纳.md` | **异常流程总图**：运行期分层错误语义（统一信封 / 路由层分流 / 鉴权 / 降级 / planned 三态判定 / 处置决策树）+ 研发期过程异常 30 条与规范动作清单 | 发现新异常模式或调整错误语义时 |
| `docs/项目进度日志报告/` | 分阶段三件套（阶段性报告 / 开发执行日志 / 测试验收报告，各含 HTML） | 每阶段收口 |
| `docs/demo/` | 各阶段端到端演示脚本（怎么跑起来看到效果） | 每阶段收口 |
| `docs/java-services/` | Java 逐文件说明（**9 篇**，覆盖 **157 个手写源文件** + 90 个生成文件；08 篇为数据存储总纲，**09 篇为 tool-executor**） | 增删 Java 文件后 |
| `docs/优化日志/` | 每轮工程化加固的过程与结论 | 每轮加固后 |
| `db/`（仓库根） | **数据存储归档**：PG/Redis/Qdrant/暂存结构文档 + 运行态快照（`snapshots/` 只增不改）与恢复指引 | 存储结构变更时；新快照带时间戳追加 |

---

*本文件首次建立于 2026-09-17 · 维护：探索者一号（WorkBuddy 侧）*
*2026-09-19 更新：Phase 5 四肢期落地 —— 新增 `tool-executor` 模块（Java 248 测）、执行视图、IN-06 契约基线门禁、DEBT-019~021；同日「真实链路打通轮」起 8090 最新 BFF + 8084 tool-executor，修复沙箱探测阻塞（`available()` 非阻塞化，+4 条探测性能契约），工具域由 `partial` 转 `partial:false`，详见《2026-09-19-工作平台真实链路打通与沙箱探测非阻塞化》。本节数字均取自 `mvn-dev.sh test` / `pytest` / `node --test` / `contract-check.py` / `java-doc-coverage.py` 的实测输出。*
