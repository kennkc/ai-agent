# 本地检索模型启用指南（BGE-M3 / bge-reranker）

> **适用**：LLM 走 API、检索用本地模型的部署形态（2026-09-20 决策）
> **权重位置**：`E:\ai_workspace\project_space\模型权重\`
> **状态**：权重已下载完成，运行时库尚未安装（因此当前仍走确定性降级后端）

---

## 1. 为什么需要本地检索模型

系统里有两个"模型"，职责完全不同，不要混淆：

| | 大语言模型（LLM） | 检索模型（本项目要本地跑的） |
|---|---|---|
| 例子 | DeepSeek-V3 / GPT-4o / Qwen-Max | **BGE-M3**（嵌入）、**bge-reranker-v2-m3**（重排） |
| 作用 | 生成回答文本 | 把文本转成向量、给候选排序 |
| 规模 | 数十亿~数千亿参数 | 约 5.68 亿参数 |
| 部署方式 | **走 API 接口**（WB-10 前台配置） | **本地**（本指南） |

**为什么检索不放 API**：检索是每次提问都要做的高频操作，本地跑零调用成本、低延迟、数据不出境。

**当前降级状态**（未装运行时库时）：

| 环节 | 降级后端 | 差异 |
|---|---|---|
| 嵌入 | 字符 n-gram 哈希（768 维） | **靠字面重叠**，不是语义相似 —— 问"如何提高团队协作"检索不到"改善跨职能配合" |
| 重排 | 词法覆盖率打分 | 非交叉编码精排 |

两者都会如实上报 `degraded=true`（DEBT-010 / DEBT-011），**不伪造质量**。

## 2. 已下载的权重

```
E:\ai_workspace\project_space\模型权重\
├── bge-m3\                     30 文件 · 4,374.8 MB   （嵌入，1024 维）
└── bge-reranker-v2-m3\         14 文件 · 2,187.3 MB   （重排，CrossEncoder）
```

下载方式（可复现，走国内 ModelScope 源；hf-mirror 在本机不稳定）：

```powershell
# 1) 安装下载器
pip install modelscope

# 2) 下载（local_dir 直指目标目录）
python -c "from modelscope import snapshot_download as d; d('BAAI/bge-m3', local_dir=r'E:\ai_workspace\project_space\模型权重\bge-m3')"
python -c "from modelscope import snapshot_download as d; d('BAAI/bge-reranker-v2-m3', local_dir=r'E:\ai_workspace\project_space\模型权重\bge-reranker-v2-m3')"
```

> 实测：ModelScope 首连偶发停滞，重跑即断点续传；若仍卡住，可用 curl 直连：
> `curl.exe -L -sS -C - -o <目标文件> "https://modelscope.cn/api/v1/models/BAAI/bge-reranker-v2-m3/repo?Revision=master&FilePath=model.safetensors"`

## 3. 启用步骤

### 步骤 1 · 安装运行时库（**必需**，约 2-3 GB）

```powershell
cd services\python\nlp-service
.\.venv\Scripts\python.exe -m pip install sentence-transformers
```

> 该库会连带安装 PyTorch（CPU 版约 2.5 GB）。代码用 `find_spec("sentence_transformers")` 探测，
> **未安装时自动走降级后端**，所以这一步不做也不影响系统运行。

### 步骤 1.1 · ⚠ torch 版本硬约束（2026-09-20 实测，踩了两轮）

| 约束 | 原因 |
|---|---|
| `torch >= 2.6` | transformers（4.5x 与 5.x 均如此）为修复 CVE-2025-32434 强制要求；低于此版本加载 `.bin` 权重直接抛 `ValueError ... require users to upgrade torch to at least v2.6` |
| `torch <= 2.7` | torch ≥2.8 在 **Python 3.10** 上 DLL 初始化失败（`OSError: [WinError 1114] ... c10.dll`） |

**结论与推荐**：

- **Python 3.10 环境（本机现状）**：必须锁定 `torch>=2.6,<2.8`，实测 `2.7.1+cpu` 可用
  ```powershell
  .\.venv\Scripts\python.exe -m pip install "torch>=2.6,<2.8" sentence-transformers
  ```
- **Python 3.12 环境（与 CI 一致）**：无上述上限，可直接装最新 torch —— **推荐升级到 3.12 以消除该约束**

**另一个相关坑**：只有 `pytorch_model.bin` 的模型会走 `torch.load` 从而触发版本检查；
**safetensors 格式不受影响**（错误信息明确写了）。本项目 reranker 是 safetensors；
BGE-M3 官方仓库只有 `.bin`（ModelScope 与 hf-mirror 均无 safetensors，实测 HTTP 404）。

**本机实测结果（torch 2.7.1+cpu）**：

```
Embedding: backend=bge-m3            dim=1024  degraded=False
Reranker : backend=bge-reranker-v2-m3          degraded=False
```

### 步骤 1.2 · 本机 Python 版本清单与 **3.12 环境验证结果**（2026-09-20）

本机 `py` launcher 不可用，Python 由 uv 统一管理。实测可用解释器：

| 解释器 | 版本 | 说明 |
|---|---|---|
| `E:\software\anaconda3\python.exe` | 3.10.9 | PATH 首位，**当前默认**；torch 受 2.6~2.7 夹逼 |
| `C:\Users\46973\.local\bin\python3.12.exe` | **3.12.13** | uv 安装，venv/pip 完整，**推荐** |
| `C:\Users\46973\.local\bin\python3.11.exe` | 3.11.x | uv 安装（未细验） |

**3.12 环境验证结果**（建在仓库外 `E:\ai_workspace\project_space\ai-agent-lifeform\venv312`，不影响现有环境）：

```text
python : 3.12.13
torch  : 2.14.0+cpu     ← 3.10 上会 DLL 初始化失败，3.12 正常加载
tf     : 5.17.0
st     : 6.1.0

Embedding: bge-m3               dim=1024  degraded=False
Reranker : bge-reranker-v2-m3             degraded=False
pytest   : 199 passed / 1 skipped
```

**结论：3.12 是推荐环境** —— 与 CI 完全一致、**无 torch 版本上限**、模型与测试全部通过。
若继续用 3.10，则必须遵守 §步骤 1.1 的 `torch>=2.6,<2.8` 约束。

**切换到 3.12 的做法**（验证通过后再执行）：

```powershell
# 1) 建正式 venv（仓库约定路径）
& "C:\Users\46973\.local\bin\python3.12.exe" -m venv services\python\venv
# 2) 装依赖（顺序：项目依赖 → sentence-transformers）
services\python\venv\Scripts\python.exe -m pip install -r services\python\nlp-service\requirements.txt -r services\python\nlp-service\requirements-dev.txt
services\python\venv\Scripts\python.exe -m pip install sentence-transformers
# 3) 设本地权重路径并验证
$env:EMBEDDING_MODEL_PATH = "E:/ai_workspace/project_space/模型权重/bge-m3"
$env:RERANKER_MODEL_PATH  = "E:/ai_workspace/project_space/模型权重/bge-reranker-v2-m3"
cd services\python\nlp-service; ..\venv\Scripts\python.exe -m pytest -q
```

### 步骤 2 · 指向本地权重（**必需**）

代码已支持环境变量覆盖（本指南配套改造）：

```powershell
$env:EMBEDDING_MODEL_PATH  = "E:/ai_workspace/project_space/模型权重/bge-m3"
$env:RERANKER_MODEL_PATH   = "E:/ai_workspace/project_space/模型权重/bge-reranker-v2-m3"
```

不设这两个变量时，默认仍按 HF 模型名（`BAAI/bge-m3`）联网加载 —— 内网环境必须设置。

### 步骤 3 · 验证加载

```powershell
cd services\python\nlp-service
.\.venv\Scripts\python.exe -c "from app.embedding import EmbeddingService as E; s=E(); print(s.status())"
```

期望：`degraded=False`，`dim=1024`，`engine` 为 BGE-M3。
（若仍显示 hash 后端，检查 `EMBEDDING_MODEL_PATH` 是否正确、sentence-transformers 是否装好。）

### 步骤 4 · **全量重索引**（切换的必要代价，不可跳过）

向量维度会从 **768 → 1024**，Qdrant 集合必须重建：

```bash
# 1) 停 body-service
# 2) 删除旧集合（维度不兼容）
# 3) 重启服务，让它按新维度建集合
# 4) 重新灌入全部知识（旧向量不可复用）
```

**为什么不能省**：新旧向量混存会导致检索结果错乱（不同维度/不同语义空间无法比较），
而且这种错误**不会报错**，只会让召回质量静默下降。

### 步骤 5 · 评测回归

- 跑 Phase 3 已登记的评测集，对比切换前后的召回命中率与排序质量
- 人工抽查 10-20 个**语义相近但用词不同**的问题（这正是本次切换要改善的场景）
- 结果记入 `docs/优化日志/`，并更新《技术债台账》DEBT-010/011 状态

## 4. 回滚

| 场景 | 动作 |
|---|---|
| 加载失败 / 内存不足 | 取消两个环境变量（回到 HF 名称）或卸载 sentence-transformers → 自动降级回 hash 后端 |
| 重索引中途失败 | 保留旧集合快照；恢复后重新灌入 |
| 质量不升反降 | 同上回滚；把评测对比结果记为"切换未达标"而不是悄悄回退 |

## 5. 资源占用参考

| 项 | 磁盘 | 运行时内存 |
|---|---|---|
| BGE-M3（fp32） | 4.37 GB（含 onnx 副本，PyTorch 版约 2.2 GB） | 约 2-3 GB |
| bge-reranker（fp32） | 2.19 GB | 约 2-3 GB |
| sentence-transformers + torch | 约 2.5 GB | — |

> 首次加载需数秒；CPU 推理可用但较慢，有 GPU 会显著加速。
> 若磁盘紧张，可删除 `bge-m3` 下的 ONNX 副本（`model.onnx_data`，约 2.1 GB）—— 当前代码用 PyTorch 路径，不需要它。

## 6. 装了运行时库之后的测试注意事项

安装 sentence-transformers **之后**，跑 pytest 必须显式指定本地权重路径，否则测试中的引擎初始化会
尝试联网拉取 `BAAI/bge-m3`（HF 官方源在本机 443 不通）→ 表现为**测试长时间卡住**：

```powershell
$env:EMBEDDING_MODEL_PATH = "E:/ai_workspace/project_space/模型权重/bge-m3"
$env:RERANKER_MODEL_PATH  = "E:/ai_workspace/project_space/模型权重/bge-reranker-v2-m3"
.\.venv\Scripts\python.exe -m pytest -q      # 实测 199 passed / 1 skipped（含模型加载约 65s）
```

CI 未安装该可选依赖，因此 CI 不受影响（仍走降级后端、快速通过）。
