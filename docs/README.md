# docs · 目录地图

> Agent-Lifeform 项目文档归档总入口
> **口径时间**：2026-09-18 · **代码基线**：`6616616`

本目录是**项目过程资产**的归档地。代码回答"怎么做"，这里回答"做到哪了、凭什么算做完"。

---

## 阅读顺序（新人接手 / 阶段汇报都适用）

| 顺序 | 文档 | 回答什么 |
|---|---|---|
| 1 | **`项目进度总览.md`** | 现在做到哪了？哪些功能可用？测试基线是多少？还差什么？ |
| 2 | **`功能开发流程.md`** | 每个功能从需求到验收的完整链路怎么走？新增功能要动哪些文件？ |
| 3 | **`技术债台账.md`** | 哪些是"先用简化版顶着"的？`// DEBT-00X` 到底指什么？到点该换什么？ |
| 4 | **`异常流程归纳.md`** | 出错了该看哪一层？404 / 405 / 401 / 降级分别是什么意思？研发期踩过的坑有没有清单？ |
| 5 | **`proto契约使用说明.md`** | 6 份 proto 怎么用？现在到底有没有被调用？改了契约要动哪几步？ |
| 6 | `java-services/` | 每个 Java 源文件是干什么的？改这里要注意什么？ |
| 7 | `项目进度日志报告/` | 每个阶段具体做了哪些事、遇到什么问题、怎么解决的？ |
| 8 | `demo/` | 怎么把系统跑起来、怎么亲眼看到效果？ |
| 9 | `优化日志/` | 每轮工程化加固改了什么、为什么改？ |
| 10 | `../db/` | 数据存在哪？表/键/向量集合结构是什么？怎么备份恢复？ |

---

## 目录清单

| 路径 | 定位 | 何时维护 |
|---|---|---|
| `项目进度总览.md` · `.html` | **当前进度总览**：阶段状态、功能矩阵、测试基线、分支与远端拓扑、剩余项与已知缺口 | 每次收口 |
| `功能开发流程.md` · `.html` | **功能开发流程**：通用 8 步流水线、按技术栈的差异、逐功能开发步骤与验收标准、新增功能清单、本机特有坑 | 流程或功能变化时 |
| `技术债台账.md` · `.html` | **技术债权威台账**：`DEBT-00X` 全量清单（简化实现 / 正式目标 / 触发点 / 当前状态 + 源码证据）、关联未实现项、一致性问题登记、代码标记索引 | 创建或关闭任何债务时 |
| `异常流程归纳.md` · `.html` | **异常流程总图**：运行期异常流程（统一错误信封 / 路由层 404·405·415 分流 / 鉴权不变量 / 逐服务降级 / planned 三态判定 / 排障决策树）+ 研发期过程异常 30 条 + 规范动作清单 | 发现新异常模式或调整错误语义时 |
| `proto契约使用说明.md` · `.html` | **proto 契约使用说明与现状**：6 份契约清单、gRPC 引用矩阵（实证）、Java/Python 双侧生成链路与本机坑、变更 6 步流程、`contract-check.py` 覆盖范围、**产物同步门禁 `proto-sync-check.sh`**、与 OpenAPI 分工、10 项缺口登记 | 契约增删改或接入方变化时 |
| `项目进度日志报告/` | **分阶段过程留痕**：每阶段三件套（阶段性报告 / 开发执行日志 / 测试验收报告），各含 HTML；含目录索引 `README.md` | 每阶段收口 |
| `java-services/` | **Java 逐文件说明**：8 篇文档覆盖 106 个手写源文件（81 主 + 25 测），90 个 proto 生成文件按契约维度说明；08 篇为数据存储总纲 | 增删 Java 文件后（配 `scripts/java-doc-coverage.py` 自查） |
| `../db/` | **数据存储归档**：PG/Redis/Qdrant/暂存 4 份结构文档 + 运行态快照（`snapshots/`，只增不改）与恢复指引 | 存储结构变更时同步结构文档；需要新快照时带时间戳追加 |
| `demo/` | **端到端演示脚本**：`Phase0-DEMO.md` / `Phase1-DEMO.md` / `Phase2-DEMO.md` | 每阶段收口 |
| `优化日志/` | **工程化加固日志**：按日期命名，记录 P0/P1/P2 项的处理与验证 | 每轮加固后 |

其余（`dev分支代码分析与进度验证-2026-09-12.md` 等）为**当期分析快照**，属历史留痕，**不回改口径**。

---

## 生成 HTML 镜像

`.md` 改动后**必须重新生成对应 `.html` 并一并提交**，否则版本漂移。

```bash
# 批量（处理目标目录「当层」的 .md，自动排除 README.md；不递归子目录）
PYTHONPATH="E:\AI\核心知识\.workbuddy\tmp\pylibs" \
  C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe \
  scripts/md2html-report.py --all "docs/项目进度日志报告"

# 单文件
PYTHONPATH="E:\AI\核心知识\.workbuddy\tmp\pylibs" \
  C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe \
  scripts/md2html-report.py "docs/项目进度总览.md"

# 覆盖标题 / 指定输出路径（仅单文件模式）
... scripts/md2html-report.py "docs/项目进度总览.md" -t "标题" -o "docs/out.html"
```

> **本机环境变更（2026-09-18）**：原先用的 anaconda（`E:/software/anaconda3/python.exe`）**已卸载**，
> 本机现只有 `E:/software/python/python312`（无 `markdown` 包）。
> 因此 HTML 生成统一走 **托管 Python + `PYTHONPATH` 指向 `pylibs`**（内含 markdown 3.10.3）：
> `pip install --target "E:\AI\核心知识\.workbuddy\tmp\pylibs" markdown`（不污染系统环境）。
> 中文路径需加引号。
>
> **脚本没有 `--dry-run`**，重新生成是**幂等覆盖**（直接重写目标 `.html`）：
> 改完 `.md` 就重跑一次，再用 `git status --short` 看哪些 `.html` 被改动即可确认同步范围。
> `--all` **只扫当层目录**，`docs/java-services/`、`docs/demo/` 这类子目录需各自传参。

---

## 归档维护要求

- [ ] Markdown 更新后同步生成 HTML 镜像
- [ ] 新增文档在本 README 与 `项目进度总览.md` 的索引中登记
- [ ] 新增 / 关闭技术债时同步更新 `技术债台账.md`（状态 + 关闭依据 + 日期），代码里用 `// DEBT-00X` 标注
- [ ] 增删改 `proto/` 契约或出现新的接入方时，同步更新 `proto契约使用说明.md` 的契约清单与引用矩阵
- [ ] 改动 `proto/` 后运行 `bash scripts/proto-sync-check.sh all` 确认生成物已同步（CI 亦有 `proto-check-python` / `proto-check-java` 门禁）
- [ ] 单文件命名保持简洁（阶段文档用 `Phase<N>-*.md`，日期型用 `YYYY-MM-DD-*.md`）
- [ ] 未通过项 / 待补验证项**必须显式登记**，不得省略 —— **诚实留痕优先于好看**
- [ ] 新增服务或写路径后，核对错误响应是否为统一信封 `{code,message,details}`；
      安全边界按**方法**判定（见 `异常流程归纳.md` §4.2），并复核该文档 §5 未闭合项
- [ ] 历史归档不回改口径；旧数据保留原样，新口径写在当期文档并标注时间

---

*本目录索引建立于 2026-09-17 · 维护：探索者一号（WorkBuddy 侧）*
